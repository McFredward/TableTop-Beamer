---
phase: 29
plan: 04
type: execute
wave: 2
depends_on: [W0, 01, 02, 03]
files_modified:
  - server.mjs
  - src/app/runtime/state/runtime-board-profiles.js
  - src/app/runtime/state/runtime-play-area-geometry.js
  - src/app/runtime/state/runtime-board-state-accessors.js
  - src/app/runtime/animation/runtime-room-management.js
  - src/app/runtime/state/runtime-board-switch.js
  - src/app/lib/persistence/board-profiles.js
  - src/app/lib/state/runtime-state.js
  - src/app/runtime/core/runtime-bootstrap.js
  - src/app/lib/domain/rooms.js
  - src/app/lib/domain/polygon-contract.js
  - src/app/runtime/polygon-editor/runtime-polygon-undo.js
  - test/phase-29-dead-grep.test.mjs
  - test/board-profile-fields.test.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "`grep -rn '\\bplayAreaPolygon\\b' src/ server.mjs` returns 0 hits"
    - "`grep -rn '\\bdeletedRoomIds\\b\\|\\broomTombstones?\\b\\|markRoomTombstone\\|clearRoomTombstone' src/ server.mjs` returns 0 hits OR a documented post-cleanup count of zero"
    - "`runtime-polygon-undo.js` uses room-catalog mutations directly (no tombstone calls); undo/redo of room-delete still works"
    - "`getShipPolygonPoints` and downstream renderers continue to read from `state.playAreasByBoard[boardId][selectedPlayAreaId].polygon` only"
    - "BOARD_PROFILE_FIELDS shrunk to exactly 11 entries: hitareaCalibration, roomGeometry, specialPolygons, playAreas, selectedPlayAreaId, outsideFx, insideFx, roomFx, defaultAnimations, frozenRooms, lastUsedProfileName"
    - "Test suite green: `node --test test/` reports `fail 0`"
    - "Two skip gates in `test/phase-29-dead-grep.test.mjs` removed (`playAreaPolygon`, `deletedRoomIds`)"
    - "The `Phase-29 W2: BOARD_PROFILE_FIELDS contains only LIVE fields` test in `board-profile-fields.test.mjs` now PASSES (skip gate removed)"
  artifacts:
    - path: "server.mjs"
      provides: "BOARD_PROFILE_FIELDS at exactly 11 LIVE entries"
      contains: "BOARD_PROFILE_FIELDS"
    - path: "src/app/runtime/state/runtime-play-area-geometry.js"
      provides: "Single-source play-area polygon resolution via `state.playAreasByBoard`; no `playAreaPolygon` fallback chain"
    - path: "src/app/runtime/polygon-editor/runtime-polygon-undo.js"
      provides: "Undo/redo room-delete via direct room-catalog mutation (no tombstone API)"
  key_links:
    - from: "renderers"
      to: "state.playAreasByBoard[boardId][selectedPlayAreaId].polygon"
      via: "getShipPolygonPoints() / getPlayAreaClipPolygons()"
      pattern: "getSelectedPlayArea\\?\\.polygon|getPlayAreaClipPolygons"
    - from: "runtime-polygon-undo.js"
      to: "board.rooms direct mutation"
      via: "filter + push (no tombstone array)"
      pattern: "board\\.rooms"
---

<objective>
Wave 2 (cleanup batch 3) — Drop the two remaining REDUNDANT board fields:
1. **`playAreaPolygon`** (board) — F4 in 29-AUDIT.md. Single-polygon legacy
   field made redundant by `playAreas[]` (Phase 8 multi-play-area + Phase 10
   HF8 canonical resolver). Render reads come from
   `state.playAreasByBoard`; the `playAreaPolygon` is fallback-chain noise.
2. **`deletedRoomIds`** (board) — F5 in 29-AUDIT.md. Phase-26 unified-board
   model removes rooms directly from `roomCatalog[]`; tombstones are appended
   defensively but never gate reads. The undo trace in 29-AUDIT.md §3
   confirms `runtime-polygon-undo.js` uses room-catalog mutations + IDs, NOT
   the tombstone array.

This is the final source-cleanup wave. After this, BOARD_PROFILE_FIELDS is at
its target 11-entry LIVE-only shape, and the `Phase-29 W2` skip gate in
`board-profile-fields.test.mjs` flips to PASS.

Output: ~12 source files modified; 3 test skip-gates removed (2 in dead-grep
+ 1 in board-profile-fields); suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/phase-29/29-CONTEXT.md
@.planning/phases/phase-29/29-RESEARCH.md
@.planning/phases/phase-29/29-AUDIT.md
@.planning/phases/phase-29/29-02-SUMMARY.md
@.planning/phases/phase-29/29-03-SUMMARY.md
@server.mjs
@src/app/runtime/state/runtime-board-profiles.js
@src/app/runtime/state/runtime-play-area-geometry.js
@src/app/runtime/state/runtime-board-state-accessors.js
@src/app/runtime/animation/runtime-room-management.js
@src/app/runtime/state/runtime-board-switch.js
@src/app/lib/persistence/board-profiles.js
@src/app/lib/state/runtime-state.js
@src/app/runtime/core/runtime-bootstrap.js
@src/app/lib/domain/rooms.js
@src/app/lib/domain/polygon-contract.js
@src/app/runtime/polygon-editor/runtime-polygon-undo.js
@test/phase-29-dead-grep.test.mjs
@test/board-profile-fields.test.mjs

<interfaces>
<!-- BOARD_PROFILE_FIELDS expected post-this-plan (the final shape): -->
```javascript
const BOARD_PROFILE_FIELDS = Object.freeze([
  "hitareaCalibration",
  "roomGeometry",
  "specialPolygons",
  "playAreas",
  "selectedPlayAreaId",
  "outsideFx",
  "insideFx",
  "roomFx",
  "defaultAnimations",
  "frozenRooms",
  "lastUsedProfileName",
]);
```

<!-- playAreaPolygon call sites (RESEARCH §F4 + grep verified): -->
- server.mjs (BOARD_PROFILE_FIELDS literal)
- runtime-board-profiles.js:42, 68, 238 (synthesis writes the redundant field)
- runtime-play-area-geometry.js:166, 168, 198, 217, 223, 225, 226, 232, 234, 245
  (fallback chains in mergePolygonPrecedence + resolveProfilePolygonContract)
- lib/persistence/board-profiles.js:51, 88, 95, 97, 126 (legacy candidate)
- lib/domain/polygon-contract.js:261, 267, 269-270, 276, 278, 344 (fallback)

<!-- deletedRoomIds / roomTombstones call sites (RESEARCH §F5): -->
- server.mjs (BOARD_PROFILE_FIELDS literal)
- runtime-state.js (state.roomTombstonesByBoard slice — verify exact line via grep)
- runtime-bootstrap.js (default builder, around line 120-130)
- runtime-board-state-accessors.js (markRoomTombstone, clearRoomTombstone, hydrateRoomTombstones)
- runtime-room-management.js:619 (markRoomTombstone call on room-delete)
- runtime-play-area-geometry.js:99-188 (filterRoomCatalogByDeletedIds + tombstone hydration)
- runtime-board-switch.js:155-170 (board-switch hydration)
- runtime-board-profiles.js:161, 180 (apply path)
- lib/domain/rooms.js:112 (mergeRoomCatalog accepts deletedRoomIds — drop the parameter)
- lib/persistence/board-profiles.js:110 (legacy load)
- runtime-polygon-undo.js:66 (clearRoomTombstone call on undo restore)
- runtime-polygon-undo.js:77 (markRoomTombstone call on undo remove)

<!-- The undo flow in runtime-polygon-undo.js (29-AUDIT.md §3 verdict): -->
The undo flow operates on `board.rooms` directly:
- `restoreState`: filters `board.rooms` to only restored IDs, then pushes back missing rooms.
- The tombstone calls (`markRoomTombstone`, `clearRoomTombstone`) are book-keeping that
  affects ONLY `state.roomTombstonesByBoard` — which has zero downstream read consumers
  (per RESEARCH §F5).
- Removing the tombstone calls makes undo/redo PURE room-catalog mutation. No regression.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop `playAreaPolygon` fallback chains + synthesis writes</name>
  <read_first>
    - .planning/phases/phase-29/29-AUDIT.md §F4 (playAreaPolygon classification)
    - .planning/phases/phase-29/29-RESEARCH.md §F4 lines 465-489
    - src/app/runtime/state/runtime-play-area-geometry.js (lines 90-260 — full mergePolygonPrecedence + resolveProfilePolygonContract)
    - src/app/runtime/state/runtime-board-profiles.js (lines 36-260 — find synth lines 42, 68, 238)
    - src/app/lib/persistence/board-profiles.js (full file — find legacy reads 51, 88, 95, 97, 126)
    - src/app/lib/domain/polygon-contract.js (lines 250-360 — find fallback chain 261, 267, 269-270, 276, 278, 344)
    - server.mjs lines 39-58 (BOARD_PROFILE_FIELDS — `playAreaPolygon` is at line 45)
    - src/app/runtime/render/runtime-canvas-clip.js (lines 100-150 — VERIFY this file is unchanged; render reads via getPlayAreaClipPolygons → state.playAreasByBoard)
  </read_first>
  <behavior>
    All `playAreaPolygon` reads/writes removed.

    - `BOARD_PROFILE_FIELDS` no longer contains `"playAreaPolygon"`.
    - `runtime-board-profiles.js` synthesis no longer emits `playAreaPolygon: ...`
      into the per-board profile (so on-disk JSONs stop accumulating the redundant field).
    - `runtime-play-area-geometry.js` `mergePolygonPrecedence` and
      `resolveProfilePolygonContract` no longer chain through `playAreaPolygon`.
      They read directly from `playAreas[]` (already done in normal path; just remove
      the fallback `??` operands).
    - `polygon-contract.js` fallback chain straightened to single-source `playAreas[]`.
    - `lib/persistence/board-profiles.js` legacy reads removed.

    NON-REGRESSION GATES:
    - `runtime-canvas-clip.js` BYTE-UNCHANGED (it reads via `getPlayAreaClipPolygons` →
      `getPlayAreas(boardId)` → `state.playAreasByBoard`; never touches the legacy field).
    - `getShipPolygonPoints()` in `runtime-play-area-geometry.js:410` continues to
      read `getSelectedPlayArea()?.polygon` (verify it's untouched).
  </behavior>
  <files>
    server.mjs,
    src/app/runtime/state/runtime-board-profiles.js,
    src/app/runtime/state/runtime-play-area-geometry.js,
    src/app/lib/persistence/board-profiles.js,
    src/app/lib/domain/polygon-contract.js
  </files>
  <action>
    **Step 1.1 — Drop the synthesis writes:**
    - `src/app/runtime/state/runtime-board-profiles.js`:
      - Line 42: delete `playAreaPolygon: ctx.normalizeShipPolygon(ctx.SHIP_POLYGON_DEFAULT),`
      - Line 68: delete `playAreaPolygon: ctx.normalizeShipPolygon(state.shipPolygonsByBoard[board.id]),`
      - Line 238: in the `mergeProfilePolygon`-style block, delete the entire
        `profile.playAreaPolygon ?? profile.shipPolygon ?? profile.shipMask ?? ctx.SHIP_POLYGON_DEFAULT`
        chain. If `playAreaPolygon` is the only one of the three, replace with
        `ctx.SHIP_POLYGON_DEFAULT` (or the surrounding fallback). Verify intent
        by reading the surrounding 10-line context first.

    **Step 1.2 — Strip the fallback chain in mergePolygonPrecedence:**
    - `src/app/runtime/state/runtime-play-area-geometry.js`:
      - Lines 166-169: in `mergePolygonPrecedence`, the chain
        `primary.playAreaPolygon ?? fallback.playAreaPolygon ?? primary.shipPolygon ?? ...`
        loses both `playAreaPolygon` operands. The chain straightens to
        `primary.shipPolygon ?? fallback.shipPolygon ?? ...` (verify with surrounding context;
        if `shipPolygon` is itself dead, escalate before deleting it).
      - Line 198: in `resolveProfilePolygonContract`, replace
        `playAreaPolygon: normalizeShipPolygon(selectedPlayArea?.polygon ?? ctx.SHIP_POLYGON_DEFAULT),`
        with — actually, since the OUTPUT field is also `playAreaPolygon`, this whole
        construction is producing the deprecated key. Remove the property assignment
        from the returned object literal entirely. The downstream code reads `selectedPlayArea?.polygon`
        directly anyway (see `getShipPolygonPoints` at line 410).
      - Lines 217-235: in `resolveProfilePolygonContract`'s candidate-resolution chain,
        delete every `??` operand that reads `*.playAreaPolygon`. Result: the chain
        reads only from `selectedPlayArea?.polygon` and the explicit fallback
        `ctx.SHIP_POLYGON_DEFAULT`.
      - Line 245: same pattern, remove `candidateFallbackPolygon` if it was assembled
        from `playAreaPolygon` reads above.

    **Step 1.3 — Strip polygon-contract fallback chain:**
    - `src/app/lib/domain/polygon-contract.js` lines 261, 267, 269-270, 276, 278, 344:
      remove every `playAreaPolygon` operand from the fallback `??` chains. The
      contract should resolve from `playAreas[i].polygon` directly.

    **Step 1.4 — Strip legacy persistence reads:**
    - `src/app/lib/persistence/board-profiles.js` lines 51, 88, 95, 97, 126:
      remove the `value.playAreaPolygon` reads from candidate-shape detection. The
      legacy candidate shape no longer carries a `playAreaPolygon` field.

    **Step 1.5 — Drop from BOARD_PROFILE_FIELDS:**
    - `server.mjs` line 45: delete `  "playAreaPolygon",`. Result: BOARD_PROFILE_FIELDS = 12 entries.

    **Step 1.6 — Verify zero remaining src references:**
    ```bash
    grep -rn "\bplayAreaPolygon\b" src/ server.mjs
    ```
    Expected: 0 hits.

    **Step 1.7 — Run suite + smoke-check renderer reads:**
    ```bash
    node --test test/ 2>&1 | tail -5
    grep -n "getPlayAreaClipPolygons\|getSelectedPlayArea" src/app/runtime/render/runtime-canvas-clip.js
    ```
    Expected: suite green; renderer reads still go through `getPlayAreaClipPolygons` /
    `state.playAreasByBoard` (the canonical post-Phase-26 resolver is intact).
  </action>
  <verify>
    <automated>node --test test/ 2>&1 | tail -5 && echo "---" && grep -rn "\bplayAreaPolygon\b" src/ server.mjs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\bplayAreaPolygon\b" src/ server.mjs` returns 0 hits
    - `grep -c '"playAreaPolygon"' server.mjs` returns 0
    - BOARD_PROFILE_FIELDS now has 12 entries
    - `runtime-canvas-clip.js` byte-unchanged (`git diff src/app/runtime/render/runtime-canvas-clip.js` empty)
    - `getShipPolygonPoints` in `runtime-play-area-geometry.js` still reads
      `getSelectedPlayArea()?.polygon` (verify by `grep`)
    - `node --test test/` exits 0 with `fail 0`
  </acceptance_criteria>
  <done>`playAreaPolygon` legacy fallback chains removed; renderers continue via canonical `playAreas[]` resolver.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop `deletedRoomIds` / tombstone plumbing + rewire undo to direct mutation + flip 3 test skip gates</name>
  <read_first>
    - .planning/phases/phase-29/29-AUDIT.md §3 (deletedRoomIds undo trace verdict — must be REDUNDANT)
    - .planning/phases/phase-29/29-RESEARCH.md §F5 lines 491-521
    - .planning/phases/phase-29/29-RESEARCH.md §"Pitfall 1" + §"Open Question 1"
    - src/app/runtime/polygon-editor/runtime-polygon-undo.js (full file — confirm undo uses board.rooms mutation, not state.roomTombstonesByBoard reads)
    - src/app/runtime/state/runtime-play-area-geometry.js lines 90-200 (filterRoomCatalogByDeletedIds + hydrateRoomTombstones)
    - src/app/runtime/state/runtime-board-state-accessors.js (markRoomTombstone, clearRoomTombstone, hydrateRoomTombstones definitions)
    - src/app/runtime/state/runtime-board-switch.js lines 150-180 (board-switch hydration)
    - src/app/runtime/state/runtime-board-profiles.js (lines 161, 180 — apply path)
    - src/app/runtime/animation/runtime-room-management.js line 619 (markRoomTombstone call)
    - src/app/lib/domain/rooms.js line 112 (mergeRoomCatalog signature with deletedRoomIds parameter)
    - src/app/lib/persistence/board-profiles.js line 110 (legacy load)
    - src/app/lib/state/runtime-state.js (find roomTombstonesByBoard slice)
    - src/app/runtime/core/runtime-bootstrap.js (find tombstone default builder ~line 110-145)
    - test/phase-29-dead-grep.test.mjs (skip gates for `deletedRoomIds` to remove)
    - test/board-profile-fields.test.mjs (the appended `Phase-29 W2: BOARD_PROFILE_FIELDS contains only LIVE fields` test — its skip gate is removed in this task)
  </read_first>
  <behavior>
    Atomic removal of the entire tombstone plumbing.

    - `BOARD_PROFILE_FIELDS` no longer contains `"deletedRoomIds"`. Final 11 entries.
    - `state.roomTombstonesByBoard` slice removed.
    - `markRoomTombstone`, `clearRoomTombstone`, `hydrateRoomTombstones`, plus
      `filterRoomCatalogByDeletedIds` removed from accessors / play-area-geometry.
    - `runtime-room-management.js:619` call removed (the room is already dropped from
      `roomCatalog` at the surrounding line; the tombstone append is bookkeeping for
      a never-read map).
    - `runtime-polygon-undo.js`:
      - Remove `ctx.markRoomTombstone(...)` call at line 77.
      - Remove `ctx.clearRoomTombstone(...)` call at line 66.
      - Verify undo flow remains correct: it filters `board.rooms` by `restoredIds`
        (line 48) and pushes missing rooms back (lines 50-67) — that is the actual
        undo mechanism. Tombstone calls were redundant book-keeping.
    - `runtime-board-switch.js` hydration block stripped of tombstone reads.
    - `runtime-board-profiles.js` apply path lines 161, 180 stripped.
    - `mergeRoomCatalog` in `lib/domain/rooms.js:112` simplified (drop the
      `deletedRoomIds` parameter; update all call sites accordingly).
    - `lib/persistence/board-profiles.js:110` legacy read removed.

    Test skip gates flipped:
    - `test/phase-29-dead-grep.test.mjs`: `playAreaPolygon` test (Task 1) and
      `deletedRoomIds` test (this task) — both un-skipped.
    - `test/board-profile-fields.test.mjs`: the appended `Phase-29 W2:
      BOARD_PROFILE_FIELDS contains only LIVE fields` test — un-skipped (post-Wave-2
      shape achieved).

    NON-REGRESSION GATES:
    - Undo/redo of room-delete still works. Verify that
      `runtime-polygon-undo.js:restoreState` continues to push missing rooms back into
      `board.rooms` (lines 48-67) and the surrounding `persistBoardProfiles()` call
      at line 93 still runs.
    - `roomCatalog` mutation path in `runtime-room-management.js` is otherwise unchanged.
  </behavior>
  <files>
    server.mjs,
    src/app/runtime/state/runtime-board-profiles.js,
    src/app/runtime/state/runtime-play-area-geometry.js,
    src/app/runtime/state/runtime-board-state-accessors.js,
    src/app/runtime/animation/runtime-room-management.js,
    src/app/runtime/state/runtime-board-switch.js,
    src/app/lib/persistence/board-profiles.js,
    src/app/lib/state/runtime-state.js,
    src/app/runtime/core/runtime-bootstrap.js,
    src/app/lib/domain/rooms.js,
    src/app/runtime/polygon-editor/runtime-polygon-undo.js,
    test/phase-29-dead-grep.test.mjs,
    test/board-profile-fields.test.mjs
  </files>
  <action>
    **Step 2.1 — Strip undo's tombstone calls (do this FIRST so we know undo doesn't read them):**
    - `src/app/runtime/polygon-editor/runtime-polygon-undo.js`:
      - Line 66: delete `ctx.clearRoomTombstone(state.boardId, snap.id);`
      - Line 77: delete `ctx.markRoomTombstone(state.boardId, id);`
      - Verify the surrounding logic still works: the loop at lines 50-67 pushes
        missing rooms back into `board.rooms` from the snapshot — that is the actual
        restore. The tombstone calls were no-ops affecting only the never-read map.
    - Run `node --test test/` to confirm no regression. If a test fails, STOP — the
      audit was wrong about undo not depending on the tombstone state; escalate.

    **Step 2.2 — Strip the room-management call:**
    - `src/app/runtime/animation/runtime-room-management.js` around line 619:
      delete the `markRoomTombstone(boardId, id)` call. Confirm via
      `grep -n "markRoomTombstone\|clearRoomTombstone\|deletedRoomIds" src/app/runtime/animation/runtime-room-management.js`
      that zero hits remain after.

    **Step 2.3 — Strip the accessor-module helpers:**
    - `src/app/runtime/state/runtime-board-state-accessors.js`: delete the function
      bodies for `markRoomTombstone`, `clearRoomTombstone`, `hydrateRoomTombstones`.
      Also remove their entries from any `window.TT_BEAMER_*` exports / the ctx map.
      Find them with:
      ```bash
      grep -n "Tombstone" src/app/runtime/state/runtime-board-state-accessors.js
      ```

    **Step 2.4 — Strip ctx-builder re-exports in runtime-orchestration:**
    - `src/app/runtime/runtime-orchestration.js`: locate every map that re-exports
      `markRoomTombstone` / `clearRoomTombstone` / `hydrateRoomTombstones`. Use
      `grep -n "Tombstone" src/app/runtime/runtime-orchestration.js`. Delete each
      property entry. (May also need to remove from `runtime-polygon-undo.js`'s ctx
      destructuring at the top of the IIFE.)

    **Step 2.5 — Strip the play-area-geometry tombstone block:**
    - `src/app/runtime/state/runtime-play-area-geometry.js` lines 99-188:
      delete `filterRoomCatalogByDeletedIds`, `hydrateRoomTombstones`, and any
      surrounding helpers that exist solely for tombstone management. Find them with:
      ```bash
      grep -n "tombstone\|deletedRoomIds\|filterRoomCatalogByDeletedIds" src/app/runtime/state/runtime-play-area-geometry.js
      ```
      Verify call sites: if any consumer in the same file calls these helpers, remove
      the call too (the catalog should be used as-is — Phase 26 already removes rooms
      from it on delete).

    **Step 2.6 — Strip the slice + bootstrap:**
    - `src/app/lib/state/runtime-state.js`: delete the `roomTombstonesByBoard: {},`
      slice line.
    - `src/app/runtime/core/runtime-bootstrap.js`: delete the
      `state.roomTombstonesByBoard = ctx.createDefaultRoomTombstonesByBoard();` line
      and the `createDefaultRoomTombstonesByBoard` ctx-destructure entry. Use
      `grep -n "Tombstone" src/app/runtime/core/runtime-bootstrap.js`.

    **Step 2.7 — Strip apply path:**
    - `src/app/runtime/state/runtime-board-profiles.js` lines 161, 180:
      delete the apply-path lines that read `profiles?.[board.id]?.deletedRoomIds`
      and write `state.roomTombstonesByBoard[board.id] = ...`.

    **Step 2.8 — Strip board-switch hydration:**
    - `src/app/runtime/state/runtime-board-switch.js` lines 155-170:
      delete the block that hydrates `state.roomTombstonesByBoard` on board switch.

    **Step 2.9 — Strip mergeRoomCatalog tombstone parameter:**
    - `src/app/lib/domain/rooms.js` line 112:
      change the signature from
      `function mergeRoomCatalog(board, roomCatalog, deletedRoomIds)`
      to
      `function mergeRoomCatalog(board, roomCatalog)`.
      Inside the body, remove any `deletedRoomIds`-based filter. Then update the 1-3
      call sites identified by `grep -rn "mergeRoomCatalog" src/`.

    **Step 2.10 — Strip legacy persistence reads:**
    - `src/app/lib/persistence/board-profiles.js` line 110:
      delete the legacy `value.deletedRoomIds` read from candidate-shape detection.

    **Step 2.11 — Drop from BOARD_PROFILE_FIELDS:**
    - `server.mjs` line 40: delete `  "deletedRoomIds",`. Final BOARD_PROFILE_FIELDS = 11 entries.

    **Step 2.12 — Verify zero remaining tombstone references in source:**
    ```bash
    grep -rn "\bdeletedRoomIds\b\|\broomTombstones?\b\|markRoomTombstone\|clearRoomTombstone\|hydrateRoomTombstones\|filterRoomCatalogByDeletedIds" src/ server.mjs | grep -v test/
    ```
    Expected: 0 hits.

    **Step 2.13 — Flip 3 test skip gates:**
    - `test/phase-29-dead-grep.test.mjs`:
      - Remove `{ skip: "Wave 2 (29-04) not landed yet" }` from the `playAreaPolygon` test (Task 1's responsibility).
      - Remove `{ skip: "Wave 2 (29-04) gated on audit verdict" }` from the `deletedRoomIds` test.
    - `test/board-profile-fields.test.mjs`:
      - In the appended `Phase-29 W2: BOARD_PROFILE_FIELDS contains only LIVE fields`
        test, remove the `{ skip: "Wave 2 (29-02..29-04) not landed yet" }` opt.

    **Step 2.14 — Run full suite:**
    ```bash
    node --test test/ 2>&1 | tail -10
    ```
    Expected: `fail 0`. The newly un-skipped tests pass:
    - `playAreaPolygon` zero-hits ✓
    - `deletedRoomIds` zero-hits ✓
    - `BOARD_PROFILE_FIELDS contains only LIVE fields` ✓ (asserts list = the 11 LIVE fields)
  </action>
  <verify>
    <automated>node --test test/ 2>&1 | tail -10 && echo "---" && grep -rn "\bdeletedRoomIds\b\|\broomTombstones?\b\|\bplayAreaPolygon\b" src/ server.mjs | grep -v test/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\bdeletedRoomIds\b\|\broomTombstones?\b\|markRoomTombstone\|clearRoomTombstone\|filterRoomCatalogByDeletedIds" src/ server.mjs` (excluding test/) returns 0 hits
    - `grep -rn "\bplayAreaPolygon\b" src/ server.mjs` returns 0 hits (Task 1 already verified)
    - BOARD_PROFILE_FIELDS contains exactly 11 entries; verify with
      `awk '/BOARD_PROFILE_FIELDS = Object.freeze/,/\]\);/' server.mjs | grep -c '^\s*"'` returns 11
    - `lastUsedProfileName` (Phase 28 B1) still in BOARD_PROFILE_FIELDS
    - `assetManifest` / `hashByPath` (Phase 28 B5) untouched (`config/asset-manifest.json` byte-unchanged)
    - `node --test test/` exits 0 with `fail 0`
    - `node --test test/phase-29-dead-grep.test.mjs` reports BOTH `playAreaPolygon` and
      `deletedRoomIds` tests passing (no longer skipped)
    - `node --test test/board-profile-fields.test.mjs` reports 4 passing tests, 0 skipped
      (the new `Phase-29 W2` test passes — list matches the 11 LIVE field names exactly)
    - Manual undo/redo non-regression: not auto-verifiable, but the test suite green
      + the structural verification of `runtime-polygon-undo.js`'s `board.rooms` mutation
      lines being preserved provides confidence
  </acceptance_criteria>
  <done>
    All four DEAD/REDUNDANT board fields removed from source. BOARD_PROFILE_FIELDS at
    its target 11-entry LIVE-only shape. The disk-side fields STILL EXIST in
    `config/boards/*.json` — Wave 3 (29-05) handles the disk strip.
  </done>
</task>

</tasks>

<verification>
After both tasks:

```bash
node --test test/ 2>&1 | tail -10
grep -rn "\bplayAreaPolygon\b\|\bdeletedRoomIds\b\|\broomTombstones?\b\|markRoomTombstone\|clearRoomTombstone\|hiddenRoomNames\|roomStateProfiles\|animationSoundMap" src/ server.mjs | grep -v test/
awk '/BOARD_PROFILE_FIELDS = Object.freeze/,/\]\);/' server.mjs | grep -c '^\s*"'
git diff config/boards/ config/asset-manifest.json
```

Expected:
- suite green (`fail 0`);
- grep returns 0 hits (all 5 DEAD field references gone from source);
- count = 11 (BOARD_PROFILE_FIELDS at target shape);
- diff empty (disk untouched — Wave 3's job).
</verification>

<success_criteria>
- `playAreaPolygon` and `deletedRoomIds` plumbing removed from source
- BOARD_PROFILE_FIELDS at final shape: 11 entries, all LIVE
- Undo flow uses direct `board.rooms` mutation only (no tombstone calls)
- Phase 28 B1 `lastUsedProfileName` + Phase 28 B5 asset-manifest preserved
- 3 skip gates flipped (2 in dead-grep + 1 in board-profile-fields)
- Suite green: `node --test test/` `fail 0`
</success_criteria>

<output>
After completion, create `.planning/phases/phase-29/29-04-SUMMARY.md` with:
- Files modified + diff stat
- Suite count before/after
- Final BOARD_PROFILE_FIELDS list (11 entries) verbatim
- Confirmation that `runtime-polygon-undo.js` undo flow is preserved (cite line numbers of `board.rooms` mutation lines)
- Confirmation that `config/boards/*.json` and `config/asset-manifest.json` are byte-unchanged
- Wave 2 closure marker: source-tree cleanup is COMPLETE; Wave 3 (29-05) handles disk
</output>