---
phase: 29
plan: 02
type: execute
wave: 2
depends_on: [W0, 01]
files_modified:
  - server.mjs
  - src/app/lib/state/runtime-state.js
  - src/app/runtime/core/runtime-bootstrap.js
  - src/app/runtime/state/runtime-board-profiles.js
  - src/app/runtime/state/runtime-board-state-accessors.js
  - src/app/runtime/animation/runtime-room-management.js
  - src/app/runtime/runtime-orchestration.js
  - src/app/lib/persistence/board-profiles.js
  - test/phase-29-dead-grep.test.mjs
  - test/board-profile-fields.test.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "`hiddenRoomNames` removed from BOARD_PROFILE_FIELDS in server.mjs"
    - "`grep -rn '\\bhiddenRoomNames\\b' src/ server.mjs` returns zero hits"
    - "`roomStateProfiles` and all its plumbing (state slice, default builder, accessors, ctx-bindings, apply paths, delete-on-room-delete) removed from src/"
    - "`grep -rn '\\broomStateProfiles?\\b' src/ server.mjs` returns zero hits (excluding the BOARD_PROFILE_FIELDS literal which is also dropped)"
    - "Phase 28 acceptance non-regressed: `lastUsedProfileName` still in BOARD_PROFILE_FIELDS"
    - "Test suite green: `node --test test/` reports `fail 0`"
    - "Two skip gates in test/phase-29-dead-grep.test.mjs are removed (hiddenRoomNames + roomStateProfiles)"
    - "The new board-profile-fields skip gate stays IN PLACE — it un-skips only after 29-04 lands"
  artifacts:
    - path: "server.mjs"
      provides: "BOARD_PROFILE_FIELDS without `hiddenRoomNames` and `roomStateProfiles`"
      contains: "BOARD_PROFILE_FIELDS"
    - path: "src/app/lib/state/runtime-state.js"
      provides: "Initial state shape without `roomStateProfilesByBoard` slice"
    - path: "src/app/runtime/state/runtime-board-state-accessors.js"
      provides: "Accessor module without `getRoomStateProfile` / `setRoomStateProfile` exports"
  key_links:
    - from: "src/app/runtime/state/runtime-board-profiles.js"
      to: "BOARD_PROFILE_FIELDS"
      via: "iteration in extractProfileFromUnifiedBoard / persistBoardProfileToBoardFile / normalizeBoardDefinition"
      pattern: "for.*BOARD_PROFILE_FIELDS"
    - from: "test/phase-29-dead-grep.test.mjs"
      to: "src/"
      via: "structural grep proves DEAD field removal"
      pattern: "grepRecursive"
---

<objective>
Wave 2 (cleanup batch 1) — Drop two zero-consumer DEAD fields:
1. **`hiddenRoomNames`** (board) — F1 in 29-AUDIT.md. Zero source references
   outside `BOARD_PROFILE_FIELDS`. Pure config-list entry to delete.
2. **`roomStateProfiles`** (board) — F2 in 29-AUDIT.md. Plumbing-only field
   (state slice + accessors + bootstrap + apply path + delete-on-room-delete)
   with ZERO render or UI consumers. Per D-01: DEAD.

Per D-02: source-tree cleanup BEFORE disk migration (Wave 3). Per D-11:
test-coverage adjustments are at planner discretion — the plan flips two skip
gates in `phase-29-dead-grep.test.mjs` so the structural assertions go LIVE.

Output: ~7 source files modified; 2 test skip-gates removed; suite GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/phase-29/29-CONTEXT.md
@.planning/phases/phase-29/29-RESEARCH.md
@.planning/phases/phase-29/29-AUDIT.md
@.planning/phases/phase-29/29-W0-SUMMARY.md
@.planning/phases/phase-29/29-01-SUMMARY.md
@server.mjs
@src/app/lib/state/runtime-state.js
@src/app/runtime/core/runtime-bootstrap.js
@src/app/runtime/state/runtime-board-profiles.js
@src/app/runtime/state/runtime-board-state-accessors.js
@src/app/runtime/animation/runtime-room-management.js
@src/app/lib/persistence/board-profiles.js
@test/phase-29-dead-grep.test.mjs
@test/board-profile-fields.test.mjs

<interfaces>
<!-- BOARD_PROFILE_FIELDS expected post-this-plan (still has playAreaPolygon + deletedRoomIds; those drop in 29-04) -->
```javascript
const BOARD_PROFILE_FIELDS = Object.freeze([
  "deletedRoomIds",         // dropped in 29-04
  "hitareaCalibration",
  "roomGeometry",
  // "roomStateProfiles",   // ← dropped HERE (Task 2)
  "specialPolygons",
  "playAreaPolygon",        // dropped in 29-04
  "playAreas",
  "selectedPlayAreaId",
  "outsideFx",
  "insideFx",
  "roomFx",
  "defaultAnimations",
  "frozenRooms",
  // "hiddenRoomNames",     // ← dropped HERE (Task 1)
  "lastUsedProfileName",
]);
```

<!-- roomStateProfiles plumbing call sites identified by RESEARCH §F2 (lines 398-428): -->
- `runtime-state.js:73` → `roomStateProfilesByBoard: {}` slice
- `runtime-bootstrap.js:122` → `state.roomStateProfilesByBoard = ctx.createDefaultRoomStateProfilesByBoard();`
- `runtime-board-state-accessors.js:192-201` → `getRoomStateProfile`, `setRoomStateProfile`,
  plus supporting helpers (`normalizeRoomStateProfile`, `createDefaultRoomStateProfileMap`,
  `normalizeRoomStateProfileMap`, `createDefaultRoomStateProfilesByBoard`)
- `runtime-board-profiles.js:38, 60, 188-193` → apply path + synthesis
- `runtime-room-management.js:609-610` → `delete state.roomStateProfilesByBoard[boardId][id]`
  on room-delete
- `runtime-orchestration.js:896-897, 1888-1889, 2028-2029` → ctx-builder re-exports
- `lib/persistence/board-profiles.js:54, 118-119` → legacy candidate-shape reads
- `lib/shared/config.js:199-202` → default room state profile shape (drop the constant)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drop `hiddenRoomNames` from BOARD_PROFILE_FIELDS + un-skip dead-grep test</name>
  <read_first>
    - server.mjs lines 39-58 (BOARD_PROFILE_FIELDS literal)
    - .planning/phases/phase-29/29-AUDIT.md §F1 (hiddenRoomNames classification)
    - test/phase-29-dead-grep.test.mjs (the skip gate to remove)
  </read_first>
  <behavior>
    - server.mjs: `BOARD_PROFILE_FIELDS` no longer contains `"hiddenRoomNames"`.
    - test/phase-29-dead-grep.test.mjs: the test
      `"W2: hiddenRoomNames has zero src/ hits"` is no longer skipped and PASSES.
    - All other tests still pass.
    - No other source files touched (zero src-tree references to the field — confirmed by RESEARCH §F1).
  </behavior>
  <files>server.mjs, test/phase-29-dead-grep.test.mjs</files>
  <action>
    1. Edit `server.mjs`. In the `BOARD_PROFILE_FIELDS = Object.freeze([...])` block
       (lines 39-58), DELETE the line `  "hiddenRoomNames",` (currently at line 53).
       Leave all other entries unchanged. The Object.freeze + array structure stays
       intact.

    2. Edit `test/phase-29-dead-grep.test.mjs`. Locate the test:
       ```javascript
       test("W2: hiddenRoomNames has zero src/ hits", { skip: "Wave 2 (29-02) not landed yet" }, () => {
       ```
       Remove the `{ skip: "..." }` options object. Result:
       ```javascript
       test("W2: hiddenRoomNames has zero src/ hits", () => {
       ```

    3. Run `node --test test/phase-29-dead-grep.test.mjs` — the formerly-skipped test
       MUST now pass. If it fails (i.e. the field name still appears in src/), STOP
       and re-grep; do not proceed to Task 2.

    Why hiddenRoomNames is mechanically simple: RESEARCH §F1 confirmed
    `grep -rn "hiddenRoomNames" src/` returns zero hits. Dropping the
    BOARD_PROFILE_FIELDS line is a 1-line removal with no callsite cleanup.
    Disk cleanup happens in Wave 3 (29-05).
  </action>
  <verify>
    <automated>node --test test/ 2>&1 | tail -5 && grep -c '"hiddenRoomNames"' server.mjs && grep -rn "\bhiddenRoomNames\b" src/ server.mjs | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"hiddenRoomNames"' server.mjs` returns 0
    - `grep -rn "\bhiddenRoomNames\b" src/ server.mjs` returns 0 hits
    - `node --test test/phase-29-dead-grep.test.mjs` exits 0 with `pass >= 2` (1 liveness + the un-skipped check)
    - `node --test test/` exits 0 with `fail 0`
    - The test file no longer contains the literal `"Wave 2 (29-02) not landed yet"` skip reason
  </acceptance_criteria>
  <done>hiddenRoomNames is gone from source; structural test asserts the absence; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drop `roomStateProfiles` plumbing across ~7 files + un-skip dead-grep test</name>
  <read_first>
    - .planning/phases/phase-29/29-AUDIT.md §F2 (roomStateProfiles classification + risk notes)
    - .planning/phases/phase-29/29-RESEARCH.md §F2 (call site list, lines 398-428)
    - .planning/phases/phase-29/29-RESEARCH.md §"Pitfall 1" (Dropping roomStateProfiles while undo system still reads it)
    - server.mjs lines 39-58 (BOARD_PROFILE_FIELDS)
    - src/app/lib/state/runtime-state.js (find roomStateProfilesByBoard slice — line 73)
    - src/app/runtime/core/runtime-bootstrap.js (find createDefaultRoomStateProfilesByBoard call — line 122)
    - src/app/runtime/state/runtime-board-profiles.js (find synth/apply lines 38, 60, 188-193)
    - src/app/runtime/state/runtime-board-state-accessors.js (find get/set + helpers 192-201)
    - src/app/runtime/animation/runtime-room-management.js (find delete-on-room-delete lines 609-610)
    - src/app/lib/persistence/board-profiles.js (find legacy reads 54, 118-119)
    - src/app/lib/shared/config.js (find default room state profile shape lines 199-202)
    - src/app/runtime/runtime-orchestration.js (find ctx-builder re-exports 896-897, 1888-1889, 2028-2029)
    - src/app/runtime/state/runtime-board-switch.js (find hydration lines 155-167 for any roomStateProfile reference)
    - test/phase-29-dead-grep.test.mjs (the skip gate to remove)
  </read_first>
  <behavior>
    Atomic removal of the entire `roomStateProfiles` plumbing chain — no half-state
    where the slice exists but its accessors don't, or vice versa.

    - `BOARD_PROFILE_FIELDS` no longer contains `"roomStateProfiles"`.
    - `state.roomStateProfilesByBoard` slice removed from initial state.
    - `createDefaultRoomStateProfilesByBoard` removed from bootstrap call.
    - `getRoomStateProfile`, `setRoomStateProfile`, `normalizeRoomStateProfile`,
      `createDefaultRoomStateProfileMap`, `normalizeRoomStateProfileMap`,
      `createDefaultRoomStateProfilesByBoard` removed from accessor module.
    - The `delete state.roomStateProfilesByBoard[boardId][id]` line in
      `runtime-room-management.js:609-610` removed.
    - The 6 ctx-builder re-export lines in `runtime-orchestration.js` removed.
    - The default room-state-profile shape in `lib/shared/config.js:199-202` removed
      IF it has zero remaining consumers.
    - `runtime-board-profiles.js` synthesis (`roomStateProfiles: ...`) and apply
      (`state.roomStateProfilesByBoard = ...`) lines removed.
    - `lib/persistence/board-profiles.js` legacy candidate-shape reads removed.
    - The `runtime-board-switch.js` hydration lines that touch the slice removed (if any).
    - test/phase-29-dead-grep.test.mjs: skip gate removed for `roomStateProfiles`.
    - Existing tests stay green; suite reports `fail 0`.

    NON-REGRESSION GATES (Pitfall 1):
    - undo / redo flow in `runtime-polygon-undo.js` MUST NOT touch `roomStateProfilesByBoard`
      (already verified in 29-AUDIT.md §3 — undo uses tombstone IDs, not the room-state map).
    - `lastUsedProfileName` (Phase 28 B1) MUST remain in BOARD_PROFILE_FIELDS.
  </behavior>
  <files>
    server.mjs,
    src/app/lib/state/runtime-state.js,
    src/app/runtime/core/runtime-bootstrap.js,
    src/app/runtime/state/runtime-board-profiles.js,
    src/app/runtime/state/runtime-board-state-accessors.js,
    src/app/runtime/animation/runtime-room-management.js,
    src/app/lib/persistence/board-profiles.js,
    test/phase-29-dead-grep.test.mjs
  </files>
  <action>
    Execute in this exact order to keep the system consistent at each commit:

    **Step 2.1 — Drop the slice + its initializer:**
    - `src/app/lib/state/runtime-state.js`: delete line `roomStateProfilesByBoard: {},`
      (line 73 — verify exact line number with current file; the surrounding object literal
      keeps trailing comma hygiene of its remaining members).
    - `src/app/runtime/core/runtime-bootstrap.js`: delete line
      `state.roomStateProfilesByBoard = ctx.createDefaultRoomStateProfilesByBoard();`
      (line 122) AND remove `createDefaultRoomStateProfilesByBoard` from the destructured
      ctx param if it appears at the top of the function.

    **Step 2.2 — Drop the accessor module surface:**
    - `src/app/runtime/state/runtime-board-state-accessors.js`: delete the entire
      `getRoomStateProfile`, `setRoomStateProfile` IIFE block (around lines 192-201),
      AND the supporting helpers (`normalizeRoomStateProfile`, `createDefaultRoomStateProfileMap`,
      `normalizeRoomStateProfileMap`, `createDefaultRoomStateProfilesByBoard`).
      To find them deterministically:
      ```bash
      grep -n "RoomStateProfile" src/app/runtime/state/runtime-board-state-accessors.js
      ```
      Delete every function whose name contains `RoomStateProfile`. Also remove their
      entries from any `window.TT_BEAMER_*` exports object at the bottom of the file.

    **Step 2.3 — Drop the synth+apply path:**
    - `src/app/runtime/state/runtime-board-profiles.js`:
      - Line 38: delete `roomStateProfiles: ctx.createDefaultRoomStateProfileMap(board.id),`
      - Line 60: delete `roomStateProfiles: ctx.normalizeRoomStateProfileMap(state.roomStateProfilesByBoard[board.id], board.id),`
      - Lines 188-193: delete the entire
        `state.roomStateProfilesByBoard = Object.fromEntries(... ctx.normalizeRoomStateProfileMap(profiles?.[board.id]?.roomStateProfiles, board.id),)`
        block.
      - Verify with `grep -n "roomStateProfile\|RoomStateProfile" src/app/runtime/state/runtime-board-profiles.js`
        returns zero hits after the edits.

    **Step 2.4 — Drop the delete-on-room-delete:**
    - `src/app/runtime/animation/runtime-room-management.js` lines 609-610:
      delete the two-line block that calls
      `delete state.roomStateProfilesByBoard[boardId][id]` (and any surrounding
      one-liner that becomes orphaned). Verify
      `grep -n "roomStateProfile" src/app/runtime/animation/runtime-room-management.js`
      returns zero hits after.

    **Step 2.5 — Drop ctx-builder re-exports:**
    - `src/app/runtime/runtime-orchestration.js`: locate the 3 ctx-builder maps
      that re-export `getRoomStateProfile` / `setRoomStateProfile` / `createDefaultRoomStateProfileMap`
      etc. Use `grep -n "getRoomStateProfile\|setRoomStateProfile\|createDefaultRoomStateProfile\|normalizeRoomStateProfile" src/app/runtime/runtime-orchestration.js`.
      Delete each matching property (4-6 entries across 3 maps).

    **Step 2.6 — Drop legacy persistence reads:**
    - `src/app/lib/persistence/board-profiles.js`:
      lines 54 and 118-119 reference `roomStateProfiles`. Remove those property reads.
      Per RESEARCH §"Anti-Patterns" — DO NOT delete the file; it's still called by
      `applyGlobalDefaultsPayloadToState`. Just trim the dead field handling.

    **Step 2.7 — Drop the default shape constant (if orphaned):**
    - `src/app/lib/shared/config.js` lines 199-202 likely define
      `DEFAULT_ROOM_STATE_PROFILE` or similar. Run
      `grep -rn "DEFAULT_ROOM_STATE_PROFILE\|defaultRoomStateProfile" src/` AFTER the
      above edits land. If zero consumers remain, delete the constant.

    **Step 2.8 — Drop from BOARD_PROFILE_FIELDS:**
    - `server.mjs`: delete `  "roomStateProfiles",` (currently line 43).
      Suite-end: BOARD_PROFILE_FIELDS now has 13 entries (down from 15: -1 hiddenRoomNames Task 1, -1 roomStateProfiles Task 2).

    **Step 2.9 — Un-skip the dead-grep test:**
    - `test/phase-29-dead-grep.test.mjs`: locate
      `test("W2: roomStateProfiles has zero src/ hits..."` and remove its `{ skip: "..." }` opt.

    **Step 2.10 — Verify NOTHING else still references the field:**
    ```bash
    grep -rn "\broomStateProfiles?\b\|RoomStateProfile" src/ server.mjs | grep -v "test/"
    ```
    Expected: 0 hits. If any remain, trace and remove them in the same commit.

    **Step 2.11 — Run full suite:**
    ```bash
    node --test test/ 2>&1 | tail -10
    ```
    Expected: `pass` count ≥ baseline; `fail 0`.

    Pitfall 1 reminder: if `runtime-polygon-undo.js` shows up in a grep, STOP and
    re-read 29-AUDIT.md §3 — undo MUST NOT depend on `roomStateProfilesByBoard`.
    If it does, the audit was wrong; escalate before proceeding.
  </action>
  <verify>
    <automated>node --test test/ 2>&1 | tail -5 && echo "---" && grep -rn "\broomStateProfiles?\b\|RoomStateProfile" src/ server.mjs | grep -v "// " | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\broomStateProfiles?\b\|RoomStateProfile" src/ server.mjs` (excluding comments/test/) returns 0 hits
    - `grep -c '"roomStateProfiles"' server.mjs` returns 0
    - `grep -c '"lastUsedProfileName"' server.mjs` returns 1 (Phase 28 B1 preserved)
    - `node --test test/` exits 0 with `fail 0`
    - `node --test test/phase-29-dead-grep.test.mjs` reports the `roomStateProfiles` test passing (no longer skipped)
    - `node --test test/board-profile-fields.test.mjs` reports the existing 3 tests still passing
    - `git diff src/app/runtime/polygon-editor/runtime-polygon-undo.js` is empty (undo flow untouched, Pitfall 1 honored)
    - BOARD_PROFILE_FIELDS array length: count is exactly 13 (verify with
      `awk '/BOARD_PROFILE_FIELDS = Object.freeze/,/\]\);/' server.mjs | grep -c '^\s*"'`)
  </acceptance_criteria>
  <done>
    Both `hiddenRoomNames` and `roomStateProfiles` plumbing fully removed from source.
    BOARD_PROFILE_FIELDS = 13 entries. Suite GREEN. Two skip-gates removed in
    `phase-29-dead-grep.test.mjs`. The `phase-29 W2` skip-gate in
    `board-profile-fields.test.mjs` STAYS skipped (it un-skips only after 29-04 also
    drops `playAreaPolygon` and `deletedRoomIds`).
  </done>
</task>

</tasks>

<verification>
After both tasks:

```bash
node --test test/ 2>&1 | tail -10
grep -rn "\bhiddenRoomNames\b\|\broomStateProfiles?\b\|RoomStateProfile" src/ server.mjs | grep -v test/
git diff --stat src/app/runtime/polygon-editor/runtime-polygon-undo.js
```

Expected: suite green; first grep 0 hits; second grep empty (undo untouched).
</verification>

<success_criteria>
- `hiddenRoomNames` and `roomStateProfiles` removed from source tree
- BOARD_PROFILE_FIELDS shrunk from 15 → 13 entries
- `lastUsedProfileName` (Phase 28 B1) intact
- `runtime-polygon-undo.js` untouched (Pitfall 1 / Open Question A2 honored)
- 2 skip gates removed in `phase-29-dead-grep.test.mjs`
- Suite green: `node --test test/` `fail 0`
</success_criteria>

<output>
After completion, create `.planning/phases/phase-29/29-02-SUMMARY.md` with:
- Files modified + line counts removed
- Diff summary (`git diff --stat HEAD~ -- server.mjs src/`)
- Suite count before/after (`node --test test/ 2>&1 | tail -5`)
- Confirmation that `lastUsedProfileName` is intact
- Confirmation that `runtime-polygon-undo.js` is byte-unchanged
</output>