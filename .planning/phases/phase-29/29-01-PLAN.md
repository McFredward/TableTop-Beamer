---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: [W0]
files_modified:
  - .planning/phases/phase-29/29-AUDIT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "29-AUDIT.md exists in .planning/phases/phase-29/ as a Markdown table"
    - "Every persisted field listed in D-05 has a row with classification LIVE / DEAD / REDUNDANT"
    - "Each row cites at least one grep-evidence line:column reference proving its classification"
    - "Five DEAD/REDUNDANT verdicts (hiddenRoomNames, roomStateProfiles, animationSoundMap, playAreaPolygon, deletedRoomIds) are explicitly justified per D-01"
    - "The deletedRoomIds row contains a sub-trace of runtime-polygon-undo.js (the §Open Question A2 from RESEARCH.md) and a final disposition"
    - "User explicitly approves the audit verdict before Wave 2 plans start executing"
  artifacts:
    - path: ".planning/phases/phase-29/29-AUDIT.md"
      provides: "The authoritative classification document that authorizes Wave 2..4 deletions"
  key_links:
    - from: "29-AUDIT.md"
      to: "29-RESEARCH.md §Per-Field Consumer Trace"
      via: "verbatim re-confirmation with current grep output"
      pattern: "F1|F2|F3|F4|F5"
    - from: "29-AUDIT.md"
      to: "29-02/29-03/29-04 PLANs"
      via: "field-name → owning Wave 2 plan"
      pattern: "Wave 2 plan: 29-0[234]"
---

<objective>
Wave 1 — Produce the authoritative `29-AUDIT.md` classification document. This
table is the gate that authorizes the field deletions in Waves 2..4. Per D-02
the source-tree cleanup MUST NOT proceed until this audit is approved.

Per D-01: a field is DEAD if (a) no draw-loop / render-module reads it AND
(b) no UI element binds it. Read-paths in normalizers / default-builders /
get-accessors WITHOUT downstream consumption do NOT count as LIVE.

This plan has ONE auto task (write the audit document) and ONE checkpoint
(user approves the classification before Wave 2 begins).

Output: `.planning/phases/phase-29/29-AUDIT.md` containing a per-field table
with grep evidence and a final action column pointing to the Wave 2..4 plan
that will execute each cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/phase-29/29-CONTEXT.md
@.planning/phases/phase-29/29-RESEARCH.md
@.planning/phases/phase-29/29-VALIDATION.md

<interfaces>
<!-- The audit verdicts must align with the Wave 2..4 plan ownership: -->

| Field | Owning Wave 2/3/4 Plan | Expected Verdict |
|-------|------------------------|------------------|
| `hiddenRoomNames` | 29-02 | DEAD |
| `roomStateProfiles` | 29-03 | DEAD |
| `animationSoundMap` | 29-03 | REDUNDANT (lossless migration first) |
| `playAreaPolygon` | 29-04 | REDUNDANT |
| `deletedRoomIds` | 29-04 | REDUNDANT (pending undo trace) |
| `lastUsedProfileName` | (keep) | LIVE — Phase 28 B1 |
| `assetManifest` (config/asset-manifest.json) | (keep) | LIVE — Phase 28 B5 |
| `roomGeometry` | (keep state slice) | LIVE-runtime, possibly DEAD-on-disk (Open Question A3) |
| `outsideFx`, `insideFx`, `roomFx`, `defaultAnimations`, `playAreas`, `selectedPlayAreaId`, `frozenRooms`, `specialPolygons`, `hitareaCalibration` | (keep) | LIVE |

If the audit deviates from any of these expectations, the Wave 2..4 plans MUST
be re-derived (escalate to user before re-planning).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write 29-AUDIT.md with grep-evidenced field classification</name>
  <read_first>
    - .planning/phases/phase-29/29-RESEARCH.md §"Per-Field Consumer Trace (Wave 1 Audit Preview)" lines 384-530
    - .planning/phases/phase-29/29-RESEARCH.md §"Risk Matrix" lines 960-970
    - .planning/phases/phase-29/29-RESEARCH.md §"Open Questions" lines 994-1021
    - server.mjs lines 39-58 (BOARD_PROFILE_FIELDS)
    - src/app/runtime/polygon-editor/runtime-polygon-undo.js (full file — needed for deletedRoomIds undo trace)
    - config/global-defaults.json (read once, count animationSoundMap entries)
    - config/boards/nemesis-board-a.json (read once, confirm DEAD field presence on disk)
  </read_first>
  <files>.planning/phases/phase-29/29-AUDIT.md</files>
  <action>
    Create `.planning/phases/phase-29/29-AUDIT.md` with this exact structure:

    1. **Header** — `# Phase 29 Audit: Persistence Field Classification`, date 2026-05-05+,
       D-01 definition quoted verbatim.

    2. **§1 Verdict Summary table** — one row per field with columns:
       `Field | Schema | Verdict | Owning Plan | Evidence (greatest-weight grep)`

       Rows MUST cover:
       - From `BOARD_PROFILE_FIELDS` (server.mjs:39-58): all 15 entries.
       - From `config/global-defaults.json`: `animationSoundMap`, `audio`, `animationSpeed`,
         `mp4Performance`, `renderMode`, `diagnosticOverlay`.
       - From `config/asset-manifest.json` schema: top-level keys (`hashByPath`).
       - From `config/projection-profiles.json` schema: top-level keys (`profiles`,
         `lastSelected`, etc.).

    3. **§2 Per-DEAD-field detailed traces** — one subsection per field marked
       DEAD/REDUNDANT in §1, each with:
       - `### F<N>: <field-name>` heading
       - Read sites table (file:line — code snippet)
       - Write sites table
       - Render-loop / UI binding consumers (must be EMPTY for DEAD verdict)
       - Action: which Wave 2 plan owns the deletion + what gets removed
       - Risk note (if any)

       Re-confirm RESEARCH §"F1..F5" with `grep -rn` run NOW (verbatim shell command +
       output snippet for each). If the grep output diverges from the RESEARCH numbers,
       escalate before continuing — do NOT silently update.

    4. **§3 deletedRoomIds undo trace (Open Question A2)** — dedicated subsection.
       Trace `runtime-polygon-undo.js` reads:
       - Confirm `markRoomTombstone(state.boardId, id)` at line 77 — call site, not read.
       - Confirm `clearRoomTombstone(state.boardId, snap.id)` at line 66 — call site, not read.
       - Confirm zero reads of `state.roomTombstonesByBoard` array contents anywhere in
         polygon-undo. (`grep -n "roomTombstonesByBoard\|deletedRoomIds" src/app/runtime/polygon-editor/runtime-polygon-undo.js`)
       - Verdict: REDUNDANT — undo operates by ID, not by enumeration of the persisted list.
       - Recommended action: drop the field; KEEP `markRoomTombstone`/`clearRoomTombstone`
         as no-ops in 29-04, OR drop them entirely if no other call site remains.

    5. **§4 LIVE-but-redundant disk fields (Open Question A3)** — `roomGeometry`.
       - State slice `state.roomGeometryByBoard` IS read by `runtime-room-management.js`
         (`grep -n "getRoomGeometry\|setRoomGeometry" src/`).
       - Disk persistence may be redundant if the runtime can reconstruct from `roomCatalog`.
       - Verdict: LIVE in source, AUDIT INCONCLUSIVE for disk → DO NOT add to Wave 3 strip
         list. Defer to a future phase if pressing.

    6. **§5 Wave 2..4 owner mapping** — explicit mapping that mirrors what Wave 2..4
       plans expect:
       ```
       29-02 (Wave 2): drop hiddenRoomNames, drop roomStateProfiles plumbing
       29-03 (Wave 2): migrate + drop animationSoundMap, drop animation-editor audio-mapping panel code
       29-04 (Wave 2): drop playAreaPolygon, drop deletedRoomIds (per §3 verdict)
       29-05 (Wave 3): purgeDeadFieldsOnBoot — fields: hiddenRoomNames, roomStateProfiles, playAreaPolygon, deletedRoomIds, animationSoundMap (global)
       29-06 (Wave 4): BOARD_PACKAGE_SCHEMA v3 → v4
       ```

    7. **§6 Sign-off block** — `Approved: [pending checkpoint]` placeholder.
       The Task 2 checkpoint will fill this with the user's verbatim approval text.

    Style notes:
    - Use plain Markdown tables, no images.
    - Cite line numbers as `file.mjs:LINE` so they collation-sort.
    - Keep total document under 300 lines (information-dense, not narrative).
    - DO NOT delete or modify any production code in this task. Audit-only.
  </action>
  <verify>
    <automated>test -f .planning/phases/phase-29/29-AUDIT.md && grep -c "^|" .planning/phases/phase-29/29-AUDIT.md | awk '{ if ($1 < 20) { print "FAIL: too few table rows ("$1")"; exit 1 } else { print "OK: "$1" table rows" } }'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/phase-29/29-AUDIT.md`
    - Document contains §1, §2, §3, §4, §5, §6 headings (grep -c "^## " >= 6)
    - §1 table contains rows for: `hiddenRoomNames`, `roomStateProfiles`,
      `animationSoundMap`, `playAreaPolygon`, `deletedRoomIds`, `lastUsedProfileName`,
      `roomGeometry`, `playAreas`, `outsideFx`, `insideFx`, `roomFx`,
      `defaultAnimations`, `selectedPlayAreaId`, `frozenRooms`, `specialPolygons`,
      `hitareaCalibration`, `audio`, `animationSpeed`, `mp4Performance`,
      `renderMode`, `diagnosticOverlay`, `hashByPath` (asset-manifest)
    - §3 (`deletedRoomIds undo trace`) explicitly cites `runtime-polygon-undo.js:66`
      and `runtime-polygon-undo.js:77`
    - §3 verdict line is one of `REDUNDANT — drop` or `LIVE — keep` (no other text)
    - §5 wave-owner mapping references exactly the 5 plans 29-02..29-06
    - §6 sign-off block exists with placeholder text
    - No production source files (`src/`, `server.mjs`, `config/`, `test/`) modified
      (verify with `git diff --stat -- src/ server.mjs config/ test/` returning empty)
  </acceptance_criteria>
  <done>29-AUDIT.md committed; classification table is the authority for Wave 2..4 deletions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User approves audit verdicts before Wave 2 begins</name>
  <what-built>
    `.planning/phases/phase-29/29-AUDIT.md` — the per-field classification document
    that authorizes the source-tree deletions in 29-02, 29-03, 29-04.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/phase-29/29-AUDIT.md` in your editor or run
       `cat .planning/phases/phase-29/29-AUDIT.md`.
    2. Read §1 (Verdict Summary table). Confirm:
       - Five DEAD/REDUNDANT verdicts: `hiddenRoomNames`, `roomStateProfiles`,
         `animationSoundMap`, `playAreaPolygon`, `deletedRoomIds`.
       - All other fields marked LIVE.
       - `lastUsedProfileName` is LIVE (Phase 28 B1 — must NOT be deleted).
       - `hashByPath` (asset-manifest) is LIVE (Phase 28 B5).
    3. Read §3 (deletedRoomIds undo trace). Confirm the verdict is "REDUNDANT — drop"
       supported by the undo-flow trace (markRoomTombstone is a write, not a read).
    4. Read §5 (Wave 2..4 owner mapping). Confirm it lists the 5 plans 29-02..29-06.
    5. If you DISAGREE with any verdict (e.g. you want to KEEP a field marked
       DEAD, or DROP a field marked LIVE), write the dissent and re-run
       `/gsd-discuss-phase 29` to revise CONTEXT.md before approving.
  </how-to-verify>
  <acceptance_criteria>
    - User responds with one of:
      - `approved` — Wave 2 plans (29-02..29-04) are unblocked.
      - `revise: <field>` — orchestrator reverts to discuss/research mode for that field.
    - On approval, the executor appends `Approved: <user-quote> at <ISO timestamp>`
      to §6 of `29-AUDIT.md` and commits.
  </acceptance_criteria>
  <resume-signal>Type `approved` to unblock Wave 2, or describe required revisions to escalate.</resume-signal>
</task>

</tasks>

<verification>
After Task 1: `grep -c "^|" .planning/phases/phase-29/29-AUDIT.md` ≥ 20.
After Task 2: §6 sign-off block contains the user's verbatim approval text.
After both: `git diff --stat -- src/ server.mjs config/ test/` is empty (audit-only plan).
</verification>

<success_criteria>
- Authoritative `29-AUDIT.md` classification document exists and is committed.
- Five DEAD/REDUNDANT verdicts are explicitly grep-evidenced.
- User has explicitly approved the verdict (Task 2 checkpoint resolved).
- Zero production code files modified by this plan.
- The Wave 2..4 plans (29-02..29-06) can now reference the AUDIT.md as their
  authority for what to delete.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-29/29-01-SUMMARY.md` with:
- Path to `29-AUDIT.md`
- Verbatim §1 table (Verdict Summary)
- §3 verdict (deletedRoomIds: REDUNDANT — drop, OR LIVE — keep)
- User approval text + timestamp
- Commit hash
</output>