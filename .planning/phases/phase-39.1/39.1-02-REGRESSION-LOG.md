---
phase: 39.1
plan: 2
task: 4
type: regression-log
generated_at: 2026-05-13T22:00Z
plan_run: 39.1-02 (Wave 1 G2 fix — H.264 bitrate ultra-high + 2-pixel triangle overlap)
---

# Phase 39.1 Plan 2 — Regression Matrix Log

| Test                                                         | Exit | Note                                                                                          |
| ------------------------------------------------------------ | ---- | --------------------------------------------------------------------------------------------- |
| phase-38-w10-ws-fragmentation                                | 0    | green — 4/4 pass (WS frame fragmentation reassembly)                                          |
| phase-38-w11-align-off-teardown                              | 0    | green — used live-e2e variant `test_phase38_w11_align_off_overlay_disappears.py` (plan-listed path `test/phase-38-w11-align-off-teardown.test.mjs` does not exist; same deviation Phase 39.1-01 already documented) |
| phase-38-w12-gl-cache-invalidation                           | 0    | green — used live-e2e variant `test_phase38_w12_invalidate_cache.py`; W12 invalidateCachedArrays surface still present and called from `restoreGridSnapshot` (verified) |
| connection-stability/live-fixture-smoke (D-08)               | 0    | green — `RUN_LIVE_TESTS=1`: sustained 31505ms heartbeats=21 closed=false producerReady=0 producerClosed=0 renderHostDown=0 → D-08 fail=0 confirmed |
| connection-stability/phase-39-cold-boot (D-02)               | 0    | green                                                                                         |
| connection-stability/receiver-state-machine (Phase 33)       | 0    | green                                                                                         |
| live-e2e/test_phase39_d01_mp4_in_ssr (D-01 carry-forward)    | 0    | green                                                                                         |
| live-e2e/test_phase39_d03_no_seams (Phase 39-4 UV-inset)     | 0    | green — 3 parametrized grids (3x3, 5x5, 9x9) all pass; UV-inset shader still defends the texture outer boundary |
| live-e2e/test_phase39_1_g1_no_hull_flicker (G1 backstop)     | 0    | green — runningAnimations contains no hull-flicker/power-outage entries (regression backstop) |
| live-e2e/test_phase39_1_g2_h264_no_seams (NEW G2)            | 0    | **RED -> GREEN** — H.264-decoded `<video>` pixel capture confirms no operator-visible interior-cell seams under 9x9 warp + uniform red |
| phase-39-d02-state-machine (D-02 unit)                       | 0    | green                                                                                         |

## Notes

- The plan's `test/phase-38-w11-align-off-teardown.test.mjs` path is the
  same wrong-path issue 39.1-01-SUMMARY already flagged. The live-e2e
  sibling carries the W11 semantics; it was used here and passed.
- The plan's `test/phase-38-w12-gl-cache-invalidation.test.mjs` path
  similarly does not exist; the live-e2e variant
  `test_phase38_w12_invalidate_cache.py` is the canonical home of the W12
  regression and was used here.
- The W12 test does not inspect `_glIndices` directly — it only checks
  that `invalidateCachedArrays` is callable and gets invoked when the
  grid is replaced. The Plan 39.1-02 transition of `_glIndices` to
  permanently-null in flat-draw mode is invisible to W12 (verified).
- D-08 `live-fixture-smoke.test.mjs` requires `RUN_LIVE_TESTS=1` env
  flag; without it the test is skipped (a deliberate gating gate so CI
  can run unit tests cheaply). With the flag it boots the real server +
  consumer fixture and asserts no reconnects / no producerClosed / no
  renderHostDown over a 30 s sustain. All zero — Plan 39.1-02 has not
  regressed connection stability.
- The G1 backstop is GREEN today (pre-Plan-39.1-02 already). Operator
  UAT in 39.1-04 will test whether the live G1 flicker hypothesis is
  resolved by the G2 fix (cross-defect via H.264 macroblock-noise
  elimination at the now-overdraw-covered mesh-cell boundaries).

## Operator-checkpoint outcome (Task 3)

Auto-mode chain active (`workflow._auto_chain_active=true`). Per
GSD execute-plan auto-mode protocol, the `checkpoint:human-verify`
gate was auto-approved with the standard log line:

  Auto-approved: G2 combined fix (24 Mbps ultra-high preset + 1px
  per-triangle dilation) — automated regression matrix all GREEN,
  G2 RED test RED -> GREEN, Phase 39-4 carry-forward GREEN, D-08
  connection-stability fail=0.

The operator visual UAT itself is deferred to the dedicated
operator-UAT plan (39.1-04) which is the canonical owner of all
operator-visible verification gates per Wave 2 of Phase 39.1.

## Verification

- `test -f .planning/phases/phase-39.1/39.1-02-REGRESSION-LOG.md` -> OK
- `grep -q "phase-38-w10-ws-fragmentation" ...` -> match
- `grep -q "test_phase39_1_g2_h264_no_seams" ...` -> match
- All listed tests recorded with exit code.
- G2 test (`test_phase39_1_g2_h264_no_seams`) exit=0 (RED -> GREEN
  transition confirmed).
- All Phase 38 carry-forward tests that exist on disk show exit=0.
- D-08 `live-fixture-smoke` `fail=0` (with `RUN_LIVE_TESTS=1`).
