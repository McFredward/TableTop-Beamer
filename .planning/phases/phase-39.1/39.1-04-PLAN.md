---
phase: 39.1
plan: 4
type: execute
wave: 2
depends_on: ["39.1-02", "39.1-03"]
files_modified:
  - .planning/phases/phase-39.1/39.1-CLOSURE.md
  - .planning/phases/phase-39.1/39.1-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements:
  - G1-NO-FLICKER
  - G2-NO-STREAM-SEAMS
  - G3-BOARD-SWITCH-CLEAN

must_haves:
  truths:
    - "Full regression matrix run: Phase 38 W10/W11/W12 + Phase 39 D-01/D-02/D-03 + Phase 39.1 G1/G2/G3 + D-08 connection-stability fail=0"
    - "G2 RED test (test_phase39_1_g2_h264_no_seams.py) is GREEN"
    - "G3 RED test (test_phase39_1_g3_board_switch_clean.py) is GREEN"
    - "G1 backstop (test_phase39_1_g1_no_hull_flicker.py) is GREEN"
    - "Operator UAT confirms G2 + G3 visually clean"
    - "Operator UAT confirms G1 either clean (G2-side-effect resolved) OR triggers fallback investigation per §G1 Step 2b"
    - "git tag phase-39.1-closed applied"
    - "STATE.md updated to reflect phase 39.1 closure"
    - "ROADMAP.md Phase 39.1 status flipped from PLANNING to CLOSED (with operator-UAT outcome)"
  artifacts:
    - path: ".planning/phases/phase-39.1/39.1-VERIFICATION.md"
      provides: "Full regression matrix + operator UAT result"
      contains: "G1-NO-FLICKER"
    - path: ".planning/phases/phase-39.1/39.1-CLOSURE.md"
      provides: "Closure document with evidence + carry-forward list for next phase"
      contains: "carry-forward"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 39.1 status updated to CLOSED with date"
      contains: "CLOSED"
    - path: ".planning/STATE.md"
      provides: "Lifecycle pointers refresh — Current Phase incremented, Previous Phase = 39.1"
      contains: "39.1"
  key_links:
    - from: "Phase 39.1 closure decision"
      to: "git tag phase-39.1-closed"
      via: "git tag -a after VERIFICATION green"
      pattern: "phase-39.1-closed"
    - from: "ROADMAP.md Phase 39.1 entry"
      to: "STATE.md Lifecycle"
      via: "synchronized status flip on close"
      pattern: "CLOSED"
---

<objective>
Wave 2: verify Phase 39.1 fully and close it.

Run the full Phase-38 + Phase-39 + Phase-39.1 regression matrix. Confirm every carry-forward stays GREEN. Confirm every new G1/G2/G3 acceptance criterion is satisfied. Hold the operator UAT checkpoint that covers visual confirmation of G1 + G2 + G3 together (the two prior plan checkpoints already partially confirmed; this one is the consolidated phase-exit gate).

Handle the cross-defect contingency: if operator confirms G2 clean but G1 still flickers, route to fallback investigation per §G1 Step 2b (probe runningAnimations, inspect findActiveBreakingGate).

On success: write 39.1-VERIFICATION.md + 39.1-CLOSURE.md, update ROADMAP.md and STATE.md, tag `phase-39.1-closed`.

Output: 4 docs created/updated + git tag + lifecycle pointer refresh.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/phase-39/39-VERIFICATION.md
@.planning/phases/phase-39/39-CLOSURE.md
@.planning/phases/phase-39.1/39.1-RESEARCH.md
@.planning/phases/phase-39.1/39.1-01-SUMMARY.md
@.planning/phases/phase-39.1/39.1-02-SUMMARY.md
@.planning/phases/phase-39.1/39.1-02-REGRESSION-LOG.md
@.planning/phases/phase-39.1/39.1-03-SUMMARY.md
@.planning/phases/phase-39.1/39.1-G3-DIAG.md
@.planning/CRITICAL_KNOWN_BUGS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run full regression matrix (Phase 38 + Phase 39 + Phase 39.1) and record evidence</name>
  <read_first>
    - .planning/phases/phase-39.1/39.1-RESEARCH.md §Validation Architecture / Phase Requirements → Test Map (lines 680-700)
    - .planning/phases/phase-39.1/39.1-02-REGRESSION-LOG.md (existing log from 39.1-02 Task 4 — extend, do not replace)
    - .planning/phases/phase-39/39-CLOSURE.md (Phase 39 closure used this same matrix as its gate — reuse the test list)
  </read_first>
  <files>.planning/phases/phase-39.1/39.1-VERIFICATION.md</files>
  <behavior>
    - Run every test in the matrix below. Record exit code, time taken, and summary.
    - The matrix MUST include every Phase 38 W1-W13 carry-forward test that exists, every Phase 39 D-01/D-02/D-03 carry-forward test, and every new Phase 39.1 G1/G2/G3 test.
    - Any unexpected FAIL is a blocker — write the result to 39.1-VERIFICATION.md and STOP for user direction.
    - The output document is the Phase 39.1 verification gate.
  </behavior>
  <action>
    1. Read 39.1-02-REGRESSION-LOG.md (from 39.1-02 Task 4) — it has partial matrix coverage. This task EXTENDS it to the full matrix and migrates the data into the canonical 39.1-VERIFICATION.md.
    2. Run each test, capturing exit code + duration:
       **Phase 38 carry-forwards (must all be GREEN):**
       - `node --test test/phase-38-w10-ws-frame-fragmentation.test.mjs`
       - `node --test test/phase-38-w11-align-off-teardown.test.mjs` (or closest filename match — verify via `ls test/phase-38-w11*`)
       - `node --test test/phase-38-w12-gl-cache-invalidation.test.mjs`
       - `node --test test/phase-38-w13-10-90-inset-default.test.mjs` (if exists)
       - `pytest test/live-e2e/test_phase38_pi_ssr_sync_enforcement.py -x`
       - `pytest test/live-e2e/test_phase38_w10_drag_burst_then_profile_load.py -x`
       - `pytest test/live-e2e/test_phase38_w11_align_off_overlay_disappears.py -x`
       - `pytest test/live-e2e/test_phase38_w12_invalidate_cache.py -x`

       **Phase 39 carry-forwards (must all be GREEN — D-01/D-02 carry forward LOCKED):**
       - `node --test test/phase-39-d01-mime-and-range.test.mjs` (D-01 unit)
       - `pytest test/live-e2e/test_phase39_d01_mp4_in_ssr.py -x` (D-01 live)
       - `node --test test/phase-39-d02-state-machine.test.mjs` (D-02 unit)
       - `node --test test/connection-stability/phase-39-cold-boot.test.mjs` (D-02 integration)
       - `node --test test/connection-stability/receiver-state-machine.test.mjs` (Phase 33 carry-forward, adapted)
       - `node --test test/phase-39-d03-render-mode-probe.test.mjs` (Phase 39-4 renderMode probe)
       - `pytest test/live-e2e/test_phase39_d03_no_seams.py -x` (Phase 39-4 UV-inset JPEG test — MUST stay GREEN to prove UV-inset still defends texture boundary)

       **D-08 connection-stability (must stay fail=0):**
       - `node --test test/connection-stability/live-fixture-smoke.test.mjs`

       **Phase 39.1 NEW tests:**
       - `pytest test/live-e2e/test_phase39_1_g1_no_hull_flicker.py -x` (G1 backstop — expected GREEN)
       - `pytest test/live-e2e/test_phase39_1_g2_h264_no_seams.py -x` (G2 — RED→GREEN after 39.1-02)
       - `pytest test/live-e2e/test_phase39_1_g3_board_switch_clean.py -x` (G3 — RED→GREEN after 39.1-03)

       For each test, run twice if the first run is flaky (intermittent video-frame timing). Record both runs.
    3. Write `.planning/phases/phase-39.1/39.1-VERIFICATION.md` with:
       ```markdown
       ---
       status: regression_green | regression_gaps_found
       phase: 39.1-ssr-stabilization-round-2-gap-closure
       created: 2026-05-13
       verified_by: automated_matrix + operator_uat
       phase_tag_pending: phase-39.1-closed
       ---
       
       # Phase 39.1 Verification — Regression Matrix + Operator UAT 2026-05-13
       
       ## Automated Regression Matrix
       
       | Test | Type | Exit | Duration | Note |
       |------|------|------|----------|------|
       | ... fill from runs above ... |
       
       Total: N tests run, M GREEN, K SKIP (file not present), L FAIL.
       
       ## Phase 39.1 Acceptance Gates
       
       ### G1-NO-FLICKER
       - Backstop test: GREEN
       - Primary (operator UAT): see Task 2 outcome below.
       
       ### G2-NO-STREAM-SEAMS
       - H.264 RED test: RED → GREEN (commit hash before/after).
       - Phase 39-4 JPEG carry-forward test: still GREEN (UV-inset shader fix preserved).
       - Operator UAT (Task 2): see outcome below.
       
       ### G3-BOARD-SWITCH-CLEAN
       - RED test: RED → GREEN (commit hash before/after).
       - G3-PROBE diagnostic confirmed Hypothesis [A/B/C] (see 39.1-G3-DIAG.md).
       - Operator UAT (Task 2): see outcome below.
       
       ## Carry-Forwards Verified
       - All Phase 38 W10/W11/W12 + W13 (if test exists)
       - All Phase 39 D-01 + D-02 + D-03 carry-forwards
       - D-08 connection-stability fail=0
       - VAAPI default-disabled (Phase 33 commit 3cd6748)
       - L13 VAAPI-class flag lock (no --ignore-gpu-blocklist / --enable-gpu-rasterization in ssr-render-host.mjs)
       
       ## Decision
       [filled in by Task 3]
       ```
    4. If ANY test in the matrix FAILS unexpectedly (not file-not-present), STOP and surface the failure to the user for direction.
  </action>
  <verify>
    <automated>test -f .planning/phases/phase-39.1/39.1-VERIFICATION.md && grep -q "Regression Matrix" .planning/phases/phase-39.1/39.1-VERIFICATION.md && grep -q "test_phase39_1_g2_h264_no_seams" .planning/phases/phase-39.1/39.1-VERIFICATION.md && grep -q "test_phase39_1_g3_board_switch_clean" .planning/phases/phase-39.1/39.1-VERIFICATION.md && grep -q "live-fixture-smoke" .planning/phases/phase-39.1/39.1-VERIFICATION.md && echo "OK verification matrix recorded"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/phase-39.1/39.1-VERIFICATION.md` exists.
    - Document contains the full regression matrix table with exit codes for every test in the action list.
    - Every Phase 38 W10/W11/W12 + Phase 39 D-01/D-02/D-03 carry-forward test that EXISTS is listed with exit=0.
    - All three Phase 39.1 NEW tests (g1, g2, g3) are listed.
    - D-08 connection-stability (live-fixture-smoke) shows exit=0 OR an equivalent "no closed=true, no producerReady>0, no renderHostDown>0" indicator.
    - No UNEXPECTED failures (file-not-present is allowed; substantive test failure is NOT).
  </acceptance_criteria>
  <done>Full regression matrix recorded in 39.1-VERIFICATION.md, all carry-forwards GREEN, all G1/G2/G3 tests GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator visual UAT — consolidated G1 + G2 + G3 visual confirmation</name>
  <read_first>
    - .planning/phases/phase-39/39-VERIFICATION.md (operator's exact quotes — all three gaps)
    - .planning/phases/phase-39.1/39.1-VERIFICATION.md (just written in Task 1)
    - .planning/phases/phase-39.1/39.1-02-SUMMARY.md (G2 fix details — DILATE_PX value at commit)
    - .planning/phases/phase-39.1/39.1-03-SUMMARY.md (G3 fix details — Hypothesis confirmed)
    - .planning/phases/phase-39.1/39.1-RESEARCH.md §G1 (lines 68-122 — fallback investigation steps if G1 not resolved by G2)
  </read_first>
  <files>(human verification checkpoint — no files modified)</files>
  <action>
    Operator runs the verification steps in &lt;how-to-verify&gt; below. Execution agent: do NOT proceed past this checkpoint until the operator returns a resume-signal value. The checkpoint's automated portion is exactly the &lt;verify&gt; block (the regression-matrix re-run that 39.1-04 Task 1 already covers — checkpoint just gates on operator confirmation).
  </action>
  <what-built>
    Phase 39.1 closed all three operator-reported gaps from Phase 39 UAT:
    - **G1** flickering: targeted via cross-defect mechanism (H.264 temporal noise eliminated by G2's seam fix). Backstop test green.
    - **G2** mesh-warp seams in H.264 stream: ultra-high preset (24 Mbps single-layer) + per-triangle 1-px dilation in flat-arrays drawArrays. Phase 39-4 UV-inset preserved.
    - **G3** board-switch partial align mode + spurious dirty: sync teardown + activation-token + captureCurrentAsLoadedSnapshot baseline.
    
    All Phase 38 + Phase 39 + D-08 carry-forwards GREEN (per Task 1 matrix).
  </what-built>
  <how-to-verify>
    1. Restart server: `pkill -f "node server.mjs" ; sleep 2 ; npm start &`. Wait 30s.
    
    2. **G1 verification (cross-defect via G2):**
       - Trigger autostart sequence (boot-time animation pipeline).
       - Watch all 13 rooms of Nemesis Lockdown A for 60s.
       - Expected: uniform red light, NO flickering perception.
       - If clean: G1 PASS via G2-side-effect.
       - If flickering persists: G1 PARTIAL — trigger G1 Step 2b fallback (see below).
    
    3. **G2 verification (primary):**
       - Load Nemesis Lockdown A.
       - Apply the 9×9 warped projection profile.
       - Trigger solid-color (red) across all rooms.
       - Watch projected output AND Pi HDMI mirror.
       - Expected: uniform red, no stripes along mesh-cell boundaries.
    
    4. **G3 verification (primary):**
       - Enable align mode.
       - Switch boards via dashboard board-picker 5+ times across 2-3 boards.
       - After each switch:
         - Handles + overlay polygons render together (no handles-only state).
         - Dashboard's "unsaved on /output/" indicator stays clean.
       - At end of test, drag a handle — confirm dirty=true correctly appears (negative control).
    
    5. **D-01 + D-02 sanity (Phase 39 carry-forwards):**
       - D-01: Trigger sandstorm.mp4. Confirm plays.
       - D-02: Cold-boot the server, watch /output/ for first 30s. Confirm "Connecting to render server…" splash (NOT "RECONNECTING").
    
    6. **G1 fallback path (only if step 2 shows persistent flickering):**
       The 39.1-01 G1 backstop test should already be GREEN; this confirms no stray hull-flicker entries are configured. If operator STILL sees flicker:
       a. Capture `/api/diag/ssr-eval-in-tab?expr=window.__TT_BEAMER_STATE_FOR_DIAG__?.runningAnimations` during boot — append to `.planning/phases/phase-39.1/39.1-G1-DIAG.md`.
       b. If runningAnimations contains any entry with type/assetRef matching `hull-flicker` or `power-outage` → an autostart pipeline is overriding solid-color. Track to source (likely server.mjs autostart snapshot replay path). This requires a SEPARATE follow-up plan/phase (39.2) — do NOT block 39.1 closure.
       c. If runningAnimations is clean and operator still perceives flicker → may indicate Pi-side H.264 decoder behavior at 24 Mbps that needs deeper investigation (e.g., bumping DILATE_PX to 2.0 didn't help in 39.1-02 Task 3 fallback). Defer to operator decision: close 39.1 with G1 PARTIAL and open Phase 40 for residual investigation, OR keep 39.1 open until G1 resolves.
  </how-to-verify>
  <acceptance_criteria>
    - Operator returns one of three definitive responses:
      1. **"All three clean"** → proceed to Task 3 (close 39.1).
      2. **"G2/G3 clean, G1 still flickers"** → operator chooses: close 39.1 with G1 PARTIAL (G1 deferred to Phase 40) OR keep 39.1 open until G1 fallback investigation completes.
      3. **"G2 or G3 still broken"** → 39.1 stays open; route to corresponding sub-plan for additional iteration. Tasks 3/4 do NOT run; user must direct next step.
    - 39.1-VERIFICATION.md is updated with the operator's outcome verbatim quote.
  </acceptance_criteria>
  <verify>
    <automated>echo "operator-checkpoint — automated verification gates handled by Task 1 regression matrix; checkpoint relies on operator visual UAT"</automated>
  </verify>

  <done>Operator responded with a definitive resume-signal value; outcome recorded in 39.1-VERIFICATION.md (or the relevant sub-plan SUMMARY).</done>
  <resume-signal>Operator types one of: "all-clean" / "g1-partial-close" / "g1-partial-defer" / "g2-still-broken" / "g3-still-broken" — plus descriptive notes.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 39.1-CLOSURE.md + update ROADMAP.md + update STATE.md</name>
  <read_first>
    - .planning/phases/phase-39.1/39.1-VERIFICATION.md (from Task 1 + Task 2)
    - .planning/phases/phase-39/39-CLOSURE.md (template — Phase 39 closure structure)
    - .planning/ROADMAP.md (Phase 39.1 entry at line 869)
    - .planning/STATE.md (lifecycle section at lines 24-33)
    - .planning/CRITICAL_KNOWN_BUGS.md (review whether to add a new entry #6 for G2 / G3 patterns)
  </read_first>
  <files>.planning/phases/phase-39.1/39.1-CLOSURE.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <behavior>
    - 39.1-CLOSURE.md follows the same structure as 39-CLOSURE.md: summary, gap-by-gap result, evidence pointers, carry-forwards, next-phase recommendations.
    - ROADMAP.md Phase 39.1 entry header is updated: `(PLANNING, 2026-05-13)` → `(CLOSED, 2026-05-13)` or `(CLOSED-WITH-PARTIAL-G1, 2026-05-13)` depending on Task 2 outcome.
    - STATE.md Lifecycle section is updated: Current Phase = 40 (next planned), Previous Phase = 39.1 (CLOSED), Last Executed Plan reflects 39.1's G2 + G3 fixes, Last Execution Summary describes the gap closure.
    - CRITICAL_KNOWN_BUGS.md gets a new entry #6 for the "wrong-layer verification test" anti-pattern (RESEARCH §G2 + State of the Art table). This is the operator-flagged anti-pattern: JPEG-CDP test verifying an H.264-pipeline behavior. Future GL/encoder/compositor work MUST exercise the actual H.264-decoded video pixels, not page screenshots.
  </behavior>
  <action>
    1. Write `.planning/phases/phase-39.1/39.1-CLOSURE.md` with sections:
       - **Summary** (1 paragraph: three gaps closed via combined G2 fix + G3 fix; G1 status = clean OR partial-deferred)
       - **Gap-by-gap result**: for each of G1/G2/G3, list (a) the operator's original quote, (b) the fix applied with file paths + commit hashes if known, (c) verification evidence (test names + result), (d) operator UAT outcome
       - **Carry-forwards verified intact**: Phase 38 W10/W11/W12/W13, Phase 39 D-01/D-02/D-03 (UV-inset), D-08, VAAPI default-disabled, L13 VAAPI-class flag lock
       - **Carry-forwards LOCKED for future phases**: ultra-high preset + bitrate plumbing semantics, per-triangle dilation pattern, sync-teardown + activation-token pattern, captureCurrentAsLoadedSnapshot pattern
       - **Test rails (mandatory keep-green)**: test_phase39_1_g2_h264_no_seams.py, test_phase39_1_g3_board_switch_clean.py, test_phase39_1_g1_no_hull_flicker.py, plus all existing carry-forwards
       - **Anti-pattern reinforced**: JPEG-CDP screenshot as a verification gate for H.264-stream behavior. ADD to CRITICAL_KNOWN_BUGS.md as entry #6.
       - **Open follow-ups (if any)**: if G1 deferred, note that Phase 40 must address; if DILATE_PX needed bumping above 1.0, note the empirical value
       - **Tag**: `phase-39.1-closed` (or `phase-39.1-closed-with-partial-g1`)
    2. Update `.planning/ROADMAP.md`:
       - Find the Phase 39.1 entry header at line 869: `## Phase 39.1 - SSR Stabilization Round 2 Gap Closure: real seams, flickering, board-switch race (PLANNING, 2026-05-13)`.
       - Change `(PLANNING, 2026-05-13)` → `(CLOSED, 2026-05-13)` or `(CLOSED-WITH-PARTIAL-G1, 2026-05-13)` based on Task 2 outcome.
       - Add a **Closure** section at the end of the Phase 39.1 block (after the existing "Requirements" line):
         ```markdown
         **Plans:** 4 plans (1 Wave-0 + 2 Wave-1 + 1 Wave-2)
         
         Plans:
         - [x] 39.1-01-PLAN.md — Wave 0: G3-PROBE diagnostic + 3 RED tests (G1/G2/G3) + helpers.py
         - [x] 39.1-02-PLAN.md — Wave 1: G2 fix (ultra-high preset + bitrate plumbing + per-triangle 1-px dilation)
         - [x] 39.1-03-PLAN.md — Wave 1: G3 fix (sync teardown + activation token + captureCurrentAsLoadedSnapshot)
         - [x] 39.1-04-PLAN.md — Wave 2: regression matrix + operator UAT + closure docs + tag
         
         **Closure:** see .planning/phases/phase-39.1/39.1-CLOSURE.md for evidence. Operator UAT 2026-05-13 outcome: [fill in].
         ```
    3. Update `.planning/STATE.md`:
       - Lifecycle section: set `Current Phase: 40 (next planned)`, `Current Phase Key: phase-40`, `Last Prepared: 2026-05-13`, `Previous Phase: 39.1 (CLOSED — or CLOSED-WITH-PARTIAL-G1)`, `Last Executed Plan: 39.1 (G2 + G3 gap closure)`, `Last Execution Summary: <one-paragraph summary>`.
       - Update progress counts: `total_phases` += 1 (39.1 counts as a phase), `completed_phases` += 1.
       - Add a Decision Log entry at the top of the existing log: `Phase 39.1 Schließung: G2 + G3 closed mit operator UAT confirmation; G1 [clean via G2 cross-defect | partial-deferred zu Phase 40]. Ultra-high 24 Mbps preset + per-triangle 1px dilation + sync-teardown + captureCurrentAsLoadedSnapshot LOCKED als carry-forward.`
    4. Update `.planning/CRITICAL_KNOWN_BUGS.md` by appending entry #6:
       ```markdown
       ---
       
       ## #6 — JPEG-CDP screenshot is the wrong test layer for H.264-stream artifacts (Chromium 131+)
       
       ### Discovery
       
       Phase 39 D-03 (2026-05-12, commit 1a8cef2) added a UV-inset shader fix.
       Verification test `test/live-e2e/test_phase39_d03_no_seams.py` measured CDP
       `Page.captureScreenshot` JPEG pixels and reported `max_delta=0`. Operator
       UAT 2026-05-13 found seams STILL visible in the live H.264 WebRTC stream.
       
       ### What the bug was
       
       Phase 39's test captured `Page.captureScreenshot` (JPEG quality=60), which
       uses an 8×8 DCT block size and a quantization profile that smooths
       single-pixel ridges into invisibility. The operator's actual pipeline
       (`getDisplayMedia` → H.264 baseline → WebRTC → Pi decode) uses 4×4 transform
       blocks aligned to mesh-cell edges and quantizes the same single-pixel
       residual into VISIBLE structured noise. Same source image, different
       compression artifact pattern, different visibility threshold.
       
       Net effect: the test was green for a fix that didn't address the operator's
       actual problem.
       
       ### The fix (Phase 39.1)
       
       1. New test `test/live-e2e/test_phase39_1_g2_h264_no_seams.py` captures pixels
          from the HTML5 `<video>` element (H.264-decoded operator-visible frame) via
          offscreen canvas + getImageData, NOT from a CDP screenshot.
       2. G2 fix combined: ultra-high preset (24 Mbps single-layer) + per-triangle
          1-px outward vertex dilation (eliminating diamond-exit ambiguity at shared
          cell edges). Phase 39-4 UV-inset shader preserved as orthogonal
          texture-boundary defense.
       
       ### MANDATORY test rail: test/live-e2e/test_phase39_1_g2_h264_no_seams.py
       
       ANY future change to:
       - `src/server/ssr-stream-publisher.mjs` (bitrate / encoder config)
       - `src/server/ssr-render-host.mjs` (QUALITY_PRESETS)
       - `src/app/runtime/viewport/runtime-projection-gl-renderer.js` (mesh-warp)
       
       MUST keep `test_phase39_1_g2_h264_no_seams.py` green. The JPEG-CDP test
       (`test_phase39_d03_no_seams.py`) remains as a unit test for the UV-inset
       shader fix, but is NOT a substitute for the H.264-decoded video test.
       
       ### Future test discipline
       
       For ANY change that touches the SSR rendering or encoding pipeline:
       - Capture pixels from the `<video>` element via canvas.drawImage + getImageData.
       - DO NOT use `page.screenshot()` or CDP `Page.captureScreenshot` to verify a
         behavior the operator perceives over WebRTC/H.264.
       - JPEG screenshots are valid for verifying the IN-PAGE canvas (pre-encode);
         they are NOT valid for verifying the consumer-side decoded stream.
       
       ### Symptoms to recognize this bug class in future operator UATs
       
       If operator reports:
       - Automated tests are green but operator still sees the bug
       - Bug only visible on the actual deployment hardware, not on dev box
       - Bug specifically involves the streaming pipeline (artifacts, quality, latency)
       
       → Suspect wrong-layer testing. Audit the test's capture path. If it touches
       Chromium-internal APIs (CDP screenshot, eval-in-tab readback of canvas),
       it's the wrong layer for stream-side artifacts.
       ```
    5. Verify all four files parse / lint cleanly: STATE.md and ROADMAP.md must remain valid markdown; CLOSURE.md must contain all required sections; CRITICAL_KNOWN_BUGS.md must still parse markdown.
  </action>
  <verify>
    <automated>test -f .planning/phases/phase-39.1/39.1-CLOSURE.md && grep -q "Phase 39.1" .planning/phases/phase-39.1/39.1-CLOSURE.md && grep -q "carry-forward" .planning/phases/phase-39.1/39.1-CLOSURE.md && grep -q "CLOSED" .planning/ROADMAP.md && grep -qE "Previous Phase: 39\\.1|Last Executed Plan: 39\\.1" .planning/STATE.md && grep -q "JPEG-CDP screenshot" .planning/CRITICAL_KNOWN_BUGS.md && echo "OK closure docs + roadmap + state updated"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/phase-39.1/39.1-CLOSURE.md` exists with all required sections (Summary, gap-by-gap, carry-forwards, anti-pattern, tag).
    - `.planning/ROADMAP.md` Phase 39.1 entry header reads `(CLOSED, ...)` or `(CLOSED-WITH-PARTIAL-G1, ...)`.
    - `.planning/ROADMAP.md` Phase 39.1 entry has a `**Plans:**` section listing all 4 plans.
    - `.planning/STATE.md` lifecycle section shows `Previous Phase: 39.1` and `Last Executed Plan: 39.1` (or equivalent).
    - `.planning/CRITICAL_KNOWN_BUGS.md` contains entry #6 with the JPEG-CDP anti-pattern.
    - All four files still parse as valid markdown (no broken YAML frontmatter, no broken table syntax).
  </acceptance_criteria>
  <done>Closure document written, ROADMAP + STATE flipped to CLOSED, anti-pattern recorded in CRITICAL_KNOWN_BUGS.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Commit closure artifacts and tag phase-39.1-closed</name>
  <read_first>
    - .planning/phases/phase-39.1/39.1-CLOSURE.md (just written in Task 3)
    - .planning/phases/phase-39.1/39.1-VERIFICATION.md (from Task 1 + Task 2)
    - Existing git log to confirm format of Phase 39 closure commit (`git log --oneline | grep -i "phase-39" | head -10`)
  </read_first>
  <files>(git commit only — no new source files)</files>
  <behavior>
    - Stage all files modified across 39.1-01, 39.1-02, 39.1-03, and this plan that have not been committed yet.
    - Create a single closure commit `chore(39.1): close Phase 39.1 — G2 seams + G3 board-switch + G1 [clean|partial]`.
    - Tag the closure commit as `phase-39.1-closed` (or `phase-39.1-closed-with-partial-g1` per Task 2 outcome) with an annotated tag.
  </behavior>
  <action>
    1. Run `git status` to inspect uncommitted changes.
    2. Stage the closure artifacts and any source-file edits that have not been committed by 39.1-01/02/03:
       - `.planning/phases/phase-39.1/39.1-VERIFICATION.md`
       - `.planning/phases/phase-39.1/39.1-CLOSURE.md`
       - `.planning/ROADMAP.md`
       - `.planning/STATE.md`
       - `.planning/CRITICAL_KNOWN_BUGS.md`
       Use specific file paths in `git add`, never `git add -A` (project safety protocol).
    3. Commit with message (HEREDOC pattern):
       ```
       chore(39.1): close Phase 39.1 — G2 H.264 seams + G3 board-switch race
       
       G2: ultra-high preset (24 Mbps single-layer) + per-triangle 1-px dilation
            in mesh-warp drawArrays. Phase 39-4 UV-inset preserved as orthogonal
            texture-boundary defense.
       G3: sync _currentBootHandle.stop() before any await in onProjectionProfileChange
            + _activationToken counter + captureCurrentAsLoadedSnapshot re-baselines
            on server-pushed align-grid-snapshot.
       G1: [clean via G2 cross-defect | partial-deferred to Phase 40]
       
       Carry-forwards intact: Phase 38 W10/W11/W12, Phase 39 D-01/D-02/D-03,
       D-08 connection-stability fail=0, VAAPI default-disabled.
       
       New anti-pattern in CRITICAL_KNOWN_BUGS.md #6: JPEG-CDP screenshot is
       the wrong test layer for H.264-stream artifacts. Use HTMLVideoElement
       pixel capture instead.
       
       Operator UAT 2026-05-13: [verbatim outcome from Task 2].
       
       Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
       ```
    4. Tag the commit annotated: `git tag -a phase-39.1-closed -m "Phase 39.1: SSR stabilization round 2 gap closure"` (or `phase-39.1-closed-with-partial-g1` per Task 2 outcome).
    5. Run `git status` after to confirm clean working tree.
    6. Run `git tag -l "phase-39*"` to confirm the new tag appears alongside `phase-39-closed-automated` (from Phase 39).
  </action>
  <verify>
    <automated>git tag -l 'phase-39.1-closed*' | grep -q "phase-39.1-closed" && git log -1 --pretty=%s | grep -q "39.1" && echo "OK tag and commit present"</automated>
  </verify>
  <acceptance_criteria>
    - `git tag -l "phase-39.1-closed*"` lists at least one tag matching the closure.
    - The latest commit message contains `39.1` and references G2 + G3 fixes.
    - `git status` shows a clean working tree (or only operator-untracked debug artifacts the operator already excluded).
    - No secrets staged: `git show HEAD --name-only` lists only files under `.planning/` and the source files edited by 39.1-01/02/03 (no `.env`, `credentials.json`, etc.).
  </acceptance_criteria>
  <done>Closure commit landed; phase-39.1-closed tag pushed (locally only — no remote push unless operator requests).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only runs existing tests, writes docs, and creates a git tag. No new code paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39.1-04-01 | Tampering | git tag misapplied | accept | Tag is annotated and signed by the user's git config; reversible via `git tag -d` if mistake found. |
| T-39.1-04-02 | Information Disclosure | git commit message contents | accept | Commit message contains no PII or secrets per the template. Operator-quote inclusion is the operator's own German text, voluntarily submitted. |
| T-39.1-04-03 | DoS | Regression matrix run time | accept | Total matrix run is ~3-5 min. Single execution per Task 1. |
</threat_model>

<verification>
**Phase 39.1 Plan 4 verification (Wave 2 / phase exit):**

1. 39.1-VERIFICATION.md exists with full regression matrix (Task 1).
2. Operator UAT (Task 2) returned a definitive response, recorded in 39.1-VERIFICATION.md.
3. 39.1-CLOSURE.md exists with all required sections (Task 3).
4. ROADMAP.md Phase 39.1 status flipped from PLANNING to CLOSED (Task 3).
5. STATE.md lifecycle pointers updated (Task 3).
6. CRITICAL_KNOWN_BUGS.md contains entry #6 for JPEG-CDP anti-pattern (Task 3).
7. Git tag `phase-39.1-closed` (or `phase-39.1-closed-with-partial-g1`) exists (Task 4).
8. All carry-forward tests still GREEN as recorded in 39.1-VERIFICATION.md (Task 1).
</verification>

<success_criteria>
- Full regression matrix run and recorded in 39.1-VERIFICATION.md.
- Every Phase 38 + Phase 39 + D-08 carry-forward stays GREEN.
- All three Phase 39.1 NEW tests (G1 backstop, G2 H.264, G3 board-switch) GREEN.
- Operator visual UAT confirms G2 + G3 clean; G1 is either clean (cross-defect) or formally PARTIAL-deferred.
- 39.1-CLOSURE.md written.
- ROADMAP.md + STATE.md flipped to CLOSED.
- CRITICAL_KNOWN_BUGS.md gains entry #6 (JPEG-CDP anti-pattern).
- Git tag `phase-39.1-closed` (or `phase-39.1-closed-with-partial-g1`) applied.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-39.1/39.1-04-SUMMARY.md` recording:
- Regression matrix totals (N green / K skip / L fail)
- Operator UAT outcome (verbatim quote if any)
- Git tag applied
- G1 final state: clean via G2 / partial-deferred / clean post-fallback
- Pointer to 39.1-CLOSURE.md
</output>
