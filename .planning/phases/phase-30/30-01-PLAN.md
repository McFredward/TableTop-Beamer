---
phase: 30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/runtime/live-sync/runtime-live-sync-core.js
  - src/app/runtime/live-sync/runtime-global-defaults.js
  - src/app/runtime/runtime-orchestration.js
  - src/app/runtime/core/runtime-bootstrap.js
  - src/app/lib/ui/runtime-panels-controller.js
  - src/styles.css
autonomous: false
requirements:
  - B3
user_setup: []
tags:
  - live-sync
  - diagnostic-overlay
  - render-stability

must_haves:
  truths:
    - "Toggling 'Show diagnostic overlay' ON in the dashboard System tab causes the .output-status-chip to appear on /output/ within ~100ms (D-11)."
    - "Toggling OFF causes the chip to disappear on /output/ within ~100ms."
    - "On /output/, document.body.dataset.diagnosticOverlay reflects state.diagnosticOverlay after every global-config-update broadcast."
    - "/output/ DevTools console shows a probe trace identifying which of the 6 hops fired (per D-09/D-10) before the fix is applied — 5 client-side probes cover hops 1/3/4/5a/5c/6; hop 2 (server) is verified out-of-band via disk inspection."
    - "After the fix is shipped, all 5 probes are removed; production code carries no leftover console.log noise."
    - "The 40-test non-regression baseline stays 40/40 green."
  artifacts:
    - path: "src/app/runtime/runtime-orchestration.js"
      provides: "syncDiagnosticOverlayPanel() (kept) + setDiagnosticOverlay() (kept) — DOM-write at line ~966 unchanged in shape."
    - path: "src/app/runtime/live-sync/runtime-live-sync-core.js"
      provides: "global-config-update WS handler invokes apply + syncRuntimePanelsFromState (broken hop fix lives here OR in one of the other 5 hops; identified by Task 2 probe trace)."
    - path: "src/styles.css"
      provides: ".output-status-chip CSS rule keyed on body[data-diagnostic-overlay='true']; selector intact on final-output role."
  key_links:
    - from: "Dashboard toggle (System tab)"
      to: "POST /api/global-defaults with { diagnosticOverlay: true|false }"
      via: "setDiagnosticOverlay → saveGlobalDefaultsToServer"
      pattern: "setDiagnosticOverlay\\(.*\\)"
    - from: "server.mjs handleGlobalDefaultsSave"
      to: "broadcastLiveSession('global-config-update', { target: 'config/global-defaults.json' })"
      via: "WS fanout"
      pattern: "broadcastLiveSession\\(.global-config-update."
    - from: "/output/ runtime-live-sync-core.js:525-577 broadcast handler"
      to: "applyGlobalDefaultsPayloadToState → state.diagnosticOverlay → syncDiagnosticOverlayPanel → document.body.dataset.diagnosticOverlay"
      via: "fetchGlobalDefaultsPayload + syncRuntimePanelsFromState"
      pattern: "applyGlobalDefaultsPayloadToState"
    - from: "document.body.dataset.diagnosticOverlay='true'"
      to: "CSS rule body[data-diagnostic-overlay='true'] .output-status-chip { display: inline-flex }"
      via: "CSS attribute selector"
      pattern: "data-diagnostic-overlay"
---

<objective>
Plan 30-01 (B3 — Diagnostic Overlay Live-Sync to /output/): the dashboard
"Show diagnostic overlay" toggle propagates to /output/ within ~100ms (D-11),
making the chip (version + fps + mode + canvas + frame cost) appear/disappear
in real time. This plan is sequenced FIRST (D-00) because Plan 30-02 (B1
Seams) requires the diagnostic overlay's `mode` readout on /output/ to
distinguish 2D-fallback vs GL-mode seams (cross-cutting constraint per
CONTEXT D-00).

Per CONTEXT D-09 the FIRST executable step is a diagnostic probe
(Toggle-Roundtrip-Smoke) covering the 6 hops mapped in RESEARCH §"B3
End-to-End Hop Map". 5 of those hops have client-side surface for a
console.log probe; hop 2 is server-side (handleGlobalDefaultsSave →
broadcastLiveSession) with no client code to instrument, so it is
verified out-of-band via server log + on-disk inspection of
`config/global-defaults.json` during Task 2 UAT. The fix follows from the
probe trace — RESEARCH inspection found all 6 hops correct in code, so
the regression must be a runtime delta (e.g. `state.localConfigDirty`
unexpectedly true on /output/, or WS reconnect race, or CSS specificity
collision).

Purpose: unblock B1 investigation by restoring the diagnostic readout on
/output/, and close the cross-client live-sync regression for the
diagnosticOverlay flag.

Output: live-sync of `state.diagnosticOverlay` from dashboard toggle to
/output/ DOM dataset to CSS chip visibility, ≤100ms end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/phase-30/30-CONTEXT.md
@.planning/phases/phase-30/30-RESEARCH.md
@.planning/phases/phase-30/30-VALIDATION.md
@.planning/phases/phase-28/28-CONTEXT.md
@.planning/phases/phase-26/SUMMARY.md

@src/app/runtime/runtime-orchestration.js
@src/app/runtime/live-sync/runtime-live-sync-core.js
@src/app/runtime/live-sync/runtime-global-defaults.js
@src/app/runtime/core/runtime-bootstrap.js
@src/app/lib/ui/runtime-panels-controller.js
@src/styles.css
@src/app/lib/state/runtime-state.js
@server.mjs

<interfaces>
<!-- The 6 hops the probe must instrument. Extracted from RESEARCH §"B3 End-to-End Hop Map". Use these exact file:line refs. -->

Hop 1: Dashboard toggle DOM event handler
  src/app/runtime/wire/runtime-wire-room-audio-binders.js:628-630
    diagnosticOverlayToggle.addEventListener("change", ...) → ctx.setDiagnosticOverlay(...)

Hop 1 setter:
  src/app/runtime/runtime-orchestration.js:968-975
    function setDiagnosticOverlay(nextEnabled) {
      state.diagnosticOverlay = Boolean(nextEnabled);
      syncDiagnosticOverlayPanel();
      saveGlobalDefaultsToServer().catch(...);
    }

Hop 2: Server endpoint
  server.mjs:3045-3088 (handleGlobalDefaultsSave) — typeof guard at 3049, persist + broadcastLiveSession at 3072-3081.

Hop 3: /output/ WebSocket message handler
  src/app/runtime/live-sync/runtime-live-sync-core.js:463-525
    socket.addEventListener("message", (event) => {
      const payload = JSON.parse(event.data);
      ...
      if (payload?.type === "global-config-update") { ... }   // line 525
    });

Hop 4: /output/ apply path guards
  src/app/runtime/live-sync/runtime-live-sync-core.js:548-577
    if (ctx.state.localConfigDirty) {
      ctx.state.remoteConfigUpdateAwaiting = true;
      ...
    } else if (ctx.shouldSuppressBroadcastReapply()) {
      // skip
    } else {
      void (async () => {
        const loaded = await ctx.fetchGlobalDefaultsPayload();
        ctx.applyGlobalDefaultsPayloadToState(loaded.payload, runtimeExtras);
        ctx.syncRuntimePanelsFromState();
        ...
      })();
    }

Hop 5a: state update
  src/app/runtime/live-sync/runtime-global-defaults.js:428-430
    if (payload && Object.prototype.hasOwnProperty.call(payload, "diagnosticOverlay")) {
      state.diagnosticOverlay = Boolean(payload.diagnosticOverlay);
    }

Hop 5b: panels-controller fan-out
  src/app/lib/ui/runtime-panels-controller.js:72
    if (typeof syncDiagnosticOverlayPanel === "function") syncDiagnosticOverlayPanel();
  Plus: src/app/runtime/core/runtime-bootstrap.js:64
    ctx.syncDiagnosticOverlayPanel?.();

Hop 5c: DOM write
  src/app/runtime/runtime-orchestration.js:957-967
    function syncDiagnosticOverlayPanel() {
      const enabled = Boolean(state.diagnosticOverlay);
      state.diagnosticOverlay = enabled;
      ...
      document.body.dataset.diagnosticOverlay = enabled ? "true" : "false";
    }

Hop 6: CSS visibility
  src/styles.css:125-159
    .output-status-chip { display: none; ... }
    body[data-diagnostic-overlay="true"] .output-status-chip { display: inline-flex; }
    body:not([data-output-role="final-output"]) .output-status-chip { ... } // dashboard variant

Initial state field:
  src/app/lib/state/runtime-state.js:61
    diagnosticOverlay: false,

Server schema:
  server.mjs:3049
    typeof parsed.diagnosticOverlay === "boolean"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instrument 5 client-side probes covering 6 hops (Toggle-Roundtrip-Smoke per CONTEXT D-09; hop 2 is server-side, verified out-of-band in Task 2)</name>
  <files>
    src/app/runtime/runtime-orchestration.js,
    src/app/runtime/live-sync/runtime-live-sync-core.js,
    src/app/runtime/live-sync/runtime-global-defaults.js
  </files>
  <read_first>
    src/app/runtime/runtime-orchestration.js (lines 950-980),
    src/app/runtime/live-sync/runtime-live-sync-core.js (lines 463-580),
    src/app/runtime/live-sync/runtime-global-defaults.js (lines 420-455),
    .planning/phases/phase-30/30-RESEARCH.md §"B3 End-to-End Hop Map"
  </read_first>
  <action>
Add 5 transient diagnostic probes (`console.log` with prefix `[B3-probe]`) at the
EXACT code positions documented in `<interfaces>` above for hops 1, 3, 4, 5a, 5c,
and 6 — that is 5 client-side probes covering 6 hops total (hop 2 is server-side
and is NOT instrumented in client code; it is verified in Task 2 via server log +
on-disk inspection of `config/global-defaults.json`).

Each probe MUST tag itself with the hop number, fire on every toggle event, and
include the smallest payload needed to distinguish a working hop from a broken one.
Keep probes side-effect-free (read state, never mutate). DO NOT modify ANY behavioral
logic — probes only.

The 5 client-side probes to add (use these exact strings so the user can grep
DevTools):

PROBE 1 — at top of `setDiagnosticOverlay(nextEnabled)` in
src/app/runtime/runtime-orchestration.js:968 (BEFORE the existing
`state.diagnosticOverlay = Boolean(nextEnabled);`).

Use this EXACT single line — no outputRole field, no ctx reference (the
function does not receive ctx in its signature, and inlining undefined
`ctx` symbols would throw a ReferenceError on Pi /output/):

```js
console.log("[B3-probe] hop1 setDiagnosticOverlay called", { nextEnabled });
```

PROBE 3 — at the start of the `if (payload?.type === "global-config-update")`
branch in src/app/runtime/live-sync/runtime-live-sync-core.js:525, AS THE FIRST
STATEMENT inside the if-block (before the `payload.target === "config/asset-manifest.json"` check):

```js
console.log("[B3-probe] hop3 global-config-update received on /output/", {
  outputRole: ctx.getOutputRole?.() ?? null,
  target: payload.target ?? null,
  hasDiagnosticOverlayInPayload: false, // payload doesn't contain diagnosticOverlay directly; will be in fetched body (hop 4)
});
```

PROBE 4 — INSIDE the if/else-if/else chain at runtime-live-sync-core.js:548-577,
add a SINGLE log line that fires regardless of branch (place it RIGHT BEFORE the
`if (ctx.state.localConfigDirty)` line at 548):

```js
console.log("[B3-probe] hop4 apply-path gate", {
  localConfigDirty: !!ctx.state?.localConfigDirty,
  shouldSuppressBroadcastReapply: typeof ctx.shouldSuppressBroadcastReapply === "function" ? ctx.shouldSuppressBroadcastReapply() : null,
  willTakeApplyBranch: !ctx.state?.localConfigDirty && !(typeof ctx.shouldSuppressBroadcastReapply === "function" && ctx.shouldSuppressBroadcastReapply()),
});
```

PROBE 5a — at the start of the `if (payload && Object.prototype.hasOwnProperty.call(payload, "diagnosticOverlay"))` block in
src/app/runtime/live-sync/runtime-global-defaults.js:428, INSIDE the if-block,
BEFORE the `state.diagnosticOverlay = Boolean(payload.diagnosticOverlay);` line:

```js
console.log("[B3-probe] hop5a state.diagnosticOverlay assignment", {
  incoming: payload.diagnosticOverlay,
  current: state.diagnosticOverlay,
  next: Boolean(payload.diagnosticOverlay),
});
```

PROBE 5c — at the start of `syncDiagnosticOverlayPanel()` in
src/app/runtime/runtime-orchestration.js:957, AS THE FIRST STATEMENT after the
`function syncDiagnosticOverlayPanel() {` line:

```js
console.log("[B3-probe] hop5c syncDiagnosticOverlayPanel called", {
  stateDiagnosticOverlay: !!state.diagnosticOverlay,
  bodyDatasetBefore: document.body?.dataset?.diagnosticOverlay ?? null,
});
```

PROBE 6 — append a logging tail at the very END of `syncDiagnosticOverlayPanel()`
in src/app/runtime/runtime-orchestration.js, AFTER the `document.body.dataset.diagnosticOverlay = ...;`
line at 966 (just before the closing `}` of the function):

```js
console.log("[B3-probe] hop6 DOM-write complete", {
  bodyDatasetAfter: document.body.dataset.diagnosticOverlay,
  chipDisplay: (() => {
    const chip = document.querySelector(".output-status-chip");
    return chip ? getComputedStyle(chip).display : "(no .output-status-chip element)";
  })(),
});
```

Hop 2 (server) is NOT instrumented — there is no client-side surface for
it. Verification of hop 2 happens via the user's UAT step (Task 2) by
inspecting `config/global-defaults.json` on disk and (optionally) the
server log after a toggle. This is why this task adds 5 client-side probes
even though the end-to-end chain has 6 hops.

DO NOT touch styles.css in this task. DO NOT remove or repurpose any
existing logic. Probes are pure additions.

Verification: after the edits, every file still parses (no syntax errors).
The 40-test suite must remain 40/40 green.
  </action>
  <verify>
    <automated>node --test "test/**/*.test.mjs" 2>&amp;1 | tail -5</automated>
    <grep>grep -c "B3-probe" src/app/runtime/runtime-orchestration.js src/app/runtime/live-sync/runtime-live-sync-core.js src/app/runtime/live-sync/runtime-global-defaults.js</grep>
  </verify>
  <acceptance_criteria>
    - `node --test "test/**/*.test.mjs"` reports `tests 40` and `pass 40`, `fail 0`.
    - `grep -c "B3-probe" src/app/runtime/runtime-orchestration.js` returns exactly 3 (hop1 + hop5c + hop6).
    - PROBE 1 contains the canonical literal: `grep -c "\[B3-probe\] hop1 setDiagnosticOverlay called" src/app/runtime/runtime-orchestration.js` returns 1 (this is the canonical wording — no outputRole field, no ctx reference).
    - `grep -c "B3-probe" src/app/runtime/live-sync/runtime-live-sync-core.js` returns >= 2 (hop3 + hop4).
    - `grep -c "B3-probe" src/app/runtime/live-sync/runtime-global-defaults.js` returns >= 1 (hop5a).
    - Total `grep -rn "B3-probe" src/app/` returns exactly 5 distinct probe markers (hops 1, 3, 4, 5a, 5c, 6 — note hop 5c + hop 6 share src/app/runtime/runtime-orchestration.js so by-file counts vary; what matters is exactly 5 distinct `[B3-probe] hopN ...` strings across the tree).
    - `node -e "require('fs').readFileSync('src/app/runtime/runtime-orchestration.js', 'utf8')"` succeeds (file is parseable as text — no syntax error introduced; production-side syntax check happens at browser load, but a heredoc-corruption sanity check works here).
    - No probe modifies state (grep: `grep "B3-probe" src/app/runtime/**/*.js | grep -v "console.log" | wc -l` returns 0 — every B3-probe line MUST be a console.log).
  </acceptance_criteria>
  <done>
    5 transient `[B3-probe]` console.log statements added at the file:line
    locations matching RESEARCH §"B3 End-to-End Hop Map" hops 1, 3, 4, 5a, 5c,
    and 6 — covering 6 hops with 5 client-side probes (hop 2 is server-side
    and is verified out-of-band in Task 2 via server log + disk inspection of
    config/global-defaults.json). PROBE 1 uses the canonical single-line form
    `console.log("[B3-probe] hop1 setDiagnosticOverlay called", { nextEnabled });`
    with no ctx/outputRole reference. The 40-test suite stays 40/40. No
    behavioral logic touched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-driven probe trace UAT — identify the broken hop</name>
  <files>(no code files modified — checkpoint task)</files>
  <action>This is a human-verify checkpoint. The executor MUST PAUSE here and surface the &lt;what-built&gt;, &lt;how-to-verify&gt; instructions, and &lt;resume-signal&gt; below to the user. Do NOT auto-resolve. Wait for the user to follow the manual UAT steps in &lt;how-to-verify&gt; and respond per &lt;resume-signal&gt;. The acceptance is gated on the user's response.</action>
  <verify><manual>See &lt;how-to-verify&gt; below for manual UAT steps.</manual></verify>
  <what-built>
    Five `[B3-probe]` console.log statements instrumenting the dashboard-toggle →
    /output/ live-sync chain (5 client-side probes covering 6 hops: 1, 3, 4, 5a,
    5c, 6 per RESEARCH §"B3 End-to-End Hop Map" — hop 2 is server-side, no client
    probe). Probes log to browser DevTools console with prefix `[B3-probe]`.
  </what-built>
  <how-to-verify>
    1. On the Pi, open `/output/` in Chromium with DevTools attached
       (`chrome://inspect` from a remote machine, OR connect a USB keyboard/mouse
       and open DevTools locally on the Pi).
    2. On the Dashboard PC, open the Dashboard URL and navigate to System tab
       (Settings ▸ System).
    3. Open DevTools console on BOTH the dashboard tab AND the Pi /output/ tab.
       Filter both consoles by `[B3-probe]`.
    4. On the dashboard, locate the "Show diagnostic overlay" toggle. Click it ON.
    5. Observe the dashboard console: hop1 should log immediately
       (`[B3-probe] hop1 setDiagnosticOverlay called { nextEnabled: true }`).
    6. Verify on the file system (via SSH or Finder): `config/global-defaults.json`
       should show `"diagnosticOverlay": true` within ~200ms of the toggle.
       Run from the project root: `cat config/global-defaults.json | grep diagnosticOverlay`.
    7. Observe the Pi /output/ console: WHICH hops fired?
       - Expected sequence on a working chain: hop3 → hop4 (willTakeApplyBranch=true)
         → hop5a (incoming=true, next=true) → hop5c (stateDiagnosticOverlay=true,
         bodyDatasetBefore="false") → hop6 (bodyDatasetAfter="true",
         chipDisplay="inline-flex").
       - Record which hop is the FIRST one that does NOT fire OR fires with an
         unexpected payload. That hop is the broken hop.
    8. Toggle OFF and repeat — same hop sequence in reverse should fire.
    9. Report findings: paste the FULL `[B3-probe]` log block from the Pi /output/
       console (both ON and OFF events) into the resume signal. Identify the
       broken hop by number (1/3/4/5a/5c/6) OR confirm "all 6 fire correctly but
       chip still doesn't appear" (which would point to a CSS specificity issue,
       hop 6 visual-only).
  </how-to-verify>
  <resume-signal>
    Paste the Pi /output/ DevTools console `[B3-probe]` log dump for both
    toggle-ON and toggle-OFF events, AND state the broken hop number (e.g.
    "broken hop: 4 — willTakeApplyBranch: false because localConfigDirty=true"),
    OR state "all 6 hops fire as expected — investigate CSS hop 6 visual".
  </resume-signal>
  <acceptance_criteria>
    - Pi /output/ DevTools console emitted at least one `[B3-probe]` line during
      the toggle test (proves at minimum hop 3 is reachable on /output/).
    - User identified the broken hop OR confirmed all-6-fire (CSS-only failure).
    - `config/global-defaults.json` confirmed to flip on disk after dashboard
      toggle (proves hop 1 + hop 2 work even if /output/ apply-chain is broken).
  </acceptance_criteria>
  <done>
    Broken hop identified by the user (hop number reported) OR all-6-fire
    confirmed pointing to CSS-only Hop 6 issue. Trace dump captured for
    posterity.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Apply pin-pointed fix at the broken hop</name>
  <files>
    src/app/runtime/live-sync/runtime-live-sync-core.js,
    src/app/runtime/live-sync/runtime-global-defaults.js,
    src/app/runtime/runtime-orchestration.js,
    src/app/runtime/core/runtime-bootstrap.js,
    src/app/lib/ui/runtime-panels-controller.js,
    src/styles.css
  </files>
  <read_first>
    The Task 2 resume-signal trace dump,
    src/app/runtime/live-sync/runtime-live-sync-core.js (lines 463-580),
    src/app/runtime/live-sync/runtime-global-defaults.js (lines 386-455 — REQUIRED step for CASE C: read lines 390-440 to identify the EXACT parameter name in the `applyGlobalDefaultsPayloadToState(payload, <PARAM_NAME>)` signature. The current codebase uses `runtimeSessionExtras` at line 390, but the executor MUST verify by reading the file before writing CASE C; do NOT use a generic placeholder name in the edit, and do NOT assume the name without confirmation),
    src/app/runtime/runtime-orchestration.js (lines 950-980),
    src/styles.css (lines 115-160),
    .planning/phases/phase-30/30-RESEARCH.md §"B3 End-to-End Hop Map" §"Probable broken hop"
  </read_first>
  <action>
Apply the MINIMAL fix to the broken hop identified in Task 2. RESEARCH lists
three runtime-only candidates the probe trace will discriminate; below is the
specific fix for each:

CASE A — Broken hop 4 (apply-path gated by `state.localConfigDirty`):
  Edit src/app/runtime/live-sync/runtime-live-sync-core.js around line 548.
  /output/ has no UI editing surface; `localConfigDirty` should not gate the
  apply-path on the final-output role. Wrap the gate so it ONLY applies to
  non-/output/ roles:

  Replace:
  ```js
  if (ctx.state.localConfigDirty) {
    ctx.state.remoteConfigUpdateAwaiting = true;
    ctx.refreshApplyDiscardButtonsUi();
  } else if (ctx.shouldSuppressBroadcastReapply()) {
    // skip
  } else {
  ```
  With:
  ```js
  // Phase 30 B3 fix: /output/ has no UI editing surface — localConfigDirty
  // should never gate the apply-path there. Force-apply on final-output role.
  const isFinalOutputRole = ctx.getOutputRole?.() === ctx.OUTPUT_ROLE_FINAL;
  if (!isFinalOutputRole && ctx.state.localConfigDirty) {
    ctx.state.remoteConfigUpdateAwaiting = true;
    ctx.refreshApplyDiscardButtonsUi();
  } else if (!isFinalOutputRole && ctx.shouldSuppressBroadcastReapply()) {
    // skip
  } else {
  ```

CASE B — Broken hop 3 (WebSocket message never arrives on /output/):
  Verify the WS reconnect logic at runtime-live-sync-core.js:582-588 (close →
  setTimeout 1200ms → connectLiveSyncSocket). If reconnect fires but message
  doesn't arrive, the issue is server-side — but Phase 30 scope excludes server
  changes per CONTEXT. Instead, file a follow-up issue and apply a defensive
  client-side workaround: inside the `socket.addEventListener("close", ...)`
  callback, log a warn so future regressions are caught early:
  ```js
  console.warn("[live-sync] socket closed; reconnecting in 1200ms");
  ```
  AND verify on Pi the WebSocket actually reconnects (this is a UAT-only assertion
  in Task 4).

CASE C — Broken hop 5a (state.diagnosticOverlay never updates after broadcast):
  This means the fetchGlobalDefaultsPayload payload doesn't include
  `diagnosticOverlay` field. Fix: ensure the GET /api/global-defaults endpoint
  returns the field. server.mjs:3579-3593 reads global-defaults.json verbatim,
  so if the field is in the file, it's in the response.

  **REQUIRED STEP BEFORE EDITING:** read
  `src/app/runtime/live-sync/runtime-global-defaults.js` lines 390-440 and
  identify the EXACT parameter name used by the
  `applyGlobalDefaultsPayloadToState(payload, <PARAM_NAME>)` signature. As of
  this plan's authoring the parameter is named `runtimeSessionExtras`, but the
  executor MUST confirm against the live file before writing the edit. If the
  signature differs, use the verified name — do NOT ship a hardcoded
  `runtimeSessionExtras` reference that would throw ReferenceError on
  /output/.

  Edit src/app/runtime/live-sync/runtime-global-defaults.js around line 428
  to ALSO accept the field via that verified parameter as a defensive
  secondary path. Pattern (substitute `<verified-extras-param-name>` with the
  actual name from the read_first step):
  ```js
  if (payload && Object.prototype.hasOwnProperty.call(payload, "diagnosticOverlay")) {
    state.diagnosticOverlay = Boolean(payload.diagnosticOverlay);
  } else if (<verified-extras-param-name> && Object.prototype.hasOwnProperty.call(<verified-extras-param-name>, "diagnosticOverlay")) {
    state.diagnosticOverlay = Boolean(<verified-extras-param-name>.diagnosticOverlay);
  }
  ```

CASE D — Broken hop 5c (syncDiagnosticOverlayPanel not called on /output/):
  Verify src/app/lib/ui/runtime-panels-controller.js:72 fires on /output/.
  RESEARCH confirms it should via the broadcast handler at runtime-live-sync-core.js:562
  (`ctx.syncRuntimePanelsFromState()`). If the trace shows hop5c never fires
  but hop5a does, add an explicit fallback call at runtime-live-sync-core.js
  right after `ctx.applyGlobalDefaultsPayloadToState(...)` at line 561:
  ```js
  if (ctx.getOutputRole?.() === ctx.OUTPUT_ROLE_FINAL && typeof ctx.syncDiagnosticOverlayPanel === "function") {
    ctx.syncDiagnosticOverlayPanel();
  }
  ```

CASE E — All 6 hops fire correctly (CSS-only failure at hop 6):
  Inspect src/styles.css lines 145-159. The dashboard-inline-variant rule at
  line 152-159 uses `body:not([data-output-role="final-output"])` which excludes
  /output/. Verify the body element on /output/ HAS `data-output-role="final-output"`
  set; if NOT, fix the bootstrap that sets it (search for `data-output-role`).
  If specificity collision exists, increase specificity of the visibility rule:
  ```css
  body[data-diagnostic-overlay="true"] .output-status-chip,
  body[data-output-role="final-output"][data-diagnostic-overlay="true"] .output-status-chip {
    display: inline-flex;
  }
  ```

Apply ONLY the case matching the Task 2 trace. DO NOT bundle multiple cases.
DO NOT remove the probes yet (Task 4 does that). DO NOT change unrelated logic.
  </action>
  <verify>
    <automated>node --test "test/**/*.test.mjs" 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "test/**/*.test.mjs"` reports `tests 40` and `pass 40`, `fail 0`.
    - The chosen case's edit appears in exactly ONE of the 6 listed files (verifiable by `git diff --name-only`).
    - All `[B3-probe]` console.log lines are still present (`grep -rc "B3-probe" src/app/` returns same count as Task 1).
    - No new `console.error` or `throw` introduced in the apply-chain.
    - For CASE A specifically: `grep -n "isFinalOutputRole" src/app/runtime/live-sync/runtime-live-sync-core.js` returns >=1 hit; `grep -n "localConfigDirty" src/app/runtime/live-sync/runtime-live-sync-core.js | head -2` confirms the gate is wrapped (the existing line + the new check).
    - For CASE C specifically: the new defensive `else if` block uses the verified parameter name from `read_first` (currently `runtimeSessionExtras` per the live signature at line 390). Verifiable: `grep -n "Object.prototype.hasOwnProperty.call.*diagnosticOverlay" src/app/runtime/live-sync/runtime-global-defaults.js` returns >=2 hits (the original payload check + the new extras-param check).
    - For CASE D specifically: `grep -n "ctx.syncDiagnosticOverlayPanel" src/app/runtime/live-sync/runtime-live-sync-core.js` returns >=1 hit.
    - For CASE E specifically: `grep -n "data-output-role.*diagnostic-overlay" src/styles.css` returns >=1 hit OR the existing line 145 selector retained verbatim and bootstrap data-output-role wiring verified.
  </acceptance_criteria>
  <done>
    Pin-pointed fix applied at the single broken hop identified by Task 2.
    40-test suite stays 40/40. Probe trace lines retained for Task 4 to clean up.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Pi /output/ live-sync UAT — chip appears within ~100ms</name>
  <files>(no code files modified — checkpoint task)</files>
  <action>This is a human-verify checkpoint. The executor MUST PAUSE here and surface the &lt;what-built&gt;, &lt;how-to-verify&gt; instructions, and &lt;resume-signal&gt; below to the user. Do NOT auto-resolve. Wait for the user to follow the manual UAT steps in &lt;how-to-verify&gt; and respond per &lt;resume-signal&gt;. The acceptance is gated on the user's response.</action>
  <verify><manual>See &lt;how-to-verify&gt; below for manual UAT steps.</manual></verify>
  <what-built>
    The Task 3 fix at the identified broken hop. Probes still in code for
    final verification.
  </what-built>
  <how-to-verify>
    1. Reload `/output/` on Pi (full reload to pick up Task 3 code changes).
    2. Open dashboard. Verify "Show diagnostic overlay" is OFF (chip absent on
       /output/, body[data-diagnostic-overlay="false"] in DevTools elements
       panel).
    3. Toggle ON in dashboard System tab. Within ≤200ms (well below the D-11
       acceptance bar of ~100ms-as-WS-broadcast latency, allowing UAT
       observation slack), the `.output-status-chip` MUST appear on /output/
       showing version + fps + mode + canvas + frame-cost.
    4. Toggle OFF. Within ≤200ms, the chip MUST disappear on /output/.
    5. Verify the Pi /output/ console `[B3-probe]` trace shows ALL hops firing
       cleanly (no localConfigDirty short-circuit, no missing hop).
    6. Repeat the toggle 5x in a row to confirm reliability — all 5 toggles
       must propagate to /output/.
  </how-to-verify>
  <resume-signal>
    Type "PASS" if chip appears/disappears reliably on Pi /output/ ≤200ms after
    every toggle. If not, paste the new probe trace dump showing residual
    failure point and tag it for revision.
  </resume-signal>
  <acceptance_criteria>
    - User reports PASS (chip toggles in real time on Pi /output/, all 5
      reliability iterations succeed).
    - Pi /output/ DevTools console `[B3-probe]` trace shows complete hop chain
      (1→3→4→5a→5c→6) with bodyDatasetAfter matching the toggle state.
    - `config/global-defaults.json` reflects the final toggle state on disk.
  </acceptance_criteria>
  <done>
    B3 acceptance bar D-11 (~100ms WS-broadcast cross-client live-sync of
    diagnosticOverlay flag to /output/) verified PASS on Pi-hardware.
  </done>
</task>

<task type="auto">
  <name>Task 5: Remove all 5 [B3-probe] console.log statements</name>
  <files>
    src/app/runtime/runtime-orchestration.js,
    src/app/runtime/live-sync/runtime-live-sync-core.js,
    src/app/runtime/live-sync/runtime-global-defaults.js
  </files>
  <read_first>
    All files modified in Task 1 (use git diff to find the exact probe lines
    to remove): `git diff HEAD~3 -- src/app/runtime/runtime-orchestration.js src/app/runtime/live-sync/runtime-live-sync-core.js src/app/runtime/live-sync/runtime-global-defaults.js`
  </read_first>
  <action>
Remove EVERY `console.log("[B3-probe] ...")` statement and ANY immediate
context lines that exist purely to compute the probe payload (e.g. the IIFE
inside the hop6 probe that queries `.output-status-chip`). DO NOT remove
the Task 3 fix lines.

Acceptable removal strategy: use the Task 1 file:line locations + the
exact opening string `console.log("[B3-probe]` to find each probe block,
remove from `console.log(` through the matching `);` (inclusive). Some
probes span multiple lines (especially hop6 with the IIFE) — remove the
entire block.

After removal, the affected files should be byte-identical to pre-Task-1
state EXCEPT for the Task 3 fix lines. Verify with:
`git diff HEAD~4 HEAD~3 -- <files>` (compares pre-Task-1 against post-Task-3)
should show ONLY Task 3 fix lines (probe additions reverted by this task).

Re-run the 40-test suite to confirm no regression.
  </action>
  <verify>
    <automated>node --test "test/**/*.test.mjs" 2>&amp;1 | tail -5</automated>
    <grep>grep -rc "B3-probe" src/app/ 2&gt;/dev/null</grep>
  </verify>
  <acceptance_criteria>
    - `grep -rn "B3-probe" src/app/` returns ZERO matches across the entire `src/app/` tree.
    - `grep -rn "B3-probe" src/` returns ZERO matches across all of `src/`.
    - `grep -rn "B3-probe" .` (excluding `.planning/`) returns ZERO production-code matches.
    - `node --test "test/**/*.test.mjs"` reports `tests 40` and `pass 40`, `fail 0`.
    - Task 3 fix lines remain (verifiable by re-running `grep -n "<case-specific-pattern>" <case-specific-file>` from Task 3 acceptance criteria).
  </acceptance_criteria>
  <done>
    All 5 `[B3-probe]` console.log statements removed from `src/app/`. The Task 3
    fix remains. 40-test suite stays 40/40.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: Final non-regression UAT — Phase 28 B6 and Phase 29 baseline</name>
  <files>(no code files modified — checkpoint task)</files>
  <action>This is a human-verify checkpoint. The executor MUST PAUSE here and surface the &lt;what-built&gt;, &lt;how-to-verify&gt; instructions, and &lt;resume-signal&gt; below to the user. Do NOT auto-resolve. Wait for the user to follow the manual UAT steps in &lt;how-to-verify&gt; and respond per &lt;resume-signal&gt;. The acceptance is gated on the user's response.</action>
  <verify><manual>See &lt;how-to-verify&gt; below for manual UAT steps.</manual></verify>
  <what-built>
    Production-ready Plan 30-01 fix: `state.diagnosticOverlay` lives-syncs from
    dashboard to /output/ within ≤200ms. No diagnostic console noise remains.
  </what-built>
  <how-to-verify>
    1. From a clean Pi /output/ reload, run a final smoke:
       (a) Toggle Show diagnostic overlay ON → chip appears on Pi /output/ ≤200ms.
       (b) Toggle OFF → chip disappears on Pi /output/ ≤200ms.
       (c) Open multiple Dashboard tabs simultaneously; toggle from one tab —
           BOTH dashboard tabs AND /output/ MUST update within ≤200ms.
    2. Confirm Phase 28 B6 dashboard-inline variant of the chip still works
       (chip in topbar, doesn't overlap logo).
    3. Confirm Phase 26 h9 baseline: render-mode toggle (auto/2d/gl) in System
       tab still works (independent of B3 fix; but verify no collateral damage).
       Switch through all three modes; observe no errors in /output/ console.
    4. Confirm Phase 29 baseline: 40-test suite is 40/40
       (`node --test "test/**/*.test.mjs"`).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 4 verification steps PASS. Otherwise describe the
    failing step.
  </resume-signal>
  <acceptance_criteria>
    - All 3 toggle scenarios (single-dashboard ON/OFF, multi-dashboard, /output/) work ≤200ms.
    - Phase 28 B6 dashboard-inline chip variant non-regressed.
    - Phase 26 h9 render-mode toggle non-regressed.
    - Test suite reports `tests 40` and `pass 40`, `fail 0`.
  </acceptance_criteria>
  <done>
    Plan 30-01 (B3) is production-ready. The cross-client live-sync of
    `diagnosticOverlay` is restored. Plan 30-02 (B1) can now begin investigation
    with the diagnostic readout active on Pi /output/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard browser → server (`POST /api/global-defaults`) | Dashboard UI sends `{ diagnosticOverlay: boolean }` to server; server is single-tenant LAN-only. |
| server → all clients (WebSocket `global-config-update` broadcast) | Server fanouts `{ target, savedAt, source }` envelope; clients refetch. |
| server filesystem (`config/global-defaults.json`) | Persisted state; no per-user permissions in scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Tampering | `POST /api/global-defaults` | mitigate | Existing `typeof parsed.diagnosticOverlay === "boolean"` typeof guard at `server.mjs:3049` rejects non-boolean payloads — already in place; B3 fix MUST NOT bypass this check (verified — fix only modifies CLIENT apply-path). |
| T-30-02 | Tampering | WebSocket `global-config-update` payload | accept | LAN-only deployment; server is the broadcast source; clients only listen and never trust unverified mutations. `applyGlobalDefaultsPayloadToState` has typeof checks for every applied field including `Boolean(payload.diagnosticOverlay)` (`runtime-global-defaults.js:429`). |
| T-30-03 | Information Disclosure | `[B3-probe]` console.log statements | mitigate | Probes are removed in Task 5 before merge. Acceptance criteria enforce zero residual `B3-probe` matches in `src/`. |

Phase 30 B3 introduces no new external surface. The fix re-routes existing
client-side state and CSS — server endpoints unchanged.
</threat_model>

<verification>
- 40-test non-regression baseline: `node --test "test/**/*.test.mjs"` returns 40/40 green.
- All `[B3-probe]` console.log lines removed: `grep -rn "B3-probe" src/` returns 0 matches.
- Phase 28 B6 dashboard-inline chip variant non-regressed (CSS rule at styles.css:152-159 intact).
- Phase 26 h9 render-mode toggle non-regressed (System tab still toggles auto/2d/gl).
- Phase 29 BOARD_PROFILE_FIELDS at 9 unchanged: `grep -c "^const BOARD_PROFILE_FIELDS" server.mjs` returns 1 (the constant lives in server.mjs, not runtime-state.js). The non-regression `node --test test/board-json-roundtrip.test.mjs` (already in the 40-test suite) is the authoritative gate; this grep is a fast smoke.
- Manual UAT on Pi /output/: chip appears/disappears within ≤200ms of dashboard toggle for 5 consecutive iterations.
</verification>

<success_criteria>
- Toggling "Show diagnostic overlay" ON in the dashboard System tab causes
  the `.output-status-chip` to appear on Pi /output/ within ≤200ms (D-11
  acceptance bar of ~100ms WS-broadcast latency, plus UAT observation slack).
- Toggling OFF causes the chip to disappear on Pi /output/ within ≤200ms.
- After 5 consecutive on/off toggles, the chip propagates correctly every
  time (no flaky state).
- The 40-test non-regression baseline (`node --test "test/**/*.test.mjs"`) is
  40/40 green throughout the plan.
- No `[B3-probe]` debug noise remains in production source after Task 5.
- Phase 28 B6 (chip in dashboard topbar) and Phase 26 h9 (render-mode toggle)
  are non-regressed.
- Plan 30-02 (B1 Seams) is unblocked: the Diagnostic Overlay's `mode` readout
  is now visible on Pi /output/ to discriminate 2D-fallback vs GL render
  paths during seam investigation.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-30/30-01-SUMMARY.md` documenting:
- The broken hop identified by the Task 2 probe trace.
- The exact fix applied at that hop (file:line + diff snippet).
- Pi UAT verification stamp (date + reliability iteration count).
- Confirmation that all 5 probes were removed (grep evidence).
- 40/40 test suite stamp.
</output>
