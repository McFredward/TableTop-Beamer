---
phase: 30
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js
  - src/app/runtime/viewport/runtime-projection-gl-renderer.js
autonomous: false
requirements:
  - B1
user_setup: []
tags:
  - render
  - mesh-warp
  - seams
  - render-stability

must_haves:
  truths:
    - "On Pi /output/ Test-Board Nemesis Lockdown Board A, solid-color animations show NO visible 1-pixel ridges or transformation seams within any room (D-04)."
    - "The same seam-free property holds for Malfunction, Fire, Slime, Scanning, Flicker, Alarm animations (D-04)."
    - "Both render modes work seam-free: render mode 2d AND render mode gl (D-01). Verifiable via System-tab Render-Mode toggle and the Diagnostic Overlay's mode readout (now working post-30-01)."
    - "Phase 26 h9 GL fix (`precision highp float;` + `TEXTURE_MIN/MAG_FILTER = NEAREST`) remains intact in src/app/runtime/viewport/runtime-projection-gl-renderer.js:96-153 — verifiable via grep."
    - "Phase 27 W4 mesh-warp behavior (trapezoid corners + squish bars + 80%/3×3 default grid) remains functional — Pi /output/ projection mapping continues to honor displaced grid points."
    - "The 40-test non-regression baseline stays 40/40 green."
  artifacts:
    - path: "src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js"
      provides: "drawAffineTriangle() with seam-free filtering strategy (NEAREST analog to GL fix per RESEARCH Pitfall 1 option (b)). postDrawMeshWarp2D() unchanged in shape."
    - path: "src/app/runtime/viewport/runtime-projection-gl-renderer.js"
      provides: "Phase 26 h9 GL fix block (highp + NEAREST) UNCHANGED. Plan 30-02 only touches GL renderer if the Task 2 UAT trace identifies a regression."
      contains: "precision highp float, TEXTURE_MIN_FILTER, _gl.NEAREST"
  key_links:
    - from: "Pi /output/ render dispatch"
      to: "postDrawMeshWarp (top-level dispatch in runtime-projection-mapping.js:145-192)"
      via: "renderMode field gates GL vs 2D path"
      pattern: "postDrawMeshWarp"
    - from: "postDrawMeshWarp (auto/gl)"
      to: "GL renderer (Phase 26 h9 highp+NEAREST shaders)"
      via: "_postDrawMeshWarpGL"
      pattern: "postDrawMeshWarpGL"
    - from: "postDrawMeshWarp (2d, or auto+no-WebGL)"
      to: "2D fallback renderer (drawAffineTriangle per cell triangle)"
      via: "postDrawMeshWarp2D"
      pattern: "drawAffineTriangle"
    - from: "Solid-color render"
      to: "Mesh-warp at frame post-process"
      via: "runtime-effect-visuals.js:280-285 fillRect bounding-box → clipToRoom polygon → mesh-warp consumes warp_tmp_canvas snapshot"
      pattern: "imageSmoothingEnabled"
---

<objective>
Plan 30-02 (B1 — Seams in Animations, especially solid-color): close the
visible 1-pixel ridges that appear within rooms on Pi /output/ across both
render modes (2D + GL). Per CONTEXT D-01 BOTH paths must be seam-free; per
D-02 the planner has free hand at the mesh-warp code (Phase 27 W4) provided
trapezoid + squish-bar behavior remains functional; per D-04 the acceptance
bar is visual UAT on Test-Board Nemesis Lockdown Board A in BOTH modes
across solid-color + 6 named animations.

Per CONTEXT cross-cutting constraint, this plan REQUIRES 30-01 (B3) PASS
because the diagnostic overlay's `mode` readout is the only way to confirm
which render path (2D vs GL) is active during UAT.

RESEARCH §"Pitfall 1" identifies the 2D-fallback `INFLATE = 1.5` strategy
with `imageSmoothingEnabled = true` as fundamentally seam-prone on uniform
solid-color content. The recommended fix (option b) is to switch the 2D
path to `imageSmoothingEnabled = false` (NEAREST analog) — symmetric to
the Phase 26 h9 GL fix. RESEARCH §"Open Question 1" notes B1 may be either
2D-only OR a Phase 27 W4 GL regression; Task 1 (UAT trace using the now-
working Diagnostic Overlay mode readout) discriminates BEFORE applying
the fix.

Purpose: deliver seam-free rendering on Pi /output/ for the listed
animations in both modes; preserve all Phase 27 trapezoid/squish behavior;
preserve the Phase 26 h9 GL fix block.

Output: a 2D-fallback renderer that no longer produces visible seams on
solid-color content, plus (if needed) a targeted GL-side adjustment if
Task 1 UAT shows GL-mode seams persist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/phase-30/30-CONTEXT.md
@.planning/phases/phase-30/30-RESEARCH.md
@.planning/phases/phase-30/30-VALIDATION.md
@.planning/phases/phase-26/SUMMARY.md
@.planning/phases/phase-30/30-01-PLAN.md

@src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js
@src/app/runtime/viewport/runtime-projection-gl-renderer.js
@src/app/runtime/viewport/runtime-projection-grid-state.js
@src/app/runtime/viewport/runtime-projection-mapping.js
@src/app/runtime/render/runtime-effect-visuals.js
@src/app/runtime/render/runtime-canvas-clip.js

<interfaces>
<!-- Key code locations from RESEARCH §"B1 Three Plausible Root Causes" + Pitfalls 1, 2, 3. Use these exact file:line refs. -->

2D Fallback renderer (RESEARCH Pitfall 1 — primary suspect for solid-color seams):
  src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js
    line 25: `function drawAffineTriangle(cctx, img, sx0, sy0, ..., dx2, dy2)`
    line 50:   const INFLATE = 1.5;
    line 70-73: cctx.imageSmoothingEnabled = true; cctx.imageSmoothingQuality = "high";
    line 83: function postDrawMeshWarp2D(canvas, canvasCtx) — top-level entry; calls drawAffineTriangle 2× per grid cell.

GL renderer (Phase 26 h9 fix block — DO NOT TOUCH unless Task 1 trace shows GL regression):
  src/app/runtime/viewport/runtime-projection-gl-renderer.js
    line 96-119: vertex+fragment shaders with `precision highp float;`
    line 135-153: NEAREST texture filtering at TEXTURE_2D bind.

Top-level dispatch:
  src/app/runtime/viewport/runtime-projection-mapping.js:145-192
    `postDrawMeshWarp(canvas, canvasCtx, renderMode)` — gates GL vs 2D vs no-warp.
    Auto: GL when grid has displacements, otherwise no warp.
    "2d": forces 2D-fallback path (postDrawMeshWarp2D).
    "gl": forces GL path even on identity warp.

Solid-color path (RESEARCH Pitfall 3):
  src/app/runtime/render/runtime-effect-visuals.js:280-285
    c.fillStyle = ...; c.fillRect(roomMinX, roomMinY, roomWidth, roomHeight);
  Plus clipToRoom at runtime-canvas-clip.js:76-83 (polygon clip BEFORE fillRect).

Phase 27 W4 default grid (RESEARCH Pitfall 2 — informational; do NOT change):
  src/app/runtime/viewport/runtime-projection-grid-state.js:50-82
    buildNewProfileDefaultGrid() — srcXs=[0,0.5,1] but dstXs=[0.10,0.50,0.90].
    hasGridDisplacements() returns TRUE on default → mesh-warp ALWAYS runs on /output/.

Render-mode toggle (UAT discriminator — gives diagnostic overlay readout):
  state.renderMode in src/app/lib/state/runtime-state.js, applied via
  applyGlobalDefaultsPayloadToState at runtime-global-defaults.js:423-426.

Diagnostic overlay mode readout (now working post-30-01):
  index.html:927-972 (samples renderMode, displays in chip).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: UAT discriminator — identify which render mode shows seams (per D-01)</name>
  <files>(no code files modified — checkpoint task)</files>
  <action>This is a human-verify checkpoint. The executor MUST PAUSE here and surface the &lt;what-built&gt;, &lt;how-to-verify&gt; instructions, and &lt;resume-signal&gt; below to the user. Do NOT auto-resolve. Wait for the user to follow the manual UAT steps in &lt;how-to-verify&gt; and respond per &lt;resume-signal&gt;. The acceptance is gated on the user's response.</action>
  <verify><manual>See &lt;how-to-verify&gt; below for manual UAT steps.</manual></verify>
  <what-built>
    Plan 30-01 PASS — Diagnostic Overlay live-syncs to Pi /output/ and shows
    `mode` (current render mode), enabling B1 root-cause discrimination.
  </what-built>
  <how-to-verify>
    1. On Pi /output/ load Test-Board Nemesis Lockdown Board A.
    2. On Dashboard System tab, ensure "Show diagnostic overlay" is ON.
       Verify the chip on Pi /output/ shows: version | fps | mode | canvas | frame-cost.
    3. Run the SEAM-DETECTION sweep across 3 modes — set `Render mode` in
       System tab to each value, wait for /output/ to apply, then trigger the
       solid-color animation on multiple rooms simultaneously:

       (a) Mode = "auto" — record: do you see 1-pixel seams within any room
           in solid-color? Record the chip's `mode` readout (likely shows
           "GL" because hasGridDisplacements()=true on default grid).

       (b) Mode = "2d" — record: do you see seams? Chip should show "2D".

       (c) Mode = "gl" — record: do you see seams? Chip should show "GL".

    4. Repeat for the 6 named animations (D-04 acceptance list): Malfunction,
       Fire, Slime, Scanning, Flicker, Alarm. For each animation, record
       seam-visible: Y/N per mode. (Solid-color is the most stress-testable
       because uniform color is the worst case — but D-04 requires the others
       seam-free too.)

    5. Inspect at projector viewing distance (Pi-driven projector ON), not
       just on the Pi's local HDMI display.
  </how-to-verify>
  <resume-signal>
    Report a 3×7 matrix (3 modes × {solid-color, Malfunction, Fire, Slime,
    Scanning, Flicker, Alarm}) of seam-visible Y/N. The matrix discriminates
    the fix scope:
    - If only mode "2d" shows seams: 2D-fallback fix (Pitfall 1 option b).
      Most likely outcome per RESEARCH.
    - If mode "auto" shows seams AND chip says "GL": Phase 27 W4 GL
      regression — escalate to a sub-investigation (D-14 allows scope split).
    - If all 3 modes show seams: combined fix needed.
    - If solid-color shows seams but the 6 named animations are clean: 2D-fix
      sufficient (content-rich textures mask sub-pixel drift, Pitfall 1
      Warning Signs).
  </resume-signal>
  <acceptance_criteria>
    - User reports the 3×7 seam-visible matrix (or equivalent narrative if Y/N is uncertain).
    - User confirms the chip's `mode` readout displays correctly on Pi /output/ (proves 30-01 PASS held).
    - At least one mode is identified as seam-visible OR user reports "no seams visible in any mode" (in which case Plan 30-02 short-circuits to a non-regression confirmation only).
  </acceptance_criteria>
  <done>
    Seam-visibility matrix recorded. Fix scope decided:
    Option α: 2D-fallback only (likely);
    Option β: GL also (Phase 27 W4 regression — extend Task 2 scope);
    Option γ: no seams observed (rare, would close Plan 30-02 with no code change beyond non-regression confirmation).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 2D-fallback seam fix — disable bilinear filtering (Pitfall 1 option b)</name>
  <files>src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js</files>
  <read_first>
    src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js (full file, 156 lines),
    src/app/runtime/viewport/runtime-projection-gl-renderer.js (lines 135-153 — symmetric NEAREST reference),
    .planning/phases/phase-30/30-RESEARCH.md §"Pitfall 1" + §"State of the Art" deprecated row,
    Task 1 resume-signal (the 3×7 matrix)
  </read_first>
  <action>
Apply the Pitfall 1 option (b) fix: switch the 2D-fallback path from
bilinear filtering to nearest-neighbor (NEAREST analog to the Phase 26 h9
GL fix).

In src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js,
INSIDE the `drawAffineTriangle` function body, REPLACE the current lines
70-73:

```js
    // Keep bilinear filtering on so the warp is smooth at the
    // seams instead of nearest-neighbor-aliased.
    cctx.imageSmoothingEnabled = true;
    cctx.imageSmoothingQuality = "high";
```

with:

```js
    // Phase 30 B1: bilinear filtering at per-triangle clip boundaries
    // produces visible 1-pixel ridges on uniform solid-color content
    // (every triangle samples a slightly different sub-texel near
    // shared edges). NEAREST forces one texel per fragment with no
    // interpolation — symmetric to the Phase-26-h9 GL fix
    // (`TEXTURE_MIN/MAG_FILTER = NEAREST` at runtime-projection-gl-
    // renderer.js:150-153). On a projector at typical viewing distance
    // NEAREST is imperceptible vs LINEAR for warp-target content; the
    // trade-off is entirely on the side of fewer seams.
    cctx.imageSmoothingEnabled = false;
```

Note: `imageSmoothingQuality` is meaningful only when smoothing is enabled,
so we drop the `cctx.imageSmoothingQuality = "high";` line entirely.

The `INFLATE = 1.5` strategy stays intact — the NEAREST switch eliminates
bilinear cross-talk; the inflate stays as belt-and-suspenders for the
clip-polygon edge-pixel coverage.

DO NOT modify `postDrawMeshWarp2D` (snapshot + redraw loop).
DO NOT modify the GL renderer in this task (handled in Task 4 if needed).
DO NOT modify `runtime-effect-visuals.js` (solid-color fillRect bounding-
box stays — Phase 26 h9 explicitly traded polygon-clip for performance).

Run the 40-test suite to confirm no behavioral regression in tested code
paths.
  </action>
  <verify>
    <automated>node --test "test/**/*.test.mjs" 2>&amp;1 | tail -5</automated>
    <grep>grep -n "imageSmoothingEnabled" src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js</grep>
  </verify>
  <acceptance_criteria>
    - `node --test "test/**/*.test.mjs"` reports `tests 40` and `pass 40`, `fail 0`.
    - `grep -c "imageSmoothingEnabled = true" src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js` returns 0.
    - `grep -c "imageSmoothingEnabled = false" src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js` returns 1.
    - `grep -c "imageSmoothingQuality" src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js` returns 0.
    - `grep -c "INFLATE = 1.5" src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js` returns 1 (Phase 27 W4 inflate kept).
    - Phase 26 h9 GL fix block intact: `grep -c "precision highp float" src/app/runtime/viewport/runtime-projection-gl-renderer.js` returns >=1; `grep -c "_gl.NEAREST" src/app/runtime/viewport/runtime-projection-gl-renderer.js` returns >=2 (TEXTURE_MIN_FILTER + TEXTURE_MAG_FILTER).
  </acceptance_criteria>
  <done>
    2D-fallback `drawAffineTriangle` now uses `imageSmoothingEnabled = false`
    (NEAREST analog). The Phase 26 h9 GL fix block is unchanged. The 40-test
    suite is 40/40.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pi /output/ UAT — seam-free in 2D mode (D-01 + D-04 partial)</name>
  <files>(no code files modified — checkpoint task)</files>
  <action>This is a human-verify checkpoint. The executor MUST PAUSE here and surface the &lt;what-built&gt;, &lt;how-to-verify&gt; instructions, and &lt;resume-signal&gt; below to the user. Do NOT auto-resolve. Wait for the user to follow the manual UAT steps in &lt;how-to-verify&gt; and respond per &lt;resume-signal&gt;. The acceptance is gated on the user's response.</action>
  <verify><manual>See &lt;how-to-verify&gt; below for manual UAT steps.</manual></verify>
  <what-built>
    2D-fallback renderer now uses NEAREST sampling — symmetric to the Phase
    26 h9 GL fix.
  </what-built>
  <how-to-verify>
    1. Reload Pi /output/ to pick up Task 2 changes.
    2. On Dashboard System tab set Render mode = "2d". Verify chip on Pi
       /output/ shows `mode: 2D`.
    3. Trigger solid-color animation on multiple rooms simultaneously on Test-
       Board Nemesis Lockdown Board A. Inspect at projector viewing distance.
    4. Verify NO visible 1-pixel ridges or transformation seams within any
       room. The interior of each room should appear truly uniform.
    5. Trigger each of the 6 named animations in turn (Malfunction, Fire,
       Slime, Scanning, Flicker, Alarm). Verify each is seam-free within a
       room.
    6. Verify trapezoid + squish behavior unchanged: enter Align Mode and
       drag a trapezoid corner; the projection geometry MUST follow the
       drag (Phase 27 W4 invariance preserved).
    7. Toggle render mode to "auto" — chip should show `mode: GL` (since
       default grid has displacements). Repeat the sweep at this mode.
    8. Toggle render mode to "gl" — repeat the sweep.
  </how-to-verify>
  <resume-signal>
    Type "PASS" if all 3 modes seam-free across all 7 stimuli (solid-color +
    6 named) at projector viewing distance AND trapezoid/squish unchanged.
    Otherwise describe which (mode, animation) combinations still show seams.
  </resume-signal>
  <acceptance_criteria>
    - User reports seam-free in 2D mode for solid-color + 6 named animations.
    - User reports seam-free in GL/auto mode for the same set OR escalates
      to Task 4 with a specific GL-mode complaint.
    - Trapezoid + squish behavior unchanged (Phase 27 W4 invariance).
    - The Diagnostic Overlay chip's `mode` readout matches the System tab
      selection (proves 30-01 fix held).
  </acceptance_criteria>
  <done>
    2D-mode acceptance bar D-04 met OR Task 4 (GL-mode regression
    investigation) escalation captured with specific failure data.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Conditional GL-mode adjustment — only if Task 3 reported GL seams</name>
  <files>src/app/runtime/viewport/runtime-projection-gl-renderer.js</files>
  <read_first>
    Task 3 resume-signal (specific failure data),
    src/app/runtime/viewport/runtime-projection-gl-renderer.js (lines 90-160 — Phase 26 h9 fix block),
    .planning/phases/phase-30/30-RESEARCH.md §"B1 Three Plausible Root Causes"
  </read_first>
  <action>
This task ALWAYS produces EXACTLY ONE marker file in
`.planning/phases/phase-30/`. The branch is determined by the Task 3
resume-signal:

**Branch 1 — Task 3 PASS (GL/auto modes are seam-free; no escalation needed):**

Write `.planning/phases/phase-30/30-02-task4-skipped.md` with content:

```
Task 4 skipped — Task 3 PASS confirmed GL/auto modes seam-free on Pi
/output/ Test-Board Nemesis Lockdown Board A. Phase 26 h9 fix block
intact (verified by grep at task-write time below).

Phase 26 h9 fix block grep verification:
- precision highp float: <count from grep -c "precision highp float" src/app/runtime/viewport/runtime-projection-gl-renderer.js>
- TEXTURE_MIN_FILTER NEAREST: <count from grep -c "TEXTURE_MIN_FILTER, _gl.NEAREST" src/app/runtime/viewport/runtime-projection-gl-renderer.js>
- TEXTURE_MAG_FILTER NEAREST: <count from grep -c "TEXTURE_MAG_FILTER, _gl.NEAREST" src/app/runtime/viewport/runtime-projection-gl-renderer.js>
```

DO NOT touch any source code in this branch. DO NOT modify the GL renderer.

**Branch 2 — Task 3 reported GL-mode (or auto-mode showing "GL" in chip) seams:**

Write `.planning/phases/phase-30/30-02-GL-ESCALATION.md` documenting:
   - The Task 3 failure data (which animations, which mode).
   - Re-verification of the Phase 26 h9 fix block (intact verified by grep):
     - `grep -c "precision highp float" src/app/runtime/viewport/runtime-projection-gl-renderer.js` MUST be >=1.
     - `grep -c "TEXTURE_MIN_FILTER, _gl.NEAREST" src/app/runtime/viewport/runtime-projection-gl-renderer.js` MUST be 1.
     - `grep -c "TEXTURE_MAG_FILTER, _gl.NEAREST" src/app/runtime/viewport/runtime-projection-gl-renderer.js` MUST be 1.
   - Suggested follow-up investigation (Phase 27 W4 mesh-triangulation
     review; possible sub-pixel-rounding fix in `runtime-projection-grid-
     state.js getStableRoomStretchAnchor`).
   - User decision required: separate hotfix or fold into Plan 30-02
     extended scope (D-14 allows scope split).

In this branch, this task ONLY writes the escalation document — it does
NOT apply any GL-side code edit. The h9 fix block stays untouched
(per CONTEXT D-02 boundary). Code-side defensive edits (if any) would
be the user's decision and would land in a follow-up plan.

**After writing the marker file (either branch):**

Run `node --test "test/**/*.test.mjs"` to confirm 40/40 stays green.

DO NOT modify the h9 fix block (highp + NEAREST). DO NOT change the
default-grid layout (Phase 27 W4 invariance per CONTEXT D-02 boundary).
DO NOT create both marker files — exactly one MUST exist after this task.
  </action>
  <verify>
    <automated>node --test "test/**/*.test.mjs" 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `node --test "test/**/*.test.mjs"` reports `tests 40` and `pass 40`, `fail 0`.
    - Phase 26 h9 GL fix block intact: `grep -c "precision highp float" src/app/runtime/viewport/runtime-projection-gl-renderer.js` >=1; `grep -c "_gl.NEAREST" src/app/runtime/viewport/runtime-projection-gl-renderer.js` >=2.
    - EITHER `.planning/phases/phase-30/30-02-task4-skipped.md` exists (Task 3 PASS path) OR `.planning/phases/phase-30/30-02-GL-ESCALATION.md` exists (Task 3 escalation path).
  </acceptance_criteria>
  <done>
    GL-mode is verified-intact (Task 3 PASS path → skip marker written) OR
    a GL-mode regression escalation document was filed for follow-up.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Final non-regression UAT — Phase 26 h9 + Phase 27 W4 baseline</name>
  <files>(no code files modified — checkpoint task)</files>
  <action>This is a human-verify checkpoint. The executor MUST PAUSE here and surface the &lt;what-built&gt;, &lt;how-to-verify&gt; instructions, and &lt;resume-signal&gt; below to the user. Do NOT auto-resolve. Wait for the user to follow the manual UAT steps in &lt;how-to-verify&gt; and respond per &lt;resume-signal&gt;. The acceptance is gated on the user's response.</action>
  <verify><manual>See &lt;how-to-verify&gt; below for manual UAT steps.</manual></verify>
  <what-built>
    Plan 30-02 fix complete: 2D-fallback uses NEAREST sampling. GL fix block
    intact. Trapezoid + squish unchanged.
  </what-built>
  <how-to-verify>
    1. From a clean Pi /output/ reload, run a final smoke on Test-Board Nemesis
       Lockdown Board A:

       (a) Render mode = 2d → trigger solid-color on 3 rooms simultaneously →
           NO visible seams within any room at projector viewing distance.
       (b) Render mode = gl → same test → NO visible seams.
       (c) Render mode = auto → same test → NO visible seams.
       (d) For each mode: trigger Malfunction, Fire, Slime, Scanning, Flicker,
           Alarm in turn → no seams in any.

    2. Verify Phase 27 W4 trapezoid + squish behavior:
       (e) Open Align Mode. Drag a trapezoid corner — projection geometry
           follows the drag.
       (f) Drag a squish bar — grid scales uniformly.
       (g) Reset to default — grid returns to 80%/3×3 layout.

    3. Verify Phase 28 B6 + Plan 30-01 cross-client live-sync:
       (h) Toggle Show diagnostic overlay OFF then ON — chip propagates to
           /output/ ≤200ms (proves 30-01 non-regressed).

    4. Verify the 40-test suite stays 40/40:
       `node --test "test/**/*.test.mjs"`.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 8 verification points (a-h + test suite) PASS.
    Otherwise describe the failing point.
  </resume-signal>
  <acceptance_criteria>
    - All 3 render modes × 7 stimuli (solid-color + 6 named) seam-free on Pi /output/.
    - Phase 27 W4 trapezoid + squish behavior unchanged.
    - Phase 28 B6 + Plan 30-01 chip live-sync unchanged.
    - `node --test "test/**/*.test.mjs"` returns 40/40.
  </acceptance_criteria>
  <done>
    Plan 30-02 (B1) is production-ready. Solid-color and the 6 named
    animations render seam-free on Pi /output/ in both render modes. Phase
    27 W4 + Phase 26 h9 + Plan 30-01 baselines are non-regressed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser canvas → GL/2D mesh-warp | Pure client-side rendering; no untrusted input crosses this boundary. |
| persisted profile (`config/global-defaults.json` projection grid) → renderer | Server-authoritative grid points; LAN-only single-tenant deployment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | DoS | 2D-fallback `drawAffineTriangle` per-triangle clip | accept | Per-cell triangle count is bounded by grid dimensions (4×4 default → 18 triangles per frame). NEAREST sampling is cheaper than bilinear; no DoS risk introduced. |
| T-30-05 | Tampering | grid points read from server config | accept | Existing typeof guards and clamps in `runtime-projection-grid-state.js` (Phase 27 W4) — not modified by Plan 30-02. |

Phase 30 B1 introduces no new external surface — fix is local to client-side
canvas state (single property change).
</threat_model>

<verification>
- 40-test non-regression baseline: `node --test "test/**/*.test.mjs"` returns 40/40 green.
- 2D fix applied: `grep -c "imageSmoothingEnabled = false" src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js` returns 1.
- Bilinear path removed: `grep -c "imageSmoothingEnabled = true" src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js` returns 0.
- Phase 27 W4 INFLATE strategy retained: `grep -c "INFLATE = 1.5" src/app/runtime/viewport/runtime-projection-2d-fallback-renderer.js` returns 1.
- Phase 26 h9 GL fix intact: `grep -c "precision highp float" src/app/runtime/viewport/runtime-projection-gl-renderer.js` >=1 AND `grep -c "_gl.NEAREST" src/app/runtime/viewport/runtime-projection-gl-renderer.js` >=2.
- Phase 29 schema invariant: BOARD_PROFILE_FIELDS at 9 unchanged (no schema field added; Plan 30-02 touches only renderer code).
- Manual UAT on Pi /output/ Test-Board Nemesis Lockdown Board A: solid-color + 6 named animations seam-free in all 3 render modes (auto, 2d, gl).
</verification>

<success_criteria>
- On Pi /output/ Test-Board Nemesis Lockdown Board A, solid-color animations
  show no visible 1-pixel ridges or transformation seams within any room
  (D-04).
- The same seam-free property holds for Malfunction, Fire, Slime, Scanning,
  Flicker, Alarm animations.
- Both render modes (2D + GL) are seam-free (D-01).
- Phase 26 h9 GL fix block remains intact (`precision highp float` +
  `TEXTURE_MIN/MAG_FILTER = NEAREST`).
- Phase 27 W4 trapezoid + squish behavior remains functional (drag
  trapezoid corner, drag squish bar, reset to default — all behave as in
  Phase 27).
- Plan 30-01 (B3 chip live-sync) non-regressed.
- 40-test suite stays 40/40.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-30/30-02-SUMMARY.md` documenting:
- The Task 1 3×7 seam-visibility matrix (UAT discriminator result).
- The exact 2D-fallback fix applied (file:line + diff snippet).
- Whether Task 4 was skipped (Task 3 PASS path) or escalated (GL regression
  filed) — with the marker file path.
- Pi UAT verification stamp (date + 3 modes × 7 animations matrix).
- Confirmation Phase 26 h9 + Phase 27 W4 + Plan 30-01 baselines non-regressed.
- 40/40 test suite stamp.
</output>
