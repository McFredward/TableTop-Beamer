---
phase: 47
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/ssr-render-host.mjs
  - test/phase-47-launch-args.test.mjs
  - test/phase-47-linux-non-regression.test.mjs
autonomous: true
requirements: [D-02, D-07, D-08, D-09]

must_haves:
  truths:
    - "`buildChromiumLaunchArgs({ platform, opts })` is exported from src/server/ssr-render-host.mjs as a pure function"
    - "Calling it with platform='linux' produces an arg array byte-identical to the iter15 Linux arg list"
    - "Calling it with platform='win32' produces the iter15 Windows arg list (still headless:false, still --app=about:blank, still --window-position=-32000,-32000, still emits --display=${display} unconditionally per iter15) — NO behavior change in this plan"
    - "launchBrowser() internally uses buildChromiumLaunchArgs() to assemble args; the only difference between platforms lives in that one function"
    - "`./start.sh --dry-run` exits 0 on Linux"
    - "`npm test` reports 384 passing / 1 pre-existing fail / 19 skipped (same baseline as iter15)"
  artifacts:
    - path: "src/server/ssr-render-host.mjs"
      provides: "Refactored launchBrowser delegating to buildChromiumLaunchArgs"
      contains: "export function buildChromiumLaunchArgs"
    - path: "test/phase-47-launch-args.test.mjs"
      provides: "Unit tests pinning Windows + Linux arg shapes (iter15 baseline both sides — D-02 + D-08)"
      exports: ["tests for win32 + linux branches"]
    - path: "test/phase-47-linux-non-regression.test.mjs"
      provides: "Pinned snapshot test — Linux arg list MUST equal iter15 byte-for-byte; WIN32_ITER15_BASELINE includes --display=:99 (iter15 emits it unconditionally on both platforms)"
      contains: "iter15 baseline"
  key_links:
    - from: "src/server/ssr-render-host.mjs#launchBrowser"
      to: "src/server/ssr-render-host.mjs#buildChromiumLaunchArgs"
      via: "delegation — launchBrowser composes its `args:` property by calling buildChromiumLaunchArgs and spreading its return value"
      pattern: "args:\\s*buildChromiumLaunchArgs\\("
    - from: "test/phase-47-linux-non-regression.test.mjs"
      to: "src/server/ssr-render-host.mjs#buildChromiumLaunchArgs"
      via: "imports + asserts byte-identity against pinned snapshot"
      pattern: "buildChromiumLaunchArgs"
---

<objective>
Phase 47 Wave 1 — Refactor `launchBrowser()` in `src/server/ssr-render-host.mjs`
to extract a pure, exported function `buildChromiumLaunchArgs({ platform, opts })`
that builds the Chromium command-line argument array. Pin both the Linux and
Windows arg lists with unit tests against the iter15 baseline. NO behavior
change — Linux byte-identical, Windows still iter15 (headless:false,
--app=about:blank, --window-position=-32000,-32000, --display=${display}
emitted unconditionally per iter15 source line 644).

This plan exists solely to install the non-regression rail before Wave 2 changes
the Windows path. Without these tests, Wave 2's headless-new flip cannot be
verified to leave Linux untouched.

Purpose: Provide a unit-testable seam (D-02 non-regression rail, D-08 build-on-top
of iter15) so Wave 2 can flip the Windows arg set with full Linux confidence.
Output: Refactored render-host module + two new unit-test files, all tests green,
`./start.sh --dry-run` still exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/phase-47/47-CONTEXT.md
@.planning/phases/phase-47/47-RESEARCH.md
@.planning/phases/phase-47/47-VALIDATION.md
@.planning/phases/phase-46/46-CLOSURE.md
@src/server/ssr-render-host.mjs
@test/phase-34-chrome-flags.test.mjs

<interfaces>
<!-- Current iter15 launchBrowser signature + the function-body locals that must
     be passed into buildChromiumLaunchArgs. Executor uses these directly — no
     codebase scavenger hunt needed. -->

From src/server/ssr-render-host.mjs (iter15, lines 430-647):

```js
async function launchBrowser({ browserPath } = {}) {
  // ... reads module-scope `port`, `viewport`, `display`, `status.encoderConfig`
  const ssrUrl = `http://127.0.0.1:${port}/ssr`;

  const disabledFeatures = [
    "CalculateNativeWinOcclusion",
    "IntensiveWakeUpThrottling",
    "BackForwardCache",
    "Translate",
    "TranslateUI",
    "PasswordManagerOnboarding",
    "InterestFeedV2",
    "AutofillServerCommunication",
  ];

  const hasIgpuDev =
    existsSync("/dev/dri/renderD128") || existsSync("/dev/dri/renderD129");
  const hasVaapiEnabled =
    process.env.SSR_ENABLE_VAAPI === "1" && hasIgpuDev;

  const enabledFeatures = [
    ...(hasVaapiEnabled ? ["VaapiVideoEncoder", "VaapiVideoDecoder", "VaapiIgnoreDriverChecks"] : []),
    ...(status.encoderConfig?.encoder === "nvenc" ? ["H264HardwareEncode"] : []),
    ...(status.encoderConfig?.encoder === "videotoolbox" ? ["PlatformHEVCEncoderSupport"] : []),
    "TabCaptureFastPath",
  ];

  const isWin32 = process.platform === "win32";
  let winUserDataDir = null;
  if (isWin32) { /* mkdir tmp dir, see lines 537-547 */ }

  return launcher({
    executablePath: browserPath,
    headless: false,  // iter15 hard-coded
    defaultViewport: viewport,
    ignoreDefaultArgs: ["--enable-automation"],
    ...(winUserDataDir ? { userDataDir: winUserDataDir } : {}),
    args: [ /* the 30-ish flags currently inline in lines 558-645 */ ],
    env: { ...process.env, DISPLAY: display },
  });
}
```

The full iter15 arg array (lines 558-645) is the byte-identical baseline both
test files must pin. Read it directly from the file — do not re-derive from
this excerpt.

IMPORTANT iter15 fact: line 644 emits `\`--display=\${display}\`` UNCONDITIONALLY
(no isWin32 gate). Therefore the iter15 win32 arg list DOES contain
`--display=:99` (it's a no-op on Windows Chrome — there is no X server — but
it is literally in the args array). Both Wave 1 baseline constants reflect
this. Wave 2 (47-02) is responsible for adding the win32 gate and updating
the baselines accordingly.

Target shape post-refactor:

```js
export function buildChromiumLaunchArgs({
  platform,        // "win32" | "linux" | "darwin" | etc. (only "win32" branches today)
  ssrUrl,
  viewport,        // { width, height }
  display,         // string like ":99"
  disabledFeatures,// string[]
  enabledFeatures, // string[]
  hasVaapiEnabled, // boolean
}) {
  const isWin32 = platform === "win32";
  return [
    "--no-sandbox",
    // ... full arg list, EXACTLY mirroring iter15 line-by-line
  ];
}
```

Test fixture invocation (both test files):

```js
const linuxArgs = buildChromiumLaunchArgs({
  platform: "linux",
  ssrUrl: "http://127.0.0.1:4173/ssr",
  viewport: { width: 1920, height: 1080 },
  display: ":99",
  disabledFeatures: ["CalculateNativeWinOcclusion","IntensiveWakeUpThrottling","BackForwardCache","Translate","TranslateUI","PasswordManagerOnboarding","InterestFeedV2","AutofillServerCommunication"],
  enabledFeatures: ["TabCaptureFastPath"],
  hasVaapiEnabled: false,
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the pinned-snapshot unit test for Linux + Windows iter15 baseline (Wave 0 — tests FIRST, then refactor)</name>
  <files>test/phase-47-launch-args.test.mjs, test/phase-47-linux-non-regression.test.mjs</files>
  <read_first>
    - src/server/ssr-render-host.mjs (lines 430-647 — read the exact iter15 args block; the snapshot MUST match this verbatim; note line 644 — `--display=${display}` is emitted unconditionally, no isWin32 gate, so the win32 baseline MUST include `--display=:99`)
    - test/phase-34-chrome-flags.test.mjs (proven pattern: import from ssr-render-host, regex/extract launchBrowser body, assert on it — use the same import/assert style)
    - .planning/phases/phase-47/47-RESEARCH.md (§ Q3 table — flag-by-flag canonical mapping)
    - .planning/phases/phase-47/47-VALIDATION.md (Wave 0 Requirements + Per-Task Verification Map rows 47-01-01..47-01-03)
  </read_first>
  <behavior>
    test/phase-47-launch-args.test.mjs (covers both branches at unit-level):
    - Test A: `buildChromiumLaunchArgs` is exported from src/server/ssr-render-host.mjs as a function
    - Test B: With platform="linux", the returned array includes `--ozone-platform=x11`, `--display=:99`, `--app=http://127.0.0.1:4173/ssr`, `--start-fullscreen`, `--window-position=0,0` (Linux iter15 fingerprint)
    - Test C: With platform="win32", the returned array does NOT include `--ozone-platform=x11` (iter15-gated to Linux) and does NOT include `--start-fullscreen` (also iter15-gated to Linux), and DOES include `--window-position=-32000,-32000`, `--app=about:blank`, AND `--display=:99` (Windows iter15 fingerprint — `--display=` is emitted unconditionally in iter15 source line 644, treated as a Win Chrome no-op but present in args; UNCHANGED in this wave)
    - Test D: With platform="win32", `--auto-select-tab-capture-source-by-title=TableTop Beamer` is present (D-D2 carry-over)
    - Test E: With platform="linux" + hasVaapiEnabled=true, the array includes `--ignore-gpu-blocklist` and `--enable-gpu-rasterization` (Phase 34 h2 carry-over)
    - Test F: With platform="linux" + hasVaapiEnabled=false, neither of those VAAPI-gated flags appears

    test/phase-47-linux-non-regression.test.mjs (pinned snapshot — byte-identity):
    - Test G: With platform="linux", default opts, the returned array EQUALS a hand-pinned const LINUX_ITER15_BASELINE = [ ...the exact 27-ish strings copied verbatim from iter15 lines 558-645 in array order... ]. Use `assert.deepStrictEqual(linuxArgs, LINUX_ITER15_BASELINE)`. The baseline array MUST be inline in the test file (not imported) so any future drift in ssr-render-host trips this test.
    - Test H: Same as G but with hasVaapiEnabled=true — pinned baseline includes the two extra VAAPI flags between --enable-features and --display=.
    - Test I: With platform="win32" and the same options, the returned array EQUALS a hand-pinned const WIN32_ITER15_BASELINE — same flags as linux but with the win32 branches taken: no `--ozone-platform=x11`, no `--start-fullscreen` (both iter15-gated to Linux), `--window-position=-32000,-32000` instead of `0,0`, `--app=about:blank` instead of `--app=ssrUrl`, AND `--display=:99` STILL PRESENT (iter15 emits it unconditionally — line 644 has no isWin32 gate). This is true iter15 byte-identity on Win32.

    These tests MUST be added BEFORE Task 2's refactor and MUST FAIL on master (because buildChromiumLaunchArgs does not yet exist). Tests turning red on master is the RED phase signal.
  </behavior>
  <action>
    Create two new test files under `test/`:

    (1) `test/phase-47-launch-args.test.mjs` — six tests A-F covering both platform branches as fingerprint assertions (presence/absence of marker flags). Imports `buildChromiumLaunchArgs` from `../src/server/ssr-render-host.mjs`. Uses `node:test` + `node:assert/strict` like phase-34-chrome-flags.test.mjs. NO regex extraction — call the function directly with explicit args.

    (2) `test/phase-47-linux-non-regression.test.mjs` — three tests G, H, I pinning byte-identity against hand-copied constant arrays. Each baseline is a `const LINUX_ITER15_BASELINE = [ "--no-sandbox", "--autoplay-policy=no-user-gesture-required", "--ozone-platform=x11", "--use-gl=angle", "--use-angle=default", "--enable-unsafe-swiftshader", "--disable-dev-shm-usage", "--disable-background-timer-throttling", "--disable-backgrounding-occluded-windows", "--disable-renderer-backgrounding", "--disable-ipc-flooding-protection", "--disable-gpu-vsync", "--disable-frame-rate-limit", "--max-gum-fps=60", "--app=http://127.0.0.1:4173/ssr", "--window-size=1920,1080", "--window-position=0,0", "--start-fullscreen", "--auto-select-tab-capture-source-by-title=TableTop Beamer", "--no-first-run", "--no-default-browser-check", "--disable-default-apps", "--disable-prompt-on-repost", "--disable-popup-blocking", "--disable-notifications", "--disable-sync", "--mute-audio", "--hide-crash-restore-bubble", "--disable-session-crashed-bubble", "--disable-infobars", "--disable-features=CalculateNativeWinOcclusion,IntensiveWakeUpThrottling,BackForwardCache,Translate,TranslateUI,PasswordManagerOnboarding,InterestFeedV2,AutofillServerCommunication", "--enable-features=TabCaptureFastPath", "--display=:99" ]` — read ssr-render-host.mjs lines 558-645 and reproduce the array EXACTLY in that order. Use `assert.deepStrictEqual` so any single-flag drift is reported with a clean diff. Do the same for WIN32_ITER15_BASELINE: same array but drop `--ozone-platform=x11`, drop `--start-fullscreen` (both are iter15-gated to Linux); replace `--app=http://127.0.0.1:4173/ssr` with `--app=about:blank`; replace `--window-position=0,0` with `--window-position=-32000,-32000`; KEEP `--display=:99` at the end (iter15 source line 644 emits it unconditionally — no isWin32 gate exists today; this is byte-identity to iter15). Wave 2 (47-02) will introduce the win32 gate and update this baseline in the same commit as the implementation change.

    Note: viewport fixture is { width: 1920, height: 1080 } (current Phase 32 default). If reading current code reveals a different default, use the actual default. `display` fixture is ":99". `ssrUrl` is built from port=4173.

    Run `node --test test/phase-47-launch-args.test.mjs test/phase-47-linux-non-regression.test.mjs` — both files MUST currently fail (import error: `buildChromiumLaunchArgs` is not exported). That failure is the RED signal. Commit RED test files with message `test(47-01): RED — pin Linux+Windows iter15 arg lists`.
  </action>
  <verify>
    <automated>node --test test/phase-47-launch-args.test.mjs test/phase-47-linux-non-regression.test.mjs 2>&amp;1 | tail -15; echo "(expected RED: import error on buildChromiumLaunchArgs)"</automated>
  </verify>
  <acceptance_criteria>
    - `test/phase-47-launch-args.test.mjs` exists and contains the literal string `import { buildChromiumLaunchArgs }` (grep: `grep -q "import { buildChromiumLaunchArgs }" test/phase-47-launch-args.test.mjs`)
    - `test/phase-47-linux-non-regression.test.mjs` exists and contains the literal string `LINUX_ITER15_BASELINE` (grep: `grep -q "LINUX_ITER15_BASELINE" test/phase-47-linux-non-regression.test.mjs`)
    - The Linux baseline array in the test contains the exact string `"--ozone-platform=x11"` (grep: `grep -q '"--ozone-platform=x11"' test/phase-47-linux-non-regression.test.mjs`)
    - The Windows baseline array in the test contains the exact string `"--window-position=-32000,-32000"` (grep: `grep -q -- '--window-position=-32000,-32000' test/phase-47-linux-non-regression.test.mjs`)
    - The Windows baseline array in the test ALSO contains the exact string `"--display=:99"` (matches iter15 source line 644 which emits it unconditionally; grep: `grep -A 60 "WIN32_ITER15_BASELINE" test/phase-47-linux-non-regression.test.mjs | grep -q -- '--display=:99'`) — this is the Wave-1 truth; Wave 2 will remove it from the baseline alongside the corresponding source change
    - Both test files use `import { test } from "node:test"` (grep: `grep -q 'from "node:test"' test/phase-47-launch-args.test.mjs && grep -q 'from "node:test"' test/phase-47-linux-non-regression.test.mjs`)
    - At RED, `node --test test/phase-47-launch-args.test.mjs` exits non-zero with output mentioning `buildChromiumLaunchArgs` not being exported (this confirms the rail is wired correctly)
    - `bash start.sh --dry-run` STILL exits 0 (test files do not touch production code; Linux must not regress)
  </acceptance_criteria>
  <done>RED tests committed; running them produces a clear failure pointing at the missing `buildChromiumLaunchArgs` export — exactly the failure mode Task 2's refactor will resolve. Linux dry-run still 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor launchBrowser to delegate args construction to a new exported pure function buildChromiumLaunchArgs (GREEN)</name>
  <files>src/server/ssr-render-host.mjs</files>
  <read_first>
    - src/server/ssr-render-host.mjs (lines 430-647 — current launchBrowser; the existing `args:` array literal must be moved verbatim into the new function; note line 644 `--display=${display}` is emitted unconditionally — Wave 1 preserves this; Wave 2 adds the gate)
    - test/phase-47-launch-args.test.mjs (just-created Task 1 file — defines the function's required signature/options)
    - test/phase-47-linux-non-regression.test.mjs (just-created — defines byte-identity contract; WIN32_ITER15_BASELINE includes `--display=:99` matching iter15)
    - .planning/phases/phase-47/47-RESEARCH.md (§ Recommended Implementation Approach — confirms refactor is M2 C1 and must be no-behavior-change)
  </read_first>
  <behavior>
    - `buildChromiumLaunchArgs({ platform, ssrUrl, viewport, display, disabledFeatures, enabledFeatures, hasVaapiEnabled })` is a pure function: no I/O, no env reads, no fs touches, no global state — only operates on its parameters.
    - It is `export`ed (not just module-internal) so the tests in Task 1 can import it.
    - Calling it with the iter15-equivalent options reproduces the iter15 args array byte-for-byte for both platform="linux" and platform="win32" (the snapshot tests pass). In particular, `--display=${display}` is emitted UNCONDITIONALLY (no isWin32 gate) — exactly matching iter15 source line 644. NO behavior change in Wave 1.
    - `launchBrowser()` is modified to call this function: the inline `args: [ ... ]` block at lines 558-645 is replaced with `args: buildChromiumLaunchArgs({ platform: process.platform, ssrUrl, viewport, display, disabledFeatures, enabledFeatures, hasVaapiEnabled })`.
    - No other lines of launchBrowser change. headless: still `false`. userDataDir / winUserDataDir logic unchanged. env block unchanged. The `winUserDataDir` mkdir, the disabledFeatures/enabledFeatures arrays, the hasVaapiEnabled derivation — all stay inside launchBrowser (impure — they read process.env / fs).
    - All Phase 32-46 inline comments inside the args block (h4, h15, h19, iter15 explanations) migrate INTO buildChromiumLaunchArgs to preserve archaeology.
    - Adjacent to `export function buildChromiumLaunchArgs`, a JSDoc header documents the function: parameters, return type, the "platform === 'win32'" branch list, and a pointer back to 47-RESEARCH.md § Q3 for the flag-by-flag rationale.
  </behavior>
  <action>
    Edit `src/server/ssr-render-host.mjs`:

    (1) Add a new exported function `buildChromiumLaunchArgs` near the top of the file, just after `getXvfbArgs` (around line 73). Signature exactly as in `<interfaces>` above. Function body: return the same array literal currently at lines 558-645, with `isWin32 = platform === "win32"` derived from the parameter (NOT from `process.platform`). Migrate the inline comments verbatim. The function MUST take ssrUrl/viewport/display/etc. as parameters — DO NOT read them from module scope inside the new function. The trailing `\`--display=\${display}\`` arg is emitted UNCONDITIONALLY (preserve iter15 exactly — no isWin32 gate in Wave 1).

    (2) Inside `launchBrowser()`, replace the inline `args: [ ... ]` block (lines 558-645) with:
    ```js
    args: buildChromiumLaunchArgs({
      platform: process.platform,
      ssrUrl,
      viewport,
      display,
      disabledFeatures,
      enabledFeatures,
      hasVaapiEnabled,
    }),
    ```
    Leave every other line of launchBrowser unchanged: headless: false stays, isWin32-conditional userDataDir block stays, env block stays.

    (3) Run `node --check src/server/ssr-render-host.mjs` to confirm syntax.

    (4) Run `node --test test/phase-47-launch-args.test.mjs test/phase-47-linux-non-regression.test.mjs` — all 9 tests MUST now pass (GREEN). If the Linux snapshot diff shows even one flag out of place, the baseline OR the refactor is wrong — fix the refactor (do NOT edit the baseline; the baseline is law). If WIN32_ITER15_BASELINE shows `--display=:99` missing from the function output, the refactor accidentally introduced a gate — remove it (Wave 1 is pure refactor; Wave 2 owns that change).

    (5) Run full `npm test` to confirm no regression. Expected: same 384 pass / 1 pre-existing fail / 19 skipped baseline. If a non-phase-47 test breaks, the refactor leaked behavior change — debug.

    (6) Run `bash start.sh --dry-run` and confirm exit code 0 (Linux launcher integrity).

    Commit with message `refactor(47-01): GREEN — extract buildChromiumLaunchArgs, pin iter15 baseline`.
  </action>
  <verify>
    <automated>node --check src/server/ssr-render-host.mjs &amp;&amp; node --test test/phase-47-launch-args.test.mjs test/phase-47-linux-non-regression.test.mjs &amp;&amp; npm test 2>&amp;1 | tail -5 &amp;&amp; bash start.sh --dry-run &gt;/dev/null 2>&amp;1 &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/server/ssr-render-host.mjs` contains the literal string `export function buildChromiumLaunchArgs` (grep: `grep -q "export function buildChromiumLaunchArgs" src/server/ssr-render-host.mjs`)
    - `launchBrowser()` no longer contains the inline `--no-sandbox` string in its args array — the only occurrence of `--no-sandbox` is inside `buildChromiumLaunchArgs` (grep: `grep -c -- '"--no-sandbox"' src/server/ssr-render-host.mjs` returns exactly `1`)
    - `launchBrowser()` body contains the literal string `args: buildChromiumLaunchArgs(` (grep: `grep -q "args: buildChromiumLaunchArgs(" src/server/ssr-render-host.mjs`)
    - `buildChromiumLaunchArgs` is a pure function — its body contains zero references to `process.env`, zero references to `existsSync(`, zero references to `status.encoderConfig`, zero references to `winUserDataDir` (grep block; each must return 0 matches inside the function body)
    - `headless: false` still appears in launchBrowser (grep: `grep -q "headless: false" src/server/ssr-render-host.mjs`) — Wave 2 will flip this; Wave 1 must NOT
    - The `--display=${display}` arg is emitted unconditionally inside `buildChromiumLaunchArgs` (grep: `grep -q -- '--display=\${display}' src/server/ssr-render-host.mjs` succeeds; and the surrounding source line is NOT inside an `isWin32 ?` ternary — verify by inspection that the `--display=` line in the function body is a bare template-literal arg, no ternary wrapper) — Wave 2 will add the gate
    - `node --check src/server/ssr-render-host.mjs` exits 0
    - `node --test test/phase-47-launch-args.test.mjs test/phase-47-linux-non-regression.test.mjs` exits 0 with all 9 tests passing
    - `npm test` reports `tests` count matches pre-refactor baseline +9 (the new tests). Specifically: `pass` count = baseline_pass + 9, `fail` count = 1 (pre-existing), `skipped` count = 19 (unchanged). Use: `npm test 2>&1 | grep -E "^# (tests|pass|fail|skipped)"` — the diff vs `git stash; npm test; git stash pop` should be exactly +9 tests/+9 pass.
    - `bash start.sh --dry-run` exits 0 (Linux non-regression rail green)
  </acceptance_criteria>
  <done>buildChromiumLaunchArgs extracted; both new test files green; full npm test green at +9 pass; Linux dry-run still 0; Wave 1 is a no-behavior-change refactor commit that installs the unit-test rail Wave 2 will lean on. `--display=` is still unconditional (iter15-faithful); Wave 2 owns the win32 gate.</done>
</task>

</tasks>

<verification>
- `node --check src/server/ssr-render-host.mjs` exits 0
- `node --test test/phase-47-launch-args.test.mjs test/phase-47-linux-non-regression.test.mjs` — all tests pass
- `npm test` — count increases by exactly 9 (new tests), pre-existing 1 fail + 19 skipped unchanged
- `bash start.sh --dry-run` exits 0
- `grep -c -- '"--no-sandbox"' src/server/ssr-render-host.mjs` returns `1` (proves the inline args block was removed, not duplicated)
- `grep -q "headless: false" src/server/ssr-render-host.mjs` succeeds (Wave 1 is pure refactor — Wave 2 flips this)
- Wave 1 truth-check: the `--display=` arg in `buildChromiumLaunchArgs` is NOT inside an `isWin32 ?` ternary — it's a bare unconditional emit. The win32 gate is a Wave 2 deliverable.
</verification>

<success_criteria>
- `buildChromiumLaunchArgs` exported, pure, parametrized
- Linux iter15 arg list pinned byte-identical in `test/phase-47-linux-non-regression.test.mjs`
- Windows iter15 arg list pinned byte-identical (still iter15 — Wave 2 will diverge it; includes `--display=:99` per iter15 source line 644)
- Zero behavior change on either platform — npm test baseline preserved, Linux dry-run green
- The unit-test rail required for Wave 2's safe headless-new flip is in place
</success_criteria>

<output>
After completion, create `.planning/phases/phase-47/47-01-SUMMARY.md` summarizing:
- The refactor delta (lines moved, no behavior change)
- The two new test files and their test counts (6 + 3 = 9 new tests)
- Confirmation that npm test count went up by exactly 9 with same pre-existing fail
- `bash start.sh --dry-run` exit code 0
- That Wave 2 is now safe to flip Windows defaults — Linux is guarded by snapshot
- Note that WIN32_ITER15_BASELINE in this wave includes `--display=:99` (matches iter15 source line 644); Wave 2 will remove it from both the source and the baseline in a single commit
</output>
</output>
