---
phase: 28
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/_helpers.mjs
  - test/board-profile-fields.test.mjs
  - test/board-json-roundtrip.test.mjs
  - test/auto-load-fallback.test.mjs
  - test/dashboard-hint-copy.test.mjs
  - test/asset-picker-dirty-gate.test.mjs
  - test/asset-delete-modal.test.mjs
  - test/asset-hash.test.mjs
  - test/asset-manifest.test.mjs
autonomous: true
requirements: [B1, B2, B3, B4, B5, B6]
must_haves:
  truths:
    - "node --test test/ runs and reports 0 failures (every test file declares at least one passing or pending placeholder so the suite has structure)."
    - "Eight Wave-0 test scaffolds exist in test/ — one per validation row in 28-VALIDATION.md (board-profile-fields, board-json-roundtrip, auto-load-fallback, dashboard-hint-copy, asset-picker-dirty-gate, asset-delete-modal, asset-hash, asset-manifest)."
    - "Each scaffold imports a tiny shared helper test/_helpers.mjs that exposes readJsonFile, withTempDir, and a minimal DOM stub (jsdom-free; uses a hand-rolled <document> shim sufficient for runtime-modal + asset-picker tests)."
    - "The scaffold tests use Node 24's builtin node:test and node:assert/strict — no npm dependency added; package.json is untouched."
    - "Initially every scaffold contains TWO test() calls: one trivial passing assertion (`assert.equal(1, 1)`) AND one `test.skip()` placeholder named exactly per the requirement ID it will fulfil (e.g. `test.skip(\"B1-D01: save flow updates lastUsedProfileName\", ...)`) — downstream waves replace the skip with real behavior assertions."
  artifacts:
    - path: "test/_helpers.mjs"
      provides: "readJsonFile, writeJsonFile, withTempDir, makeMinimalDocumentStub utilities for downstream test files"
      contains: "export function readJsonFile"
    - path: "test/board-profile-fields.test.mjs"
      provides: "Wave-0 placeholder for B1-D01 + path-traversal validation"
      contains: "test.skip(\"B1-D01"
    - path: "test/board-json-roundtrip.test.mjs"
      provides: "Wave-0 placeholder for B1-D02 boards/<id>.json round-trip"
      contains: "test.skip(\"B1-D02"
    - path: "test/auto-load-fallback.test.mjs"
      provides: "Wave-0 placeholder for B1-D03 fallback (no popup when absent)"
      contains: "test.skip(\"B1-D03"
    - path: "test/dashboard-hint-copy.test.mjs"
      provides: "Wave-0 placeholder for B2-D05 locked hint copy contract"
      contains: "test.skip(\"B2-D05"
    - path: "test/asset-picker-dirty-gate.test.mjs"
      provides: "Wave-0 placeholder for B3-D07 (3 cases) + B3-D08"
      contains: "test.skip(\"B3-D07"
    - path: "test/asset-delete-modal.test.mjs"
      provides: "Wave-0 placeholder for B4-D09/D10 (no confirm + reuse showConfirm)"
      contains: "test.skip(\"B4-D09"
    - path: "test/asset-hash.test.mjs"
      provides: "Wave-0 placeholder for B5-D11/D12 sha256[:12] truncation"
      contains: "test.skip(\"B5-D12"
    - path: "test/asset-manifest.test.mjs"
      provides: "Wave-0 placeholder for B5-D13 manifest read/write/sync"
      contains: "test.skip(\"B5-D13"
  key_links:
    - from: "test/_helpers.mjs"
      to: "every test/*.test.mjs"
      via: "ESM `import { ... } from \"./_helpers.mjs\";`"
      pattern: "from \"./_helpers.mjs\""
    - from: "node --test test/"
      to: "all 8 test files"
      via: "Node 24 builtin glob discovery of *.test.mjs"
      pattern: "node --test test/"
---

<objective>
Scaffold the test/ directory and create eight `*.test.mjs` placeholders matching the
Wave 0 list in `28-VALIDATION.md`. This is foundation work: every downstream plan
(01..05) replaces a `test.skip()` placeholder with a real behavioral assertion, so the
exact filenames + skip-test names locked here become the contract every subsequent plan
must satisfy.

Purpose: The codebase has zero pre-existing automated tests. Phase 28 is the first
phase to introduce them. Without a Wave 0 scaffold, downstream plans would each have
to bootstrap test infrastructure repeatedly — context waste. With Wave 0 done, every
later plan reads ONE existing skipped test and converts it to a real assertion.
Output: `test/` directory containing 8 scaffold files + `_helpers.mjs`. `node --test test/`
runs cleanly with N passing trivial assertions and N skipped (named) requirement-bound
placeholders. ZERO failures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/phase-28/28-CONTEXT.md
@.planning/phases/phase-28/28-RESEARCH.md
@.planning/phases/phase-28/28-VALIDATION.md
@.planning/phases/phase-28/28-BACKLOG.md

<interfaces>
<!-- Node 24 builtin test framework. No imports from project code in Wave 0 — these are
     pure scaffold files that don't yet exercise any production module. -->

Builtin Node 24 API used:
```javascript
import { test } from "node:test";
import assert from "node:assert/strict";

test("name", () => { /* assertions */ });
test.skip("name", () => { /* placeholder */ });
```

Project files referenced ONLY for filename + path constants (Wave 0 does NOT import
production code; downstream waves replace skips and do import production code):
- `config/boards/nemesis-board-a.json` — board JSON shape (read in 28-01).
- `server.mjs` BOARD_PROFILE_FIELDS — list at lines 36–51 (referenced in 28-01).
- `src/app/runtime/ui/animation-editor-edit-pane-asset-picker.js` — line 133 + 304 confirm() (referenced in 28-03).
- `src/app/runtime/ui/runtime-modal.js` — showConfirm signature (referenced in 28-03).
- `src/app/runtime/render/runtime-gif-playback.js` line 18 cache (referenced in 28-04).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create test/_helpers.mjs scaffold</name>
  <files>test/_helpers.mjs</files>
  <read_first>
    - .planning/phases/phase-28/28-VALIDATION.md (the eight Wave-0 file names + their requirement coverage are the contract)
    - .planning/phases/phase-28/28-RESEARCH.md §"Test Framework" (confirms Node 24 builtin, no npm deps)
  </read_first>
  <behavior>
    - readJsonFile(path) returns parsed JSON or throws clearly.
    - writeJsonFile(path, data) writes with 2-space indent + trailing newline.
    - withTempDir(prefix, fn) creates a unique temp dir under os.tmpdir(), passes its absolute path to fn (which may be async), and recursively cleans it up on completion or error.
    - makeMinimalDocumentStub() returns an object with createElement/getElementById/body shim sufficient for the asset-picker + runtime-modal unit tests in waves 28-03 and 28-04.
  </behavior>
  <action>
    Create `test/_helpers.mjs` as an ESM module exporting EXACTLY these named functions
    (no defaults, no class wrappers):
    
    1. `readJsonFile(absolutePath)` — uses `fs/promises` `readFile` + `JSON.parse`. Throws
       `new Error("readJsonFile failed: " + path + " — " + cause.message)` on any failure.
    2. `writeJsonFile(absolutePath, data)` — uses `fs/promises` `mkdir` (recursive) on
       dirname, then `writeFile` with `JSON.stringify(data, null, 2) + "\n"`.
    3. `withTempDir(prefix, fn)` — uses `os.tmpdir()` + `fs.mkdtempSync` to create
       `<tmp>/<prefix>-<random>`, awaits `fn(dirAbsolutePath)`, then `fs.rmSync(dir, { recursive: true, force: true })`
       in a `finally` block. Returns whatever `fn` returns.
    4. `makeMinimalDocumentStub()` — returns `{ body, createElement, getElementById, addEventListener }`
       where `body` exposes `append`, `appendChild`, `removeChild`, `dataset`, `classList`
       (toggle/add/remove), `setAttribute`, `removeAttribute`, and a child-list array.
       `createElement(tag)` returns a similar element. `getElementById(id)` looks up by
       a registered Map. `addEventListener` is a no-op stub. This is intentionally tiny
       — it is upgraded in waves 28-03 / 28-04 if real DOM interaction is needed.
    
    Use IIFE-pattern is NOT applicable for ESM tests — write standard ESM `export function`
    declarations. No third-party deps. Only `node:fs`, `node:fs/promises`, `node:os`,
    `node:path`, `node:url` builtins.
    
    Header comment at top of file:
    `// Phase 28 Wave 0 — shared helpers for *.test.mjs files. Pure Node 24 builtins.`
  </action>
  <verify>
    <automated>node --test test/ 2>&1 | tail -5 || true</automated>
    Expect: command runs (after subsequent task creates *.test.mjs files); helper file
    is syntactically valid ESM and importable.
  </verify>
  <acceptance_criteria>
    - File `test/_helpers.mjs` exists.
    - `node --check test/_helpers.mjs` exits 0 (no syntax errors).
    - `grep -c "^export function" test/_helpers.mjs` returns exactly 4 (readJsonFile, writeJsonFile, withTempDir, makeMinimalDocumentStub).
    - File contains the literal string `node:fs/promises` (proves use of Node builtins, not third-party fs lib).
    - File does NOT contain any `import .* from "[^.@]` pattern other than `node:` builtins (no npm deps).
  </acceptance_criteria>
  <done>test/_helpers.mjs exists, is syntactically valid ESM, exports exactly the four named functions, uses only `node:` builtins.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create eight test scaffold files with named skipped placeholders</name>
  <files>
    test/board-profile-fields.test.mjs,
    test/board-json-roundtrip.test.mjs,
    test/auto-load-fallback.test.mjs,
    test/dashboard-hint-copy.test.mjs,
    test/asset-picker-dirty-gate.test.mjs,
    test/asset-delete-modal.test.mjs,
    test/asset-hash.test.mjs,
    test/asset-manifest.test.mjs
  </files>
  <read_first>
    - test/_helpers.mjs (just created in Task 1)
    - .planning/phases/phase-28/28-VALIDATION.md (per-task requirement IDs locked)
    - .planning/phases/phase-28/28-CONTEXT.md (decision IDs D-01..D-21 — every test.skip() name MUST cite its decision ID)
  </read_first>
  <behavior>
    - Each file contains exactly two test() calls: one trivial passing test asserting `1 === 1` (so the file always exercises at least one assertion and the suite never has zero tests), and ONE OR MORE `test.skip()` placeholders named with the decision ID and a one-line summary.
    - `node --test test/` exits 0 with N passing tests and ≥8 skipped tests reported.
    - Skipped test names are stable contracts: downstream waves locate them by exact name (`grep -F "B1-D01: save flow updates lastUsedProfileName"`).
  </behavior>
  <action>
    Create eight ESM test files. Each file uses this exact template (with file-specific
    skip names per the table below):
    
    ```javascript
    // Phase 28 Wave 0 scaffold — <FILE_PURPOSE>.
    // Downstream wave <WAVE_N> replaces the skipped placeholders with real assertions.
    import { test } from "node:test";
    import assert from "node:assert/strict";
    // import * as helpers from "./_helpers.mjs";  // Re-enabled by downstream wave.
    
    test("scaffold: <FILE_NAME> loads", () => {
      // Trivial liveness check so the file always has at least one passing assertion.
      assert.equal(1, 1);
    });
    
    test.skip("<DECISION_ID>: <ONE_LINE_SUMMARY>", () => {
      // Downstream wave fills this in.
    });
    // additional test.skip() entries per skip table below
    ```
    
    Skip-name table (LOCKED contract — downstream plans grep these names verbatim):
    
    | File | Skip names (one test.skip per row) |
    |------|------------------------------------|
    | board-profile-fields.test.mjs | `B1-D01: save flow updates lastUsedProfileName` AND `B1-D01: lastUsedProfileName rejects path-traversal characters` |
    | board-json-roundtrip.test.mjs | `B1-D02: lastUsedProfileName persists in config/boards/<id>.json round-trip` |
    | auto-load-fallback.test.mjs | `B1-D03 fallback: null lastUsedProfileName loads default geometry without popup` |
    | dashboard-hint-copy.test.mjs | `B2-D05: hint copy locked — short chip is "Unsaved on /output/" and tooltip is the full sentence` |
    | asset-picker-dirty-gate.test.mjs | `B3-D07.1: upload of asset NOT in current selection fires no dirty` AND `B3-D07.2: upload with same content-hash fires no dirty` AND `B3-D07.3: upload with different content-hash fires dirty=true` AND `B3-D08: library-only delete fires no dirty` |
    | asset-delete-modal.test.mjs | `B4-D09: delete path does not call window.confirm` AND `B4-D10: delete path reuses TT_BEAMER_RUNTIME_MODAL.showConfirm with danger:true` |
    | asset-hash.test.mjs | `B5-D11/D12: sha256(bytes).slice(0,12) is deterministic and exactly 12 hex chars` AND `B5-D11: same content produces same hash` |
    | asset-manifest.test.mjs | `B5-D13: manifest round-trips — write then read returns the same hash + size + mtime` AND `B5-D13: missing manifest synthesizes from disk on boot` |
    
    Use the literal exclamation-free names above; no Markdown formatting inside test names.
    The decision-ID prefix is non-negotiable — the downstream wave locates the test by it.
    
    For waves 28-03 (asset-picker, asset-delete-modal) the `<WAVE_N>` placeholder in the
    header comment must read `Wave 3`. For 28-01: `Wave 1`. For 28-02 (dashboard-hint-copy):
    `Wave 2`. For 28-04 (asset-hash + asset-manifest): `Wave 4`. Auto-load-fallback +
    board-profile-fields + board-json-roundtrip → `Wave 1` (B1).
  </action>
  <verify>
    <automated>cd /home/claw/tt-beamer && node --test test/ 2>&1 | tail -10</automated>
    Expect: `# pass 8` (the eight scaffold "loads" tests) and `# tests <≥16>` (8 pass + ≥8 skip), with `# fail 0`. The exact "tests" total depends on skip count — at minimum 16, since some files have 2+ skips. NOT 0 fail.
  </verify>
  <acceptance_criteria>
    - All eight files exist in `test/` directory.
    - `node --test test/` exits with code 0.
    - `node --test test/ 2>&1 | grep -c "^ok "` reports ≥16 (8 passing scaffolds + ≥8 skipped placeholders, all listed as "ok" in TAP output even when skipped).
    - `node --test test/ 2>&1 | grep -c "^# fail 0"` reports 1 (suite has zero failures).
    - `grep -F "B1-D01: save flow updates lastUsedProfileName" test/board-profile-fields.test.mjs` returns the line (verifies the locked contract name).
    - `grep -F "B5-D11/D12: sha256(bytes).slice(0,12)" test/asset-hash.test.mjs` returns the line.
    - `grep -F "B4-D10: delete path reuses TT_BEAMER_RUNTIME_MODAL.showConfirm with danger:true" test/asset-delete-modal.test.mjs` returns the line.
    - `grep -F "B6" test/` returns no matches (B6 is manual-only per VALIDATION.md, so it has no test scaffold).
    - Each file imports `node:test` and `node:assert/strict` — no third-party imports.
  </acceptance_criteria>
  <done>Eight `*.test.mjs` scaffolds exist in `test/`, the suite passes 0 failures, every locked skip-name from the table above is grep-findable in its file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem ↔ test process | Test scaffolds may create + delete temp dirs via `withTempDir`. No write outside of `os.tmpdir()`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-W0-01 | Tampering | `test/_helpers.mjs` `withTempDir` | mitigate | dir name uses `fs.mkdtempSync(path.join(os.tmpdir(), prefix + "-"))` — Node enforces unpredictable suffix, no shell expansion. |
| T-28-W0-02 | Denial of Service | `node --test test/` | accept | Tests are placeholders running ~50ms total; no flood risk on a single-developer dev machine. |
| T-28-W0-03 | Information Disclosure | scaffold files | accept | No secrets in test scaffolds; placeholders contain only requirement-ID strings. |
</threat_model>

<verification>
- `cd /home/claw/tt-beamer && node --test test/` exits 0 with N pass + ≥8 skip.
- `ls test/*.test.mjs | wc -l` returns exactly 8.
- `node --check test/_helpers.mjs` exits 0.
- Every locked skip-name from the Task 2 table is grep-findable.
</verification>

<success_criteria>
- Wave 0 test scaffold complete: 8 `.test.mjs` files + `_helpers.mjs`.
- `node --test test/` runs cleanly (0 failures).
- Every requirement-ID in 28-VALIDATION.md `Per-Task Verification Map` (B1-D01, B1-D02, B1-D03, B2-D05, B3-D07.1/2/3, B3-D08, B4-D09, B4-D10, B5-D11, B5-D12, B5-D13) has a named test.skip() placeholder downstream waves can locate.
- No `package.json` modification, no `node_modules`, no third-party deps.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-28/28-00-SUMMARY.md` documenting:
1. The 8 scaffold filenames and their wave assignment.
2. The complete locked skip-name table (so downstream waves have an in-phase reference).
3. The `node --test test/` baseline output (pass/skip counts).
4. Any deviations from the plan (e.g., if a stub helper had to be expanded).
</output>
