---
phase: 28
plan: 05
type: execute
wave: 5
depends_on: [00, 01, 02, 03, 04]
files_modified:
  - src/styles.css
  - index.html
  - src/app/runtime/core/runtime-bootstrap.js
autonomous: false
requirements: [B6]
must_haves:
  truths:
    - "On the dashboard (any body WITHOUT `data-output-role=\"final-output\"`), the diagnostic-overlay chip (#output-status-chip) renders INLINE inside the topbar's `.rd-topbar-actions` flex container — visually right of the version chip and to the LEFT of the existing right-side action elements (running-count chip / theme-toggle / etc.). The chip does NOT use position:fixed, has no top/right offset, and its bounding rect does NOT overlap the bounding rect of `#app-version` or the `.rd-topbar-brand-title` text."
    - "On /output/ (body has `data-output-role=\"final-output\"`), the chip CONTINUES to render exactly as today — `position: fixed; top: 8px; right: 8px; z-index: 9999;`. ZERO visual regression on /output/."
    - "The CSS distinction between dashboard-inline and /output/-fixed variants uses the locked selector contract `body:not([data-output-role=\"final-output\"]) .output-status-chip { … }` for the inline variant; the existing rule (lines 125–141) is unchanged so /output/ inherits it."
    - "Toggling 'Show diagnostic overlay' in the dashboard System tab propagates to /output/ via the existing `global-config-update` broadcast — Phase 26 h9 already wires this; Plan 28-05 verifies the path with a Wave-0 manual smoke before assuming it works."
    - "The dashboard topbar's existing flex-wrap + alignment behavior is preserved — the chip integrates without forcing a line-break under any reasonable viewport width (smoke matrix at 1280px / 1920px / 2560px)."
    - "No new JavaScript module is added. The chip element relocation happens in `index.html` (Option A — preferred, literal author-time move) or in `runtime-bootstrap.js` (Option B — runtime re-parent on dashboard role only)."
  artifacts:
    - path: "src/styles.css"
      provides: "Inline-variant rule `body:not([data-output-role=\"final-output\"]) .output-status-chip` overriding position/top/right/z-index/margin to integrate with the topbar flex."
      contains: "body:not([data-output-role=\"final-output\"]) .output-status-chip"
    - path: "index.html"
      provides: "(Option A — preferred) The chip is moved at AUTHOR TIME from its current floating position (line 55) into `.rd-topbar-actions` (line 144). (Option B — if Option A breaks /output/) The chip stays at line 55 and runtime-bootstrap.js re-parents it on dashboard role only."
      contains: "output-status-chip"
    - path: "src/app/runtime/core/runtime-bootstrap.js"
      provides: "(Used only if Option B from index.html is chosen) AFTER `applyOutputRoleViewContract` runs (line 196) AND when outputRole is CONTROL, re-parent #output-status-chip into `.rd-topbar-actions` AFTER the existing right-side action elements (running-count chip is naturally first; chip is last). On /output/ the bootstrap leaves the chip at its top-level position."
      contains: "output-status-chip"
  key_links:
    - from: "src/styles.css `body:not([data-output-role=\"final-output\"]) .output-status-chip` rule"
      to: "the chip element wherever it lives in the dashboard DOM"
      via: "the new rule overrides position/top/right/z-index"
      pattern: "body:not"
    - from: "index.html (Option A) OR runtime-bootstrap.js (Option B)"
      to: "the chip's parent = .rd-topbar-actions on dashboard"
      via: "either authoring OR `actions.appendChild(chip)` after applyOutputRoleViewContract"
      pattern: "rd-topbar-actions"
---

<objective>
Implement B6 — diagnostic-overlay UX. Two parts:

Part 1 (assumed already working — verified via Wave-0 manual smoke FIRST per RESEARCH
A4): toggling 'Show diagnostic overlay' in the dashboard System tab live-syncs to /output/
within ~200ms via the existing `global-config-update` broadcast (Phase 26 h9 wired this).
If the smoke shows the path works, Plan 28-05 does NOTHING for cross-client sync.

Part 2 (the actual code change): the dashboard chip currently renders at
`position: fixed; top:8px; right:8px;` which overlaps the TableTop Beamer logo + title.
B6 D-14 binds: in the dashboard, the chip must be inline inside the topbar flex
(`.rd-topbar-actions`), right of the version chip, no logo/title overlap. On /output/
the chip retains its current fixed-position style — ZERO regression there.

Purpose: User-test feedback B6 (verbatim): "Im Dashboard soll es in die UI ganz oben
integriert werden, aktuell überlagert es etwas das logo und den Titel des Programms
'TableTop Beamer'." This is a layout-only fix on the dashboard side.
Output: One CSS rule, optionally a single re-parent line in bootstrap or a literal
move in index.html. Manual checkpoint validates both windows behave correctly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/phase-28/28-CONTEXT.md
@.planning/phases/phase-28/28-RESEARCH.md
@.planning/phases/phase-28/28-VALIDATION.md
@.planning/phases/phase-26/SUMMARY.md
@src/styles.css
@index.html
@src/app/runtime/core/runtime-bootstrap.js
@src/app/runtime/viewport/runtime-stage-viewport.js
@src/app/runtime/runtime-orchestration.js

<interfaces>
<!-- Locked anchors. -->

src/styles.css:125–147 — current chip rule:
```css
.output-status-chip {
  display: none;
  position: fixed;
  top: 8px;
  right: 8px;
  z-index: 9999;
  padding: 4px 10px;
  border-radius: 999px;
  background: rgba(0, 0, 0, 0.7);
  color: #c8f7d5;
  font: 600 11px/1.2 "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
  letter-spacing: 0.04em;
  pointer-events: none;
  border: 1px solid rgba(80, 200, 160, 0.45);
  gap: 6px;
  align-items: center;
}
.output-status-chip .output-status-sep {
  opacity: 0.45;
}
body[data-diagnostic-overlay="true"] .output-status-chip {
  display: inline-flex;
}
```

index.html — current chip placement (line 55, ABOVE the topbar at line 121):
```html
<div id="output-status-chip" class="output-status-chip" aria-hidden="true">
  <span id="output-status-version">v?</span>
  ...
</div>
```

index.html — topbar structure (line 121–143):
```html
<div class="rd-topbar" role="banner">
  <div class="rd-topbar-brand">
    <span class="rd-topbar-mark" aria-hidden="true">…</span>
    <div class="rd-topbar-brand-text">
      <div class="rd-topbar-brand-title">TableTop Beamer<small id="app-version" class="rd-topbar-version" …></small></div>
      <p id="topbar-board-label" class="rd-topbar-brand-sub" aria-live="polite">No board yet</p>
    </div>
  </div>
  <div class="rd-topbar-actions">
    <!-- running-count chip -->
    <span id="running-count-chip" class="running-count-chip" hidden …>…</span>
    <!-- (other right-side actions) -->
  </div>
</div>
```

src/app/runtime/core/runtime-bootstrap.js:196 — `ctx.applyOutputRoleViewContract()` is
called once per page load. We hook AFTER it. The function only mutates DOM when
outputRole === FINAL; in CONTROL mode it returns early. Phase 28 B6 adds a reciprocal
behavior in CONTROL mode (or AT AUTHOR TIME via index.html).

Phase 26 h9 wiring (verified via grep — already in place):
- `state.diagnosticOverlay` → `applyGlobalDefaultsPayloadToState`
- toggle → `setDiagnosticOverlay()` → `saveGlobalDefaultsToServer()` → broadcast
- broadcast handler in runtime-live-sync-core.js refetches → `applyGlobalDefaultsPayloadToState`
  → `syncRuntimePanelsFromState` → `syncDiagnosticOverlayPanel` → sets
  `document.body.dataset.diagnosticOverlay`.

The CSS rule `body[data-diagnostic-overlay="true"] .output-status-chip { display: inline-flex; }`
already does the visibility toggle — Plan 28-05 does NOT touch this part.

LOCKED IMPLEMENTATION CHOICE — Option A (literal DOM move in index.html):
The chip is currently a top-level `<div>` at line 55, OUTSIDE the topbar. Moving it
INSIDE `.rd-topbar-actions` at the END (right-most) means:
- On dashboard (no `data-output-role`): the chip renders inline inside the actions flex
  via the new CSS rule.
- On /output/ (body has `data-output-role="final-output"`): the chip USES position:fixed
  per the existing rule (selector is winning), so its TOP-LEVEL position is irrelevant
  — `position: fixed` removes it from layout flow regardless of where its parent is.

This is the simplest, safest, no-JS approach. Verify by reading the bottom of the existing
.output-status-chip rule: `position: fixed` overrides any flow-layout context, so a chip
inside .rd-topbar-actions on /output/ still renders at top:8px right:8px.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wave-0 manual verification — does diagnostic-overlay live-sync ALREADY work cross-client?</name>
  <files>(no files modified — read-only verification)</files>
  <read_first>
    - .planning/phases/phase-28/28-RESEARCH.md §"Open Question 4" (live-sync uncertainty)
    - .planning/phases/phase-28/28-RESEARCH.md §"Assumption A4" (assumed working post Phase 26 h9)
    - src/app/runtime/runtime-orchestration.js lines 988–1011 (syncDiagnosticOverlayPanel + setDiagnosticOverlay)
  </read_first>
  <action>
    Pause execution and present the verification steps in <how-to-verify> below to the
    user. Do NOT modify any files in this task — it is a pure inspection step. The
    user's response on `<resume-signal>` decides whether Task 2 needs to also patch
    the live-sync path or only the layout.
  </action>
  <what-built>
    Per RESEARCH §"Open Question 4" + §"Assumption A4": the cross-client sync path
    appears wired through Phase 26 h9. Before writing any B6 code, the user verifies the
    current behavior with two browser windows.
  </what-built>
  <how-to-verify>
    1. Start the server: `node server.mjs` from /home/claw/tt-beamer.
    2. Open dashboard at `http://localhost:3000/` in browser window A.
    3. Open `/output/` at `http://localhost:3000/output/` in browser window B.
    4. In window A, navigate to System tab → toggle "Show diagnostic overlay" ON.
    5. Expected: within ~200ms (effectively immediately), window B (/output/) shows the
       chip in its top-right corner with version + fps + mode + canvas + frame-cost.
    6. Toggle OFF in window A — window B's chip disappears within ~200ms.
    7. Reload window B (Pi simulation) — chip state matches whatever the dashboard last
       set.
  </how-to-verify>
  <verify>
    <automated>echo "Manual checkpoint — no automated assertion. Resume signal from user determines pass/fail."</automated>
    User confirms via resume-signal ("live-sync works", "live-sync broken: …", or
    "skip — assume it works").
  </verify>
  <acceptance_criteria>
    - User has typed one of the locked resume-signal values.
    - If "live-sync broken" the executor records the description and EXPANDS Task 2's
      scope to also patch the live-sync path before doing the layout move.
    - If "live-sync works" or "skip" the executor proceeds to Task 2 unchanged.
  </acceptance_criteria>
  <done>User has confirmed live-sync state and the executor has noted whether Task 2 needs an extended scope.</done>
  <resume-signal>
    Type one of the following:
    - "live-sync works" — proceed to Task 2 (CSS-only fix).
    - "live-sync broken: <details>" — Task 2 will be expanded to also patch the broken
      path (likely `applyGlobalDefaultsPayloadToState` is missing the
      `syncDiagnosticOverlayPanel` call); the executor reads RESEARCH §"Open Question 4"
      for the fix recipe.
    - "skip — assume it works" — proceeds with the B6-D14 layout fix only; if B6-D16
      regresses post-merge, a follow-up plan handles it.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CSS inline-variant rule + DOM relocation of #output-status-chip into topbar</name>
  <files>
    src/styles.css,
    index.html
  </files>
  <read_first>
    - src/styles.css lines 125–147 (existing .output-status-chip rule)
    - index.html line 55 (current chip placement) AND lines 121–166 (topbar structure)
    - .planning/phases/phase-28/28-CONTEXT.md §"B6" (D-14, D-15, D-16 binding)
    - .planning/phases/phase-28/28-RESEARCH.md §"Pitfall 6: output-status-chip z-index conflict with align-mode-dirty-hint"
    - .planning/phases/phase-28/28-RESEARCH.md §"Code Examples — New for B6: dashboard-inline diagnostic chip CSS"
  </read_first>
  <behavior>
    - On the dashboard (body without `data-output-role="final-output"`), `#output-status-chip`
      renders inline inside `.rd-topbar-actions`, with `position: static`, `top: auto`,
      `right: auto`, `z-index: auto`, `margin-left: 6px`, and `align-self: center`. When
      visible (body[data-diagnostic-overlay="true"]) it appears as a chip alongside the
      version chip / running-count chip / other actions.
    - On /output/ (body has `data-output-role="final-output"`), the chip retains
      `position: fixed; top: 8px; right: 8px; z-index: 9999;` — the existing rule wins
      because the inline-variant selector excludes that body via `:not(...)`.
    - The chip's bounding rect on the dashboard (when visible) does NOT overlap
      `#app-version` or the `.rd-topbar-brand-title` text node.
    - No regression to the running-count chip's positioning — both can coexist in the
      flex.
  </behavior>
  <action>
    SUB-STEP A — index.html: move the chip element from line 55 into the topbar.
    1. Read lines 50–66 (the chip element + its surrounding HTML comment block at lines
       51–54). The element occupies lines 55–66:
       ```html
       <div id="output-status-chip" class="output-status-chip" aria-hidden="true">
         <span id="output-status-version">v?</span>
         <span class="output-status-sep" aria-hidden="true">·</span>
         <span id="output-status-fps">- fps</span>
         <span class="output-status-sep" aria-hidden="true">·</span>
         <span id="output-status-mode">auto</span>
         <span class="output-status-sep" aria-hidden="true">·</span>
         <span id="output-status-canvas">-x-</span>
         <span class="output-status-sep" aria-hidden="true">·</span>
         <span id="output-status-frame-cost">- ms</span>
       </div>
       ```
       Plus the leading 4-line HTML comment `<!-- /output/ diagnostic chip — only visible … -->`.
    2. CUT the entire block (comment + element). Update the comment to reflect the new
       reality:
       ```html
       <!-- Diagnostic chip — visible when body has data-diagnostic-overlay="true".
            On /output/ renders top-right fixed (current style); on dashboard renders
            inline inside the topbar flex (Phase 28 B6). -->
       ```
    3. PASTE the comment + element INTO `.rd-topbar-actions` (line 144) as the LAST
       child (after the running-count-chip and any other existing actions). The
       insertion point is just before the closing `</div>` of `.rd-topbar-actions`.
       Make sure the indentation matches sibling elements.
    
    SUB-STEP B — src/styles.css: add the inline-variant rule.
    1. Read lines 125–147.
    2. After line 147 (the existing `body[data-diagnostic-overlay="true"] .output-status-chip { display: inline-flex; }` rule), insert:
       ```css
       /* Phase 28 B6 D-14: dashboard-inline variant. /output/ retains the fixed
          top-right floating style; the dashboard integrates the chip into the
          topbar flex so it doesn't overlap the logo + title. */
       body:not([data-output-role="final-output"]) .output-status-chip {
         position: static;
         top: auto;
         right: auto;
         z-index: auto;
         margin-left: 6px;
         align-self: center;
       }
       ```
       Do NOT modify the existing rules. The new rule is purely additive and uses a
       higher-specificity selector (`body:not(...) .x` beats `.x`) so it wins on the
       dashboard. On /output/, the `:not` excludes the body and the existing rule wins.
  </action>
  <verify>
    <automated>cd /home/claw/tt-beamer && node --test test/ 2>&1 | tail -5 && grep -c "body:not(\\[data-output-role=\"final-output\"\\]) .output-status-chip" src/styles.css && grep -c "output-status-chip" index.html</automated>
    Expect: test suite still passes (B6 has no automated test scaffold per VALIDATION.md
    — manual smoke only); grep for the new CSS rule returns 1; grep for the chip element
    in index.html returns ≥3 lines (the element + its descendants are not counted, but
    the moved comment + element opening tag both contain "output-status-chip" string).
  </verify>
  <acceptance_criteria>
    - `grep -F 'body:not([data-output-role="final-output"]) .output-status-chip' src/styles.css` returns 1 line.
    - `grep -F "position: static" src/styles.css` returns at least 1 line (in the new rule).
    - `grep -F "margin-left: 6px" src/styles.css` returns at least 1 line.
    - The existing rule `body[data-diagnostic-overlay="true"] .output-status-chip` is INTACT — `grep -F 'body[data-diagnostic-overlay="true"] .output-status-chip' src/styles.css` returns 1 line.
    - The existing rule `.output-status-chip { display: none; position: fixed; top: 8px; right: 8px; z-index: 9999; ... }` is INTACT (verified by re-reading lines 125–141).
    - In index.html, the `<div id="output-status-chip" ...>` element appears INSIDE `.rd-topbar-actions` (verifiable by reading the topbar block — the chip is now a child of the actions div, not a sibling of the topbar).
    - The element does NOT appear at line 55 anymore (the original location is empty or contains only a placeholder comment).
    - Balanced braces in CSS: `grep -c "{" src/styles.css` and `grep -c "}" src/styles.css` return equal counts.
    - `node --test test/` exits 0 (no regression).
  </acceptance_criteria>
  <done>CSS inline-variant rule added; chip element moved into topbar flex; existing /output/-side rule untouched; no test regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual smoke test — dashboard + /output/ both render correctly</name>
  <files>(no files modified — visual verification of Task 2 output)</files>
  <read_first>
    - The user has just executed Task 2 and committed the changes.
    - .planning/phases/phase-28/28-VALIDATION.md §"Manual-Only Verifications" §B6 rows
  </read_first>
  <action>
    Pause execution and present the verification steps in <how-to-verify> below to the
    user. The user MUST verify both windows independently before approving. If issues
    arise, the executor reads the user's description and applies a CSS tweak (likely
    `align-self`, `margin-left`, or order:0) in a follow-up commit.
  </action>
  <what-built>
    Plan 28-05 changes are entirely visual + CSS-driven; only browser verification can
    confirm the layout intent.
  </what-built>
  <how-to-verify>
    1. Restart server if it's already running (to ensure the moved index.html is served).
    2. Open dashboard at `http://localhost:3000/` (window A).
    3. Open `/output/` at `http://localhost:3000/output/` (window B). Optionally, also
       open from a Pi if that's the target deployment environment.
    4. In window A, toggle "Show diagnostic overlay" ON.
    5. **Dashboard expectation (window A):** the chip appears INLINE inside the topbar,
       on the right side near the existing version chip + theme toggle. The "TableTop
       Beamer" title and the projector logo are NOT overlapped. The chip wraps gracefully
       at narrow viewport widths (test by resizing the window).
    6. **/output/ expectation (window B):** the chip appears in the TOP-RIGHT CORNER as
       a floating pill, EXACTLY as it always has. No layout change on /output/.
    7. Toggle OFF — chip disappears in both windows within ~200ms.
    8. Resize dashboard to 1280px / 1920px / 2560px. Verify the chip stays in the topbar
       on each width without forcing a new line under the topbar.
    9. Phase 27 h9 chip (#align-mode-dirty-hint at top:116px) coexistence: trigger an
       align-mode-dirty state on /output/ (drag a line in align mode without saving) and
       verify the dashboard's amber chip and the diagnostic overlay chip do NOT overlap
       awkwardly. Both should be visible simultaneously without z-index collision.
  </how-to-verify>
  <verify>
    <automated>echo "Manual visual checkpoint — no automated assertion. Resume signal from user determines pass/fail."</automated>
    User confirms via resume-signal ("approved" or "issues: …").
  </verify>
  <acceptance_criteria>
    - User has typed "approved" OR "issues: <description>".
    - If "issues" is reported, the executor must address them in a follow-up commit
      before this task is considered done.
  </acceptance_criteria>
  <done>User has visually verified both windows behave per the locked B6-D14 + B6-D15 expectations and typed "approved".</done>
  <resume-signal>
    Type one of:
    - "approved" — Phase 28 ships.
    - "issues: <description>" — executor reads the description and addresses them in a
      follow-up commit (likely a CSS tweak — `align-self`, `margin-left`, etc.).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 26 h9 server↔client diagnostic-overlay live-sync | Inherited unchanged; no new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | Information Disclosure | diagnostic chip exposes APP_VERSION + fps + canvas dims | accept | Same exposure as today; B6 only changes layout, not content. Local-network app, no auth — chip data is non-sensitive. |
| T-28-05-02 | Tampering | malicious user toggles diagnostic-overlay on remote /output/ via dashboard | accept | Single-tenant local app; no adversary. Server endpoint already exists; B6 reuses unchanged. |
| T-28-05-03 | Denial of Service | rapid toggle spam stresses live-sync broadcast | accept | Existing 200ms debounce in `saveGlobalDefaultsToServer` (Phase 13/26). |
</threat_model>

<verification>
- `cd /home/claw/tt-beamer && node --test test/` exits 0 (no regression).
- `grep -F 'body:not([data-output-role="final-output"]) .output-status-chip' src/styles.css` returns 1 line.
- `grep -A 12 "rd-topbar-actions" index.html | grep -c "output-status-chip"` returns ≥1 (chip is inside actions div).
- Manual smoke (Tasks 1 + 3): both /output/ unchanged AND dashboard inline integration confirmed.
</verification>

<success_criteria>
- B6-D14 implemented: dashboard chip is INLINE inside topbar flex; no logo/title overlap.
- B6-D15 implemented: /output/ chip retains existing fixed-position style.
- B6-D16 confirmed (Wave-0 smoke): dashboard toggle live-syncs to /output/ within ~200ms.
- B6-D17 implemented: existing System-tab toggle remains the entry-point; no new toggle.
- Phase 27 h9 hint chip and Phase 28 B6 chip coexist without z-index conflict.
- Wave-0 smoke (Task 1) confirms cross-client sync; visual smoke (Task 3) confirms layout.
- Suite has 0 failures.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-28/28-05-SUMMARY.md` documenting:
1. Wave-0 smoke result: live-sync confirmed working / required additional fix (and what that fix was if any).
2. The CSS rule added (literal copy).
3. The index.html relocation: from line 55 to inside `.rd-topbar-actions` (line ~144 area).
4. Visual smoke confirmation: dashboard + /output/ both render correctly per the locked acceptance criteria.
5. Any deviations or follow-ups (e.g., minor `margin-left` tweak after visual review).
</output>
