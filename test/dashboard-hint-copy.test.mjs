// Phase 28 Wave 0 scaffold — dashboard-hint-copy (B2 board-switch hint chip copy contract).
// Downstream wave Wave 2 replaces the skipped placeholders with real assertions.
import { test } from "node:test";
import assert from "node:assert/strict";
// import * as helpers from "./_helpers.mjs";  // Re-enabled by downstream wave.

test("scaffold: dashboard-hint-copy.test.mjs loads", () => {
  // Trivial liveness check so the file always has at least one passing assertion.
  assert.equal(1, 1);
});

test.skip("B2-D05: hint copy locked — short chip is \"Unsaved on /output/\" and tooltip is the full sentence", () => {
  // Downstream wave fills this in.
});
